name: Run and debug tests

on:
  workflow_dispatch:
    inputs:
      pull_number:
        description: "Pull request number, ex: 12345"
        required: true
        type: string

jobs:
  prepare-merge:
    runs-on: [self-hosted, tiny-worker]
    timeout-minutes: 600
    outputs:
      commit_sha: ${{ steps.prepare-merge.outputs.commit_sha }}
    steps:
      - name: Checkout workflow repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          sparse-checkout: |
            .github/scripts/prepare_merge_ref.py
          sparse-checkout-cone-mode: false
          token: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}

      - name: Prepare merge commit for PR
        id: prepare-merge
        env:
          GH_TOKEN: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail

          if ! [[ "${{ inputs.pull_number }}" =~ ^[0-9]+$ ]]; then
            echo "pull_number must be numeric"
            exit 1
          fi

          set +e
          python3 .github/scripts/prepare_merge_ref.py "${{ inputs.pull_number }}" --verbose --json-out merge_info.json
          script_exit=$?
          set -e

          if [[ $script_exit -ne 0 && $script_exit -ne 2 ]]; then
            echo "prepare_merge_ref.py failed unexpectedly with exit code $script_exit"
            exit 1
          fi

          if [[ ! -f merge_info.json ]]; then
            echo "merge_info.json was not created"
            exit 1
          fi

          MERGEABLE=$(jq -r '.mergeable // false' merge_info.json)
          MERGE_SHA=$(jq -r '.merge_commit_sha // empty' merge_info.json)

          if [[ "$MERGEABLE" != "true" || -z "$MERGE_SHA" ]]; then
            echo "Unable to resolve mergeable merge commit for PR #${{ inputs.pull_number }}"
            jq -r '.error // "PR is not mergeable or merge_commit_sha is null"' merge_info.json
            exit 1
          fi

          echo "commit_sha=$MERGE_SHA" >> "$GITHUB_OUTPUT"
          echo "Resolved PR #${{ inputs.pull_number }} to merge commit: $MERGE_SHA"

  build_and_test:
    needs:
      - prepare-merge
    if: needs.prepare-merge.outputs.commit_sha != ''
    strategy:
      fail-fast: false
      matrix:
        include:
          - build_preset: relwithdebinfo
            threads_count: 52
            timeout: 240
            build_target: "ydb/"
            test_size: small,medium
          - build_preset: release-asan
            threads_count: 52
            timeout: 240
            build_target: "ydb/"
            test_size: small,medium
    runs-on: [ self-hosted, auto-provisioned, "${{ format('build-preset-{0}', matrix.build_preset) }}" ]
    timeout-minutes: ${{ matrix.timeout }}
    name: Build and test ${{ matrix.build_preset }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare-merge.outputs.commit_sha }}
          fetch-depth: 2
      - name: Setup ydb access
        uses: ./.github/actions/setup_ci_ydb_service_account_key_file_credentials
        with:
          ci_ydb_service_account_key_file_credentials: ${{ secrets.CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS }}
      - name: Build and test
        uses: ./.github/actions/build_and_test_ya
        with:
          build_preset: ${{ matrix.build_preset }}
          build_target: ${{ matrix.build_target }}
          increment: true
          run_tests: true
          test_size: ${{ matrix.test_size }}
          test_threads: ${{ matrix.threads_count }}
          put_build_results_to_cache: true
          additional_ya_make_args: -DDEBUGINFO_LINES_ONLY
          secs: ${{ format('{{"AWS_KEY_ID":"{0}","AWS_KEY_VALUE":"{1}","REMOTE_CACHE_USERNAME":"{2}","REMOTE_CACHE_PASSWORD":"{3}","TELEGRAM_YDBOT_TOKEN":"{4}"}}',
            secrets.AWS_KEY_ID, secrets.AWS_KEY_VALUE, secrets.REMOTE_CACHE_USERNAME, secrets.REMOTE_CACHE_PASSWORD, secrets.TELEGRAM_YDBOT_TOKEN ) }}
          vars: ${{ format('{{"AWS_BUCKET":"{0}","AWS_ENDPOINT":"{1}","REMOTE_CACHE_URL":"{2}","GH_ALERTS_TG_LOGINS":"{3}","GH_ALERTS_TG_CHAT":"{4}"}}',
            vars.AWS_BUCKET, vars.AWS_ENDPOINT, vars.REMOTE_CACHE_URL_YA, vars.GH_ALERTS_TG_LOGINS, vars.GH_ALERTS_TG_CHAT ) }}
