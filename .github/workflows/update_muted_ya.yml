name: Update Muted tests

on:
  schedule:
    - cron: "0 * * * *"  # Every hour at :00 UTC
  workflow_dispatch:
    inputs:
      branches:
        description: 'Optional: comma-separated branches instead of stable_tests_branches.json. Empty = use stable_tests_branches.json from main.'
        required: false
        default: ''
      build_types:
        description: 'Comma-separated presets or "all" (default). Cannot request presets outside the allowed list passed to mute_update_workflow_matrix.py in this workflow. Matrix keeps only (branch, preset) where the mute file exists on origin/<branch>.'
        required: false
        default: all
        type: string

env:
  # workflow_dispatch: branch selected in UI. schedule: repository default branch.
  WORKFLOW_CHECKOUT_REF: ${{ github.event_name == 'workflow_dispatch' && github.ref || github.event.repository.default_branch }}
  BRANCHES_CONFIG_PATH: .github/config/stable_tests_branches.json
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_BRANCH_PREFIX: update-muted-ya
  REVIEWERS: '["ci"]'
  LABELS: "mute-unmute,not-for-changelog,ok-to-test,automerge"

jobs:
  setup:
    runs-on: [ self-hosted, auto-provisioned, build-preset-analytic-node]
    outputs:
      matrix_include: ${{ steps.set-mute-matrix.outputs.matrix_include }}
    steps:
      - name: Checkout branch list and mute files
        uses: actions/checkout@v4
        with:
          ref: ${{ env.WORKFLOW_CHECKOUT_REF }}
          path: config-repo
          sparse-checkout: |
            .github/config/
            .github/scripts/tests/mute_update_workflow_matrix.py
            .github/scripts/tests/resolve_muted_ya_path.py
          sparse-checkout-cone-mode: false

      - id: set-mute-matrix
        env:
          INPUT_BRANCHES: ${{ github.event.inputs.branches || '' }}
          INPUT_BUILD_TYPES: ${{ github.event.inputs.build_types || '' }}
        run: |
          set -euo pipefail
          python3 config-repo/.github/scripts/tests/mute_update_workflow_matrix.py matrix \
            --branches-file "config-repo/${{ env.BRANCHES_CONFIG_PATH }}" \
            --git-cwd config-repo \
            --allowed-build-types relwithdebinfo,release-asan

  collect-testowners-and-sync:
    runs-on: [ self-hosted, auto-provisioned, build-preset-analytic-node]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.WORKFLOW_CHECKOUT_REF }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ydb[yc] codeowners requests

      - name: Setup ydb access
        uses: ./.github/actions/setup_ci_ydb_service_account_key_file_credentials
        with:
          ci_ydb_service_account_key_file_credentials: ${{ secrets.CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS }}

      - name: Collect testowners
        run: python3 .github/scripts/analytics/upload_testowners.py

      # ``mute/manual_unmute.py sync`` selects CLOSED+COMPLETED candidates from YDB ``issues``;
      # refresh that table here so fast-unmute does not wait for collect_analytics_fast (~30 min).
      - name: Export GitHub issues to YDB
        env:
          GITHUB_TOKEN: ${{ secrets.YDBOT_TOKEN }}
        run: python3 .github/scripts/analytics/export_issues_to_ydb.py

      - name: Sync manual fast-unmute state
        env:
          GITHUB_TOKEN: ${{ secrets.YDBOT_TOKEN }}
        run: python3 .github/scripts/tests/mute/manual_unmute.py sync

  update-muted-tests:
    needs: [setup, collect-testowners-and-sync]
    runs-on: [ self-hosted, auto-provisioned, build-preset-analytic-node]
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.setup.outputs.matrix_include) }}
    steps:
      - name: Checkout workflow branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.YDBOT_TOKEN }}
          ref: ${{ env.WORKFLOW_CHECKOUT_REF }}
          fetch-depth: 0

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ydb[yc] PyGithub pandas numpy requests

      - name: Setup ydb access
        uses: ./.github/actions/setup_ci_ydb_service_account_key_file_credentials
        with:
          ci_ydb_service_account_key_file_credentials: ${{ secrets.CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS }}

      - name: Resolve mute file and fetch base from target branch
        id: mute_gate
        run: |
          set -euo pipefail
          python3 .github/scripts/tests/mute_update_workflow_matrix.py prepare-job \
            --repo-root . \
            --base-branch "${{ matrix.BASE_BRANCH }}" \
            --build-type "${{ matrix.BUILD_TYPE }}" \
            --pr-branch-prefix "${{ env.PR_BRANCH_PREFIX }}"

      - name: Collect test history data
        if: steps.mute_gate.outputs.skip != 'true'
        run: python3 .github/scripts/analytics/flaky_tests_history.py --branch=${{ env.BASE_BRANCH }} --build_type=${{ env.BUILD_TYPE }}

      - name: Update muted tests in DB
        if: steps.mute_gate.outputs.skip != 'true'
        run: python3 .github/scripts/tests/get_muted_tests.py upload_muted_tests --branch=${{ env.BASE_BRANCH }} --build_type=${{ env.BUILD_TYPE }} --muted_ya_file=base_muted_ya.txt

      - name: Update test monitor
        if: steps.mute_gate.outputs.skip != 'true'
        run: python3 .github/scripts/analytics/tests_monitor.py --branch=${{ env.BASE_BRANCH }} --build_type=${{ env.BUILD_TYPE }}

      - name: Generate new muted_ya.txt
        if: steps.mute_gate.outputs.skip != 'true'
        run: |
          .github/scripts/tests/mute/create_new_muted_ya.py update_muted_ya \
            --branch=${{ env.BASE_BRANCH }} \
            --muted_ya_file=base_muted_ya.txt \
            --build-type=${{ env.BUILD_TYPE }}

      - name: Check if changes exist
        if: steps.mute_gate.outputs.skip != 'true'
        id: changes_check
        run: |
          # Compare base_muted_ya.txt with newly generated file
          if git diff --no-index --quiet base_muted_ya.txt mute_update/new_muted_ya.txt; then
            echo "No changes detected in muted_ya.txt"
            echo "changes=false" >> $GITHUB_ENV
          else
            echo "Changes detected in muted_ya.txt"
            echo "changes=true" >> $GITHUB_ENV
          fi

      - name: Setup git config
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        run: |
          git config user.name YDBot
          git config user.email ydbot@ydb.tech

      - name: Create PR branch and apply changes
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        run: |
          # Создаем PR ветку на основе целевой ветки
          git checkout -B ${{ env.PR_BRANCH }} origin/${{ env.BASE_BRANCH }}

          OUT=$(python3 .github/scripts/tests/resolve_muted_ya_path.py --print-dedicated-relative --preset "${{ env.BUILD_TYPE }}")
          mkdir -p "$(dirname "$OUT")"
          cp mute_update/new_muted_ya.txt "$OUT"

          git add "$OUT"
          git commit -m "Update mute list (${{ env.BUILD_TYPE }}) for ${{ env.BASE_BRANCH }}"

          echo "✓ PR branch ${{ env.PR_BRANCH }} created with changes ($OUT)"

      - name: Collect PR description
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        run: |
          
          PR_BODY=''
          PR_BODY_FILE="pr_body_content.txt"
          
          PR_BODY+=$'# Muted tests update for ${{ env.BASE_BRANCH }} (build: ${{ env.BUILD_TYPE }})\n\n'

          if [ -s mute_update/to_delete_debug.txt ]; then
              DELETED_COUNT=$(wc -l < mute_update/to_delete_debug.txt)
              PR_BODY+=$'**Removed from mute: '"${DELETED_COUNT}**"$'\n\n'
              PR_BODY+=$'```\n'
              PR_BODY+=$(cat mute_update/to_delete_debug.txt)
              PR_BODY+=$'\n```\n\n'
          fi
          if [ -s mute_update/to_mute_debug.txt ]; then
              MUTED_COUNT=$(wc -l < mute_update/to_mute_debug.txt)
              PR_BODY+=$'**Muted flaky: '"${MUTED_COUNT}**"$'\n\n'
              PR_BODY+=$'```\n'
              PR_BODY+=$(cat mute_update/to_mute_debug.txt)
              PR_BODY+=$'\n```\n\n'

              if [ -s mute_update/to_mute.txt ]; then
                  BASE_URL="https://datalens.yandex.cloud/34xnbsom67hcq-ydb-autotests-test-history-link?branch=${{ env.BASE_BRANCH }}"
                  MAX_TESTS=50
                  TEST_COUNT=0
                  FULL_URL="${BASE_URL}"
                  while read -r test_name && [ ${TEST_COUNT} -lt ${MAX_TESTS} ]; do
                    formatted_name=$(echo "${test_name}" | sed 's/ /\//')
                    encoded_name=$(echo "${formatted_name}" \
                      | sed 's/ /%20/g' \
                      | sed 's/\//%2F/g' \
                      | sed 's/\[/%5B/g' \
                      | sed 's/\]/%5D/g' \
                      | sed 's/+/%2B/g' \
                      | sed 's/-/%2D/g')
                    FULL_URL="${FULL_URL}&full_name=${encoded_name}"
                    TEST_COUNT=$((TEST_COUNT+1))
                  done < mute_update/to_mute.txt
                  PR_BODY+=$"[View history of muted flaky tests on Dashboard](${FULL_URL})"
                  PR_BODY+=$'\n'
              fi
          fi
          if [ -s mute_update/to_unmute_debug.txt ]; then
              UNMUTED_COUNT=$(wc -l < mute_update/to_unmute_debug.txt)
              PR_BODY+=$'**Unmuted stable: '"${UNMUTED_COUNT}**"$'\n\n'
              PR_BODY+=$'```\n'
              PR_BODY+=$(cat mute_update/to_unmute_debug.txt)
              PR_BODY+=$'\n```\n\n'
          fi

          echo "$PR_BODY" > "$PR_BODY_FILE"
          echo "PR_BODY_PATH=$PR_BODY_FILE" >> $GITHUB_ENV

      - name: Upload all generated files as artifacts
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: muted-tests-all-files-${{ env.BASE_BRANCH }}-${{ env.BUILD_TYPE }}
          path: |
            mute_update/
          retention-days: 7

      - name: Push PR branch
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        run: |
          # Retry logic for transient GitHub API timeouts
          MAX_RETRIES=5
          RETRY_DELAY=10
          ATTEMPT=1
          
          while [ $ATTEMPT -le $MAX_RETRIES ]; do
            echo "Attempt $ATTEMPT of $MAX_RETRIES to push branch ${{ env.PR_BRANCH }}"
            
            if git push origin ${{ env.PR_BRANCH }} --force; then
              echo "✓ Successfully pushed branch ${{ env.PR_BRANCH }}"
              exit 0
            else
              EXIT_CODE=$?
              echo "⚠️  Push failed with exit code $EXIT_CODE"
              
              if [ $ATTEMPT -lt $MAX_RETRIES ]; then
                echo "Waiting ${RETRY_DELAY} seconds before retry..."
                sleep $RETRY_DELAY
                RETRY_DELAY=$((RETRY_DELAY * 2))  # Exponential backoff
              else
                echo "❌ Failed to push after $MAX_RETRIES attempts"
                exit $EXIT_CODE
              fi
            fi
            
            ATTEMPT=$((ATTEMPT + 1))
          done

      - name: Create or update PR
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        id: create_or_update_pr
        env:
          GITHUB_TOKEN: ${{ secrets.YDBOT_TOKEN }}
        run: |
          # Возвращаемся на main для доступа к скриптам
          git checkout main
          
          python .github/scripts/create_or_update_pr.py create_or_update \
            --base_branch="${{ env.BASE_BRANCH }}" \
            --branch_for_pr="${{ env.PR_BRANCH }}" \
            --title="Update muted YA (${{ env.BUILD_TYPE }}) in ${{ env.BASE_BRANCH }}" \
            --body="${{ env.PR_BODY_PATH }}"
        
      - name: Prepare PR comment
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        id: comment_pr
        run: |
          # Читаем содержимое PR body
          PR_BODY_CONTENT=$(cat pr_body_content.txt)
          WORKFLOW_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          COMPLETE_BODY=$(cat << EOF
          Collected in workflow [#${{ github.run_number }}](${WORKFLOW_URL}) for ${{ env.BASE_BRANCH }} (build: ${{ env.BUILD_TYPE }})

          ${PR_BODY_CONTENT}
          EOF
          )
          
          echo "$COMPLETE_BODY" > pr_comment_final.txt
          echo "PR_COMMENT_FILE=pr_comment_final.txt" >> $GITHUB_OUTPUT

      - name: Add comment, reviewers and labels
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        uses: actions/github-script@v7
        env:
          LABELS: ${{ env.LABELS }}
          REVIEWERS: ${{ env.REVIEWERS }}
        with:
          github-token: ${{ secrets.YDBOT_TOKEN }}
          script: |
            const fs = require('fs');
            
            // Читаем комментарий из файла
            const commentFile = '${{ steps.comment_pr.outputs.PR_COMMENT_FILE }}';
            const commentBody = fs.readFileSync(commentFile, 'utf8');
            
            // Добавляем комментарий
            await github.rest.issues.createComment({
              issue_number: ${{ steps.create_or_update_pr.outputs.pr_number }},
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });
            
            // Добавляем лейблы
            const labelsToAdd = process.env.LABELS.split(',');
            await github.rest.issues.addLabels({
              ...context.repo,
              issue_number: ${{ steps.create_or_update_pr.outputs.pr_number }},
              labels: labelsToAdd
            });
            
            // Добавляем ревьюеров
            const reviewers = JSON.parse(process.env.REVIEWERS);
            await github.rest.pulls.requestReviewers({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: ${{ steps.create_or_update_pr.outputs.pr_number }},
              team_reviewers: reviewers
            });
          
      - name: Enable auto-merge (squash)
        if: steps.mute_gate.outputs.skip != 'true' && env.changes == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.YDBOT_TOKEN }}
          script: |
            const pr = ${{ steps.create_or_update_pr.outputs.pr_number }};
            
            const { data: pullRequest } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr
            });
            
            const mutation = `
              mutation EnableAutoMerge {
                enablePullRequestAutoMerge(input: {
                  pullRequestId: "${pullRequest.node_id}",
                  mergeMethod: SQUASH
                }) {
                  clientMutationId
                }
              }
            `;
            
            try {
              await github.graphql(mutation);
              console.log("Auto-merge with squash successfully enabled");
            } catch (error) {
              console.error("Failed to enable auto-merge with squash:", error);
            }

