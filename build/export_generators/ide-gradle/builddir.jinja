{#- empty string #}
val baseBuildDir = "{{ export_root }}/gradle.build"
buildDir = file(baseBuildDir + "/" + project.path.replace(":", "/"))
subprojects {
    buildDir = file(baseBuildDir + "/" + project.path.replace(":", "/"))
}
