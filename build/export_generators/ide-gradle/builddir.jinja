{#- empty string #}
val arcadia_root = "{{ arcadia_root }}"
val output_root = "{{ output_root }}"
val project_root = "{{ project_root }}"

val baseBuildDir = "{{ export_root }}/gradle.build"
val projectBuildDir = "$baseBuildDir/{{ curdir }}/build"
val projectCodegenDir = "$projectBuildDir/{{ CODEGENS_DIRNAME }}"

layout.buildDirectory = file(projectBuildDir)

{%- if proto_template %}
{%      if prepareProtosTask %}
val mainProtosDir = layout.buildDirectory.dir("gradle.protos/main")
{%-     endif %}
{%-     if extractLibrariesProtosTask %}
val mainIncludeProtosDir = layout.buildDirectory.dir("gradle.protos/include")
{%-     endif %}
{%- endif %}
