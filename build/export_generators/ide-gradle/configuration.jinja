{#- empty string #}
val arcadia_root = "{{ arcadia_root }}"
val output_root = "{{ output_root }}"
val project_root = "{{ project_root }}"

{%  if not(proto_template) and mainClass -%}
application {
    mainClass.set("{{ mainClass }}")
}

{% else %}
configurations.api {
    isTransitive = false
}

{%  endif -%}
java {
    withSourcesJar()
    withJavadocJar()
}

configurations.implementation {
    isTransitive = false
}

configurations.testImplementation {
    isTransitive = false
}
