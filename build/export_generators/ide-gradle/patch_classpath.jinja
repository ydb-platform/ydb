{%- if common_dir_classpath -%}
{%-     set classpath = classpath|replace(common_dir_classpath, '"')|replace('""', '":"') -%}
{%- else -%}
{%-     set classpath = classpath|replace(project_dir_classpath, '"')|replace('""', '":"') -%}
{%- endif -%}
