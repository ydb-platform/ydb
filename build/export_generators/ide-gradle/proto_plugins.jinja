{#- empty string #}
plugins {
    id("java-library")
    id("com.google.protobuf") version "0.8.19"
{%- if publish %}
    `maven-publish`
    `signing`
{%- endif %}
}

{%- if jdk_version %}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of("{{ jdk_version }}")
    }
}
{%- endif -%}
