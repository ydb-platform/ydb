LIBRARY()

NO_UTIL()
NO_RUNTIME()
NO_PLATFORM()

IF (NOT EXPORT_CMAKE)
    LD_PLUGIN(00-cuda.py)
    LDFLAGS(-L/CUDA:ARCH=$CUDA_ARCHITECTURES -L/CUDA:NVPRUNE=$CUDA_ROOT/bin/nvprune -L/CUDA:OBJCOPY=$OBJCOPY_TOOL)
    IF (CLANG_COVERAGE)
        LDFLAGS(-L/CUDA:CLANG_COVERAGE=True)
    ENDIF()
ENDIF()

END()
