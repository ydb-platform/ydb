RESOURCES_LIBRARY()

IF(NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS AND NOT HOST_OS_DARWIN)
    MESSAGE(FATAL_ERROR Unsupported platform for YFM tool)
ENDIF()

DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
    YFM_TOOL
    sbr:5783396332 FOR DARWIN-ARM64
    sbr:5783396332 FOR DARWIN
    sbr:5783389232 FOR LINUX
    sbr:5783403092 FOR WIN32
)

END()
