revision: 2899505

    Integer overflow on bad psd headers fix + ut REVIEW:271381 IMAGES-11892

--- a/coders/psd.c
+++ b/coders/psd.c
@@ -1915,7 +1915,7 @@ static MagickBooleanType ReadPSDLayersInternal(Image *image,
 
               blob_size = GetBlobSize(image);
               offset = TellBlob(image);
-              if (blob_size > 0 && length+offset >= blob_size)
+              if (blob_size > 0 && (length >= blob_size || length+offset >= blob_size))
                   ThrowBinaryException(CorruptImageError,
                     "ImproperImageHeader",image->filename);
 
