Revert patch that affect jpeg decoder selection and slow down performance.
See docs here https://github.com/libjpeg-turbo/libjpeg-turbo/blob/a62895265f425db48c8538bb3f468ddb4961bf0f/libjpeg.txt#L968

    * https://github.com/ImageMagick/ImageMagick6/commit/f2d8f894af552a5f39afeb591e8be1656f183d4b
--- a/coders/jpeg.c
+++ b/coders/jpeg.c
@@ -1443,6 +1443,9 @@ static Image *ReadJPEGImage_(const ImageInfo *image_info,
   (void) FormatLocaleString(value,MaxTextExtent,"%.20g",(double)
     jpeg_info->out_color_space);
   (void) SetImageProperty(image,"jpeg:colorspace",value);
+  if ((dct_method == (const char *) NULL) && (image->quality > 0) &&
+      (image->quality <= 90))
+    jpeg_info->dct_method=JDCT_IFAST;
   if (image_info->ping != MagickFalse)
     {
       jpeg_destroy_decompress(jpeg_info);
