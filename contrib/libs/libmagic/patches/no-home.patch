Do not look at ~/.magic.mgc by default.

--- a/src/magic.c
+++ b/src/magic.c
@@ -255,5 +255,5 @@ magic_getpath(const char *magicfile, int action)
 		return magicfile;
 
-	return action == FILE_LOAD ? get_default_magic() : MAGIC;
+	return MAGIC;
 }
 
