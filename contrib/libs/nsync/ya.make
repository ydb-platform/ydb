# Generated by devtools/yamaker/ym2 from nixpkgs 24.05.

LIBRARY()

LICENSE(Apache-2.0)

LICENSE_TEXTS(.yandex_meta/licenses.list.txt)

VERSION(1.30.0)

ORIGINAL_SOURCE(https://github.com/google/nsync/archive/1.30.0.tar.gz)

IF (OS_LINUX)
    ADDINCL(
        contrib/libs/nsync/platform/c++11.futex
    )
ENDIF()

ADDINCL(
    contrib/libs/nsync/platform/c++11
)

IF (CLANG)
    ADDINCL(
        contrib/libs/nsync/platform/clang
    )
ELSEIF (GCC)
    ADDINCL(
        contrib/libs/nsync/platform/gcc
    )
ELSEIF (MSVC)
    ADDINCL(
        contrib/libs/nsync/platform/msvc
    )
ENDIF()

IF (OS_DARWIN)
    ADDINCL(
        contrib/libs/nsync/platform/macos
    )
ELSEIF (OS_WINDOWS)
    ADDINCL(
        contrib/libs/nsync/platform/win32
    )
ENDIF()

IF (ARCH_ARM64)
    ADDINCL(
        contrib/libs/nsync/platform/aarch64
    )
ELSEIF (ARCH_ARM7)
    ADDINCL(
        contrib/libs/nsync/platform/arm
    )
ELSEIF (ARCH_I386)
    ADDINCL(
        contrib/libs/nsync/platform/x86_32
    )
ELSEIF (ARCH_PPC64LE)
    ADDINCL(
        contrib/libs/nsync/platform/ppc64
    )
ELSEIF (ARCH_X86_64)
    ADDINCL(
        contrib/libs/nsync/platform/x86_64
    )
ENDIF()

ADDINCL(
    GLOBAL contrib/libs/nsync/public
    contrib/libs/nsync/platform/posix
    contrib/libs/nsync/internal
)

NO_COMPILER_WARNINGS()

NO_UTIL()

CFLAGS(
    -DNSYNC_ATOMIC_CPP11
    -DNSYNC_USE_CPP11_TIMEPOINT
)

SRCS(
    internal/common.cpp
    internal/counter.cpp
    internal/cv.cpp
    internal/debug.cpp
    internal/dll.cpp
    internal/mu.cpp
    internal/mu_wait.cpp
    internal/note.cpp
    internal/once.cpp
    internal/sem_wait.cpp
    internal/time_internal.cpp
    internal/wait.cpp
    platform/c++11/src/nsync_panic.cc
    platform/c++11/src/time_rep_timespec.cc
    platform/c++11/src/yield.cc
    platform/posix/src/per_thread_waiter.cpp
)

IF (OS_LINUX)
    SRCS(
        platform/linux/src/nsync_semaphore_futex.cpp
    )
ELSE()
    SRCS(
        platform/c++11/src/nsync_semaphore_mutex.cc
    )
ENDIF()

IF (OS_WINDOWS)
    SRCS(
        platform/win32/src/clock_gettime.cpp
        platform/win32/src/pthread_key_win32.cc
    )
ENDIF()

END()
