--- a/src/include/fst/accumulator.h
+++ b/src/include/fst/accumulator.h
@@ -31,11 +31,11 @@ class DefaultAccumulator {

   DefaultAccumulator() {}

-  DefaultAccumulator(const DefaultAccumulator &acc, bool safe = false) {}
+  DefaultAccumulator([[maybe_unused]] const DefaultAccumulator &acc, [[maybe_unused]] bool safe = false) {}

-  void Init(const Fst<Arc> &fst, bool copy = false) {}
+  void Init([[maybe_unused]] const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) {}

-  void SetState(StateId state) {}
+  void SetState([[maybe_unused]] StateId state) {}

   Weight Sum(Weight w, Weight v) { return Plus(w, v); }

@@ -65,11 +65,11 @@ class LogAccumulator {

   LogAccumulator() {}

-  LogAccumulator(const LogAccumulator &acc, bool safe = false) {}
+  LogAccumulator([[maybe_unused]] const LogAccumulator &acc, [[maybe_unused]] bool safe = false) {}

-  void Init(const Fst<Arc> &fst, bool copy = false) {}
+  void Init([[maybe_unused]] const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) {}

-  void SetState(StateId s) {}
+  void SetState([[maybe_unused]] StateId s) {}

   Weight Sum(Weight w, Weight v) { return LogPlus(w, v); }

@@ -244,1 +244,1 @@ class FastLogAccumulator {
-  FastLogAccumulator(const FastLogAccumulator &acc, bool safe = false)
+  FastLogAccumulator(const FastLogAccumulator &acc, [[maybe_unused]] bool safe = false)
@@ -513,1 +513,1 @@ class CacheLogAccumulator {
-  void SetState(StateId s, int depth = 0) {
+  void SetState(StateId s, [[maybe_unused]] int depth = 0) {
@@ -750,1 +750,1 @@ class ReplaceAccumulator {
-  void Init(const Fst<Arc> &fst, bool copy = false) {
+  void Init([[maybe_unused]] const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) {
@@ -785,1 +785,1 @@ class ReplaceAccumulator {
-  Weight Sum(Weight w, ArcIter *aiter, ssize_t begin, ssize_t end) {
+  Weight Sum(Weight w, [[maybe_unused]] ArcIter *aiter, ssize_t begin, ssize_t end) {
@@ -821,1 +821,1 @@ class SafeReplaceAccumulator {
-  SafeReplaceAccumulator(const SafeReplaceAccumulator &copy, bool safe)
+  SafeReplaceAccumulator(const SafeReplaceAccumulator &copy, [[maybe_unused]] bool safe)
@@ -844,1 +844,1 @@ class SafeReplaceAccumulator {
-  void Init(const Fst<Arc> &fst, bool copy = false) {
+  void Init([[maybe_unused]] const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) {
@@ -872,1 +872,1 @@ class SafeReplaceAccumulator {
-  Weight Sum(Weight w, ArcIter *aiter, ssize_t begin, ssize_t end) {
+  Weight Sum(Weight w, [[maybe_unused]] ArcIter *aiter, ssize_t begin, ssize_t end) {
@@ -890,1 +890,1 @@ class SafeReplaceAccumulator {
-    ArcIteratorPtr(const ArcIteratorPtr &copy) {}
+    ArcIteratorPtr([[maybe_unused]] const ArcIteratorPtr &copy) {}
--- a/src/include/fst/add-on.h
+++ b/src/include/fst/add-on.h
@@ -46,1 +46,1 @@ class NullAddOn {
-  static NullAddOn *Read(std::istream &strm, const FstReadOptions &opts) {
+  static NullAddOn *Read([[maybe_unused]] std::istream &strm, [[maybe_unused]] const FstReadOptions &opts) {
@@ -50,1 +50,1 @@ class NullAddOn {
-  bool Write(std::ostream &ostrm, const FstWriteOptions &opts) const {
+  bool Write([[maybe_unused]] std::ostream &ostrm, [[maybe_unused]] const FstWriteOptions &opts) const {
--- a/src/include/fst/arcfilter.h
+++ b/src/include/fst/arcfilter.h
@@ -34,1 +34,1 @@ namespace fst {
-  bool operator()(const Arc &arc) const { return true; }
+  bool operator()([[maybe_unused]] const Arc &arc) const { return true; }
--- a/src/include/fst/bi-table.h
+++ b/src/include/fst/bi-table.h
@@ -124,1 +124,1 @@ struct HashSet : public std::unordered_set<K, H, E, PoolAllocator<K>> {
-  void rehash(size_t n) {}
+  void rehash([[maybe_unused]] size_t n) {}
--- a/src/include/fst/cache.h
+++ b/src/include/fst/cache.h
@@ -213,1 +213,1 @@ class CacheState {
-  void *operator new(size_t size, StateAllocator *alloc) {
+  void *operator new([[maybe_unused]] size_t size, StateAllocator *alloc) {
@@ -439,1 +439,1 @@ class HashCacheStore {
-  explicit HashCacheStore(const CacheOptions &opts) {
+  explicit HashCacheStore([[maybe_unused]] const CacheOptions &opts) {
@@ -1224,1 +1224,1 @@ class CacheArcIterator {
-  void SetFlags(uint8_t flags, uint8_t mask) {}
+  void SetFlags([[maybe_unused]] uint8_t flags, [[maybe_unused]] uint8_t mask) {}
--- a/src/include/fst/compose.h
+++ b/src/include/fst/compose.h
@@ -212,2 +212,2 @@ class ComposeFstImplBase
-  virtual MatcherBase<Arc> *InitMatcher(const F &fst,
-                                        MatchType match_type) const {
+  virtual MatcherBase<Arc> *InitMatcher([[maybe_unused]] const F &fst,
+                                        [[maybe_unused]] MatchType match_type) const {
@@ -862,1 +862,1 @@ class ComposeFstMatcher : public MatcherBase<typename CacheStore::Arc> {
-  bool MatchArc(StateId s, Arc *arc1, Arc *arc2) {
+  bool MatchArc([[maybe_unused]] StateId s, Arc *arc1, Arc *arc2) {
--- a/src/include/fst/connect.h
+++ b/src/include/fst/connect.h
@@ -54,1 +54,1 @@ class CcVisitor {
-  void InitVisit(const Fst<Arc> &fst) {}
+  void InitVisit([[maybe_unused]] const Fst<Arc> &fst) {}
@@ -56,1 +56,1 @@ class CcVisitor {
-  bool InitState(StateId s, StateId root) {
+  bool InitState(StateId s, [[maybe_unused]] StateId root) {
@@ -78,1 +78,1 @@ class CcVisitor {
-  void FinishState(StateId s) {}
+  void FinishState([[maybe_unused]] StateId s) {}
@@ -135,1 +135,1 @@ class SccVisitor {
-  bool TreeArc(StateId s, const Arc &arc) { return true; }
+  bool TreeArc([[maybe_unused]] StateId s, [[maybe_unused]] const Arc &arc) { return true; }
--- a/src/include/fst/const-fst.h
+++ b/src/include/fst/const-fst.h
@@ -253,1 +253,1 @@ class ConstFst : public ImplToExpandedFst<internal::ConstFstImpl<A, Unsigned>> {
-  ConstFst(const ConstFst &fst, bool unused_safe = false)
+  ConstFst(const ConstFst &fst, [[maybe_unused]] bool unused_safe = false)
@@ -307,1 +307,1 @@ class ConstFst : public ImplToExpandedFst<internal::ConstFstImpl<A, Unsigned>> {
-  static Impl *GetImplIfConstFst(const FST &fst) {
+  static Impl *GetImplIfConstFst([[maybe_unused]] const FST &fst) {
--- a/src/include/fst/determinize.h
+++ b/src/include/fst/determinize.h
@@ -234,1 +234,1 @@ class DefaultDeterminizeFilter {
-  DefaultDeterminizeFilter(const Fst<Arc> &fst, std::unique_ptr<Filter> filter)
+  DefaultDeterminizeFilter(const Fst<Arc> &fst, [[maybe_unused]] std::unique_ptr<Filter> filter)
@@ -245,1 +245,1 @@ class DefaultDeterminizeFilter {
-  void SetState(StateId s, const StateTuple &tuple) {}
+  void SetState([[maybe_unused]] StateId s, [[maybe_unused]] const StateTuple &tuple) {}
@@ -249,1 +249,1 @@ class DefaultDeterminizeFilter {
-  bool FilterArc(const Arc &arc, const Element &src_element,
+  bool FilterArc(const Arc &arc, [[maybe_unused]] const Element &src_element,
@@ -262,1 +262,1 @@ class DefaultDeterminizeFilter {
-  Weight FilterFinal(Weight weight, const Element &element) { return weight; }
+  Weight FilterFinal(Weight weight, [[maybe_unused]] const Element &element) { return weight; }
--- a/src/include/fst/dfs-visit.h
+++ b/src/include/fst/dfs-visit.h
@@ -83,1 +83,1 @@ struct DfsState {
-  void *operator new(size_t size, MemoryPool<DfsState<FST>> *pool) {
+  void *operator new([[maybe_unused]] size_t size, MemoryPool<DfsState<FST>> *pool) {
--- a/src/include/fst/factor-weight.h
+++ b/src/include/fst/factor-weight.h
@@ -102,1 +102,1 @@ struct FactorWeightOptions : CacheOptions {
-  explicit IdentityFactor(const W &weight) {}
+  explicit IdentityFactor([[maybe_unused]] const W &weight) {}
--- a/src/include/fst/float-weight.h
+++ b/src/include/fst/float-weight.h
@@ -320,7 +320,7 @@ constexpr TropicalWeightTpl<double> Times(const TropicalWeightTpl<double> &w1,
 template <class T>
 constexpr TropicalWeightTpl<T> Divide(const TropicalWeightTpl<T> &w1,
                                       const TropicalWeightTpl<T> &w2,
-                                      DivideType typ = DIVIDE_ANY) {
+                                      [[maybe_unused]] DivideType typ = DIVIDE_ANY) {
   // The following is safe in the context of the Tropical (and Log) semiring
   // for all IEEE floating point values, including infinities and NaNs,
   // because:
@@ -578,7 +578,7 @@ constexpr LogWeightTpl<double> Times(const LogWeightTpl<double> &w1,
 template <class T>
 constexpr LogWeightTpl<T> Divide(const LogWeightTpl<T> &w1,
                                  const LogWeightTpl<T> &w2,
-                                 DivideType typ = DIVIDE_ANY) {
+                                 [[maybe_unused]] DivideType typ = DIVIDE_ANY) {
   // The comments for Divide(Tropical...) above apply here unchanged.
   using Weight = LogWeightTpl<T>;
   return w2.Member() ? Weight(w1.Value() - w2.Value()) : Weight::NoWeight();
@@ -782,7 +782,7 @@ constexpr RealWeightTpl<double> Times(const RealWeightTpl<double> &w1,
 template <class T>
 constexpr RealWeightTpl<T> Divide(const RealWeightTpl<T> &w1,
                                   const RealWeightTpl<T> &w2,
-                                  DivideType typ = DIVIDE_ANY) {
+                                  [[maybe_unused]] DivideType typ = DIVIDE_ANY) {
   using Weight = RealWeightTpl<T>;
   return w2.Member() ? Weight(w1.Value() / w2.Value()) : Weight::NoWeight();
 }
@@ -947,7 +947,7 @@ constexpr MinMaxWeightTpl<double> Times(const MinMaxWeightTpl<double> &w1,
 template <class T>
 constexpr MinMaxWeightTpl<T> Divide(const MinMaxWeightTpl<T> &w1,
                                     const MinMaxWeightTpl<T> &w2,
-                                    DivideType typ = DIVIDE_ANY) {
+                                    [[maybe_unused]] DivideType typ = DIVIDE_ANY) {
   return w1.Value() >= w2.Value() ? w1 : MinMaxWeightTpl<T>::NoWeight();
 }

--- a/src/include/fst/fst.h
+++ b/src/include/fst/fst.h
@@ -293,1 +293,1 @@ class Fst {
-  virtual bool Write(std::ostream &strm, const FstWriteOptions &opts) const {
+  virtual bool Write([[maybe_unused]] std::ostream &strm, [[maybe_unused]] const FstWriteOptions &opts) const {
@@ -301,1 +301,1 @@ class Fst {
-  virtual bool Write(const std::string &source) const {
+  virtual bool Write([[maybe_unused]] const std::string &source) const {
@@ -586,1 +586,1 @@ class ArcIterator {
-void *operator new(size_t size,
+void *operator new([[maybe_unused]] size_t size,
@@ -604,1 +604,1 @@ void Destroy(ArcIterator<FST> *aiter, MemoryPool<ArcIterator<FST>> *pool) {
-MatcherBase<Arc> *Fst<Arc>::InitMatcher(MatchType match_type) const {
+MatcherBase<Arc> *Fst<Arc>::InitMatcher([[maybe_unused]] MatchType match_type) const {
@@ -728,1 +728,1 @@ class FstImpl {
-  void SetProperties(uint64_t props, uint64_t mask) const {
+  void SetProperties([[maybe_unused]] uint64_t props, uint64_t mask) const {
--- a/src/include/fst/label-reachable.h
+++ b/src/include/fst/label-reachable.h
@@ -84,7 +84,7 @@ class LabelReachableData {
   Label FinalLabel() const { return final_label_; }

   static LabelReachableData *Read(std::istream &istrm,
-                                  const FstReadOptions &opts) {
+                                  [[maybe_unused]] const FstReadOptions &opts) {
     // NB: Using `new` to access private constructor.
     auto data = fst::WrapUnique(new LabelReachableData());
     ReadType(istrm, &data->reach_input_);
@@ -99,1 +99,1 @@ class LabelReachableData {
-  bool Write(std::ostream &ostrm, const FstWriteOptions &opts) const {
+  bool Write(std::ostream &ostrm, [[maybe_unused]] const FstWriteOptions &opts) const {
@@ -164,1 +164,1 @@ class LabelLowerBound {
-  void Init(const FST &fst, bool reach_input, bool is_copy) {
+  void Init([[maybe_unused]] const FST &fst, bool reach_input, [[maybe_unused]] bool is_copy) {
@@ -169,1 +169,1 @@ class LabelLowerBound {
-  void SetState(StateId aiter_s) {}
+  void SetState([[maybe_unused]] StateId aiter_s) {}
--- a/src/include/fst/lexicographic-weight.h
+++ b/src/include/fst/lexicographic-weight.h
@@ -97,1 +97,1 @@ class LexicographicWeight : public PairWeight<W1, W2> {
-  LexicographicWeight Quantize(float delta = kDelta) const {
+  LexicographicWeight Quantize([[maybe_unused]] float delta = kDelta) const {
--- a/src/include/fst/lookahead-matcher.h
+++ b/src/include/fst/lookahead-matcher.h
@@ -233,1 +233,1 @@ class TrivialLookAheadMatcher
-  void InitLookAheadFst(const Fst<Arc> &fst, bool copy = false) override {}
+  void InitLookAheadFst([[maybe_unused]] const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) override {}
@@ -270,2 +270,2 @@ class ArcLookAheadMatcher : public LookAheadMatcherBase<typename M::FST::Arc> {
-  ArcLookAheadMatcher(const FST &fst, MatchType match_type,
-                      std::shared_ptr<MatcherData> data = nullptr)
+  ArcLookAheadMatcher(const FST &fst, MatchType match_type,
+                      [[maybe_unused]] std::shared_ptr<MatcherData> data = nullptr)
@@ -278,2 +278,2 @@ class ArcLookAheadMatcher : public LookAheadMatcherBase<typename M::FST::Arc> {
-  ArcLookAheadMatcher(const FST *fst, MatchType match_type,
-                      std::shared_ptr<MatcherData> data = nullptr)
+  ArcLookAheadMatcher(const FST *fst, MatchType match_type,
+                      [[maybe_unused]] std::shared_ptr<MatcherData> data = nullptr)
@@ -333,1 +333,1 @@ class ArcLookAheadMatcher : public LookAheadMatcherBase<typename M::FST::Arc> {
-  void InitLookAheadFst(const Fst<Arc> &fst, bool copy = false) override {
+  void InitLookAheadFst(const Fst<Arc> &fst, [[maybe_unused]] bool copy = false) override {
--- a/src/include/fst/matcher.h
+++ b/src/include/fst/matcher.h
@@ -473,1 +473,1 @@ class HashMatcher : public MatcherBase<typename F::Arc> {
-  MatchType Type(bool test) const override { return match_type_; }
+  MatchType Type([[maybe_unused]] bool test) const override { return match_type_; }
--- a/src/include/fst/memory.h
+++ b/src/include/fst/memory.h
@@ -261,2 +261,2 @@ class BlockAllocator {
-bool operator==(const BlockAllocator<T> &alloc1,
-                const BlockAllocator<U> &alloc2) {
+bool operator==([[maybe_unused]] const BlockAllocator<T> &alloc1,
+                [[maybe_unused]] const BlockAllocator<U> &alloc2) {
@@ -267,2 +267,2 @@ class BlockAllocator {
-bool operator!=(const BlockAllocator<T> &alloc1,
-                const BlockAllocator<U> &alloc2) {
+bool operator!=([[maybe_unused]] const BlockAllocator<T> &alloc1,
+                [[maybe_unused]] const BlockAllocator<U> &alloc2) {
@@ -351,2 +351,2 @@ class PoolAllocator {
-bool operator==(const PoolAllocator<T> &alloc1,
-                const PoolAllocator<U> &alloc2) {
+bool operator==([[maybe_unused]] const PoolAllocator<T> &alloc1,
+                [[maybe_unused]] const PoolAllocator<U> &alloc2) {
@@ -357,2 +357,2 @@ class PoolAllocator {
-bool operator!=(const PoolAllocator<T> &alloc1,
-                const PoolAllocator<U> &alloc2) {
+bool operator!=([[maybe_unused]] const PoolAllocator<T> &alloc1,
+                [[maybe_unused]] const PoolAllocator<U> &alloc2) {
--- a/src/include/fst/mutable-fst.h
+++ b/src/include/fst/mutable-fst.h
@@ -96,1 +96,1 @@ class MutableFst : public ExpandedFst<A> {
-  virtual void ReserveStates(size_t) {}
+  virtual void ReserveStates([[maybe_unused]] size_t) {}
@@ -99,1 +99,1 @@ class MutableFst : public ExpandedFst<A> {
-  virtual void ReserveArcs(StateId, size_t) {}
+  virtual void ReserveArcs([[maybe_unused]] StateId, [[maybe_unused]] size_t) {}
--- a/src/include/fst/prune.h
+++ b/src/include/fst/prune.h
@@ -111,1 +111,1 @@ void Prune(MutableFst<Arc> *fst, const PruneOptions<Arc, ArcFilter> &opts =
-void Prune(MutableFst<Arc> *fst, const PruneOptions<Arc, ArcFilter> &opts =
+void Prune(MutableFst<Arc> *fst, [[maybe_unused]] const PruneOptions<Arc, ArcFilter> &opts =
--- a/src/include/fst/queue.h
+++ b/src/include/fst/queue.h
@@ -997,1 +997,1 @@ class FilterQueue : public QueueBase<typename Queue::StateId> {
-  void Update(StateId s) final {}
+  void Update([[maybe_unused]] StateId s) final {}
--- a/src/include/fst/shortest-path.h
+++ b/src/include/fst/shortest-path.h
@@ -127,1 +127,1 @@ void SingleShortestTree(
-  bool operator()(S s, W d, W f) const { return f == Plus(d, f); }
+  bool operator()([[maybe_unused]] S s, W d, W f) const { return f == Plus(d, f); }
--- a/src/include/fst/signed-log-weight.h
+++ b/src/include/fst/signed-log-weight.h
@@ -174,7 +174,7 @@ inline SignedLogWeightTpl<T> Times(const SignedLogWeightTpl<T> &w1,
 template <class T>
 inline SignedLogWeightTpl<T> Divide(const SignedLogWeightTpl<T> &w1,
                                     const SignedLogWeightTpl<T> &w2,
-                                    DivideType typ = DIVIDE_ANY) {
+                                    [[maybe_unused]] DivideType typ = DIVIDE_ANY) {
   using W2 = LogWeightTpl<T>;
   if (!w1.Member() || !w2.Member()) return SignedLogWeightTpl<T>::NoWeight();
   const auto s1 = w1.IsPositive();
--- a/src/include/fst/sparse-power-weight.h
+++ b/src/include/fst/sparse-power-weight.h
@@ -120,1 +120,1 @@ inline SparsePowerWeight<W, K> SparsePowerWeightMap(
-  return SparsePowerWeightMap(w1, w2, [](const K &k, const W &v1, const W &v2) {
+  return SparsePowerWeightMap(w1, w2, []([[maybe_unused]] const K &k, const W &v1, const W &v2) {
@@ -129,1 +129,1 @@ inline SparsePowerWeight<W, K> Plus(const SparsePowerWeight<W, K> &w1,
-  return SparsePowerWeightMap(w1, w2, [](const K &k, const W &v1, const W &v2) {
+  return SparsePowerWeightMap(w1, w2, []([[maybe_unused]] const K &k, const W &v1, const W &v2) {
@@ -138,1 +138,1 @@ inline SparsePowerWeight<W, K> Plus(const SparsePowerWeight<W, K> &w1,
-  return SparsePowerWeightMap(w1, w2, [](const K &k, const W &v1, const W &v2) {
+  return SparsePowerWeightMap(w1, w2, []([[maybe_unused]] const K &k, const W &v1, const W &v2) {
@@ -146,7 +146,7 @@ inline SparsePowerWeight<W, K> Divide(const SparsePowerWeight<W, K> &w1,
                                       const SparsePowerWeight<W, K> &w2,
                                       DivideType type = DIVIDE_ANY) {
   return SparsePowerWeightMap(w1, w2,
-                              [type](const K &k, const W &v1, const W &v2) {
+                              [type]([[maybe_unused]] const K &k, const W &v1, const W &v2) {
                                 return Divide(v1, v2, type);
                               });
 }
@@ -168,7 +168,7 @@ inline bool ApproxEqual(const SparsePowerWeight<W, K> &w1,
                         const SparsePowerWeight<W, K> &w2,
                         float delta = kDelta) {
   auto result = SparsePowerWeightMap(
-      w1, w2, [delta](const K &k, const W &v1, const W &v2) {
+      w1, w2, [delta]([[maybe_unused]] const K &k, const W &v1, const W &v2) {
         return ApproxEqual(v1, v2, delta) ? W::One() : W::Zero();
       });
   return result == SparsePowerWeight<W, K>::One();
--- a/src/include/fst/state-reachable.h
+++ b/src/include/fst/state-reachable.h
@@ -67,1 +67,1 @@ class IntervalReachVisitor {
-  bool InitState(StateId s, StateId r) {
+  bool InitState(StateId s, [[maybe_unused]] StateId r) {
@@ -97,1 +97,1 @@ class IntervalReachVisitor {
-  bool BackArc(StateId s, const Arc &arc) {
+  bool BackArc([[maybe_unused]] StateId s, [[maybe_unused]] const Arc &arc) {
@@ -155,1 +155,1 @@ class StateReachable {
-  explicit StateReachable(const StateReachable<Arc> &reachable) {
+  explicit StateReachable([[maybe_unused]] const StateReachable<Arc> &reachable) {
--- a/src/include/fst/state-table.h
+++ b/src/include/fst/state-table.h
@@ -329,1 +329,1 @@ class GenericComposeStateTable : public StateTable {
-  GenericComposeStateTable(const Fst<Arc> &fst1, const Fst<Arc> &fst2) {}
+  GenericComposeStateTable([[maybe_unused]] const Fst<Arc> &fst1, [[maybe_unused]] const Fst<Arc> &fst2) {}
@@ -331,1 +331,1 @@ class GenericComposeStateTable : public StateTable {
-  GenericComposeStateTable(const Fst<Arc> &fst1, const Fst<Arc> &fst2,
+  GenericComposeStateTable([[maybe_unused]] const Fst<Arc> &fst1, [[maybe_unused]] const Fst<Arc> &fst2,
@@ -497,1 +497,1 @@ template <typename Arc, typename StateTuple>
-  ErasableComposeStateTable(const Fst<Arc> &fst1, const Fst<Arc> &fst2) {}
+  ErasableComposeStateTable([[maybe_unused]] const Fst<Arc> &fst1, [[maybe_unused]] const Fst<Arc> &fst2) {}
--- a/src/include/fst/string-weight.h
+++ b/src/include/fst/string-weight.h
@@ -110,1 +110,1 @@ class StringWeight {
-  StringWeight Quantize(float delta = kDelta) const { return *this; }
+  StringWeight Quantize([[maybe_unused]] float delta = kDelta) const { return *this; }
@@ -300,7 +300,7 @@ inline bool operator!=(const StringWeight<Label, S> &w1,
 template <typename Label, StringType S>
 inline bool ApproxEqual(const StringWeight<Label, S> &w1,
                         const StringWeight<Label, S> &w2,
-                        float delta = kDelta) {
+                        [[maybe_unused]] float delta = kDelta) {
   return w1 == w2;
 }

--- a/src/include/fst/topsort.h
+++ b/src/include/fst/topsort.h
@@ -45,1 +45,1 @@ class TopOrderVisitor {
-  void InitVisit(const Fst<Arc> &fst) {
+  void InitVisit([[maybe_unused]] const Fst<Arc> &fst) {
--- a/src/include/fst/util.h
+++ b/src/include/fst/util.h
@@ -178,1 +178,1 @@ std::istream &ReadType(std::istream &strm, std::vector<T...> *c) {
-  return internal::ReadContainerType(strm, c, [](decltype(c) v, int n) {});
+  return internal::ReadContainerType(strm, c, []([[maybe_unused]] decltype(c) v, [[maybe_unused]] int n) {});
@@ -183,1 +183,1 @@ std::istream &ReadType(std::istream &strm, std::vector<T...> *c) {
-  return internal::ReadContainerType(strm, c, [](decltype(c) v, int n) {});
+  return internal::ReadContainerType(strm, c, []([[maybe_unused]] decltype(c) v, [[maybe_unused]] int n) {});
@@ -188,1 +188,1 @@ std::istream &ReadType(std::istream &strm, std::vector<T...> *c) {
-  return internal::ReadContainerType(strm, c, [](decltype(c) v, int n) {});
+  return internal::ReadContainerType(strm, c, []([[maybe_unused]] decltype(c) v, [[maybe_unused]] int n) {});
--- a/src/include/fst/vector-fst.h
+++ b/src/include/fst/vector-fst.h
@@ -137,1 +137,1 @@ class VectorState {
-  void *operator new(size_t size, StateAllocator *alloc) {
+  void *operator new([[maybe_unused]] size_t size, StateAllocator *alloc) {
@@ -572,1 +572,1 @@ class VectorFst : public ImplToMutableFst<internal::VectorFstImpl<S>> {
-  VectorFst(const VectorFst &fst, bool unused_safe = false)
+  VectorFst(const VectorFst &fst, [[maybe_unused]] bool unused_safe = false)
--- a/src/include/fst/weight.h
+++ b/src/include/fst/weight.h
@@ -234,1 +234,1 @@ class Adder {
-  W2 operator()(W1 w1) const {
+  W2 operator()([[maybe_unused]] W1 w1) const {
--- a/src/include/fst/extensions/far/far.h
+++ b/src/include/fst/extensions/far/far.h
@@ -241,1 +241,1 @@ class FstFarWriter final : public FarWriter<A> {
-  void Add(std::string_view key, const Fst<A> &fst) final {
+  void Add([[maybe_unused]] std::string_view key, const Fst<A> &fst) final {
@@ -432,1 +432,1 @@ class FstFarReader : public FarReader<A> {
-  bool Find(std::string_view key) final {
+  bool Find([[maybe_unused]] std::string_view key) final {
--- a/src/include/fst/extensions/far/stlist.h
+++ b/src/include/fst/extensions/far/stlist.h
@@ -197,1 +197,1 @@ class STListReader {
-  bool Find(std::string_view key) {
+  bool Find([[maybe_unused]] std::string_view key) {
--- a/src/include/fst/extensions/pdt/pdt.h
+++ b/src/include/fst/extensions/pdt/pdt.h
@@ -171,1 +171,1 @@ class PdtStateTable : public CompactHashStateTable<
-  PdtStateTable(const PdtStateTable &other) {}
+  PdtStateTable([[maybe_unused]] const PdtStateTable &other) {}
--- a/src/include/fst/extensions/pdt/shortest-path.h
+++ b/src/include/fst/extensions/pdt/shortest-path.h
@@ -580,7 +580,7 @@ inline void PdtShortestPath<Arc, Queue>::ProcOpenParen(Label paren_id,
 template <class Arc, class Queue>
 inline void PdtShortestPath<Arc, Queue>::ProcCloseParen(Label paren_id,
                                                         SearchState s,
-                                                        const Weight &weight) {
+                                                        [[maybe_unused]] const Weight &weight) {
   const internal::ParenState<Arc> paren_state(paren_id, s.start);
   if (!(sp_data_.Flags(s) & kExpanded)) {
     balance_data_.CloseInsert(paren_id, s.start, s.state);
