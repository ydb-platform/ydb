Note: sentencepiece_processor.h is public API, so we do want it to
depend on _USE_INTERNAL_STRING_VIEW or _USE_TF_STRING_VIEW.

--- a/src/filesystem.cc
+++ b/src/filesystem.cc
@@ -49,3 +49,6 @@ class PosixReadableFile : public ReadableFile {
   bool ReadLine(TString *line) {
-    return static_cast<bool>(std::getline(*is_, *line));
+    std::string sline;
+    bool ok = static_cast<bool>(std::getline(*is_, sline));
+    *line = sline;
+    return ok;
   }
@@ -57,4 +60,6 @@ class PosixReadableFile : public ReadableFile {
     }
-    line->assign(std::istreambuf_iterator<char>(*is_),
+    std::string sline;
+    sline.assign(std::istreambuf_iterator<char>(*is_),
                  std::istreambuf_iterator<char>());
+    *line = sline;
     return true;
--- a/src/sentencepiece_processor.h
+++ b/src/sentencepiece_processor.h
@@ -23,6 +23,7 @@
 #include <string_view>
 #include <utility>
 #include <vector>
+#include <util/generic/string.h>
 
 #ifndef SWIG
 namespace absl {
--- a/src/util.h
+++ b/src/util.h
@@ -78,3 +78,3 @@ inline bool lexical_cast(std::string_view arg, bool *result) {
   TString lower_value = TString(arg);
-  std::transform(lower_value.begin(), lower_value.end(), lower_value.begin(),
+  std::transform(lower_value.begin(), lower_value.vend(), lower_value.begin(),
                  ::tolower);
@@ -398,3 +398,3 @@ class StatusBuilder {
 
-  operator Status() const { return Status(code_, os_.str()); }
+  operator Status() const { return Status(code_, TString(os_.str())); }
 
