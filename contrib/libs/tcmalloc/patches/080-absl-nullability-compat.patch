diff --git a/tcmalloc/huge_page_aware_allocator_test.cc b/tcmalloc/huge_page_aware_allocator_test.cc
index 8945b2a..bb4a79f 100644
--- a/tcmalloc/huge_page_aware_allocator_test.cc
+++ b/tcmalloc/huge_page_aware_allocator_test.cc
@@ -1629,7 +1629,7 @@ using FakeHugePageAwareAllocator =
     huge_page_allocator_internal::HugePageAwareAllocator<
         huge_page_allocator_internal::FakeStaticForwarder>;
 struct SpanDeleter {
-  explicit SpanDeleter(absl::Nonnull<FakeHugePageAwareAllocator*> allocator)
+  explicit SpanDeleter(FakeHugePageAwareAllocator* absl_nonnull allocator)
       : allocator(*allocator) {}
 
   void operator()(Span* s) ABSL_LOCKS_EXCLUDED(pageheap_lock) {
diff --git a/tcmalloc/pagemap.h b/tcmalloc/pagemap.h
index 98f92ba..bb864f5 100644
--- a/tcmalloc/pagemap.h
+++ b/tcmalloc/pagemap.h
@@ -516,7 +516,7 @@ class PageMap {
   // Return the descriptor and sizeclass for the specified page.
   // PageId must have been previously allocated.
   // No locks required.  See SYNCHRONIZATION explanation at top of tcmalloc.cc.
-  [[nodiscard]] inline std::pair<absl::Nullable<Span*>, CompactSizeClass>
+  [[nodiscard]] inline std::pair<Span* absl_nullable, CompactSizeClass>
   GetExistingDescriptorAndSizeClass(PageId p) const
       ABSL_NO_THREAD_SAFETY_ANALYSIS {
     return map_.get_existing_with_sizeclass(p.index());
@@ -525,7 +525,7 @@ class PageMap {
   // Return the descriptor for the specified page.
   // PageId must have been previously allocated.
   // No locks required.  See SYNCHRONIZATION explanation at top of tcmalloc.cc.
-  [[nodiscard]] inline absl::Nullable<Span*> GetExistingDescriptor(
+  [[nodiscard]] inline Span* absl_nullable GetExistingDescriptor(
       PageId p) const ABSL_NO_THREAD_SAFETY_ANALYSIS {
     return map_.get_existing(p.index());
   }
