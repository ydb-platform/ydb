--- contrib/python/Babel/py3/babel/support.py	(index)
+++ contrib/python/Babel/py3/babel/support.py	(working tree)
@@ -15,6 +15,8 @@ from __future__ import annotations
 import gettext
 import locale
 import os
+import io
+import library.python.resource
 from collections.abc import Iterator
 from typing import TYPE_CHECKING, Any, Callable, Iterable, Literal
 
@@ -658,6 +660,9 @@ class Translations(NullTranslations, gettext.GNUTranslations):
         filename = gettext.find(domain, dirname, _locales_to_names(locales))
         if not filename:
             return NullTranslations()
+        if binary_data := library.python.resource.find(f'resfs/file/{filename}'):
+            bytes_io = io.BytesIO(binary_data)
+            return cls(fp=bytes_io, domain=domain)
         with open(filename, 'rb') as fp:
             return cls(fp=fp, domain=domain)
