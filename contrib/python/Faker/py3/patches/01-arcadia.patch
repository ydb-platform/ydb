--- contrib/python/Faker/py3/faker/utils/loading.py	(index)
+++ contrib/python/Faker/py3/faker/utils/loading.py	(working tree)
@@ -9,6 +9,11 @@ def get_path(module):
     if getattr(sys, "frozen", False):
         # frozen
 
+        paths = list(getattr(module, "__path__", []))
+        if len(paths) == 1:
+            import os
+            return os.path.normpath(paths[0])
+
         if getattr(sys, "_MEIPASS", False):
             # PyInstaller
             lib_dir = getattr(sys, "_MEIPASS")
