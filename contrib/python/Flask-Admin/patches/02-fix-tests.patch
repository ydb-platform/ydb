--- contrib/python/Flask-Admin/flask_admin/tests/fileadmin/test_fileadmin.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/fileadmin/test_fileadmin.py	(working tree)
@@ -4,2 +4,4 @@ import os
 
+import pytest
+
 from flask_admin.contrib import fileadmin
@@ -138,2 +139,3 @@ class Base:
 
+        @pytest.mark.skip('Skip in Arcadia')
         def test_modal_edit(self):
@@ -210,2 +212,3 @@ class LocalFileAdminTests(Base.FileAdminTests):
 
+    @pytest.mark.skip('Skip in Arcadia')
     def test_fileadmin_sort_bogus_url_param(self):
--- contrib/python/Flask-Admin/flask_admin/tests/peeweemodel/test_basic.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/peeweemodel/test_basic.py	(working tree)
@@ -3,2 +3,3 @@
 import peewee
+import pytest
 
@@ -104,2 +106,3 @@ def fill_db(Model1, Model2):
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_model():
@@ -251,2 +254,3 @@ def test_column_editable_list():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_details_view():
@@ -304,2 +308,3 @@ def test_details_view():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_column_filters():
@@ -886,2 +890,3 @@ def test_default_sort():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_extra_fields():
--- contrib/python/Flask-Admin/flask_admin/tests/sqla/test_basic.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/sqla/test_basic.py	(working tree)
@@ -8,5 +8,9 @@
 from flask_admin._compat import iteritems
 from flask_admin.contrib.sqla import ModelView, filters, tools
-from flask_babelex import Babel
+try:
+    from flask_babelex import Babel
+except ImportError:
+    Babel = lambda _: None
+import pytest
 
 from sqlalchemy.ext.hybrid import hybrid_property
@@ -2185,2 +2189,3 @@ def test_extra_field_order():
 
+@pytest.mark.skip('depends on flask_babelex')
 def test_modelview_localization():
@@ -2218,2 +2224,3 @@ def test_modelview_localization():
 
+@pytest.mark.skip('depends on flask_babelex')
 def test_modelview_named_filter_localization():
--- contrib/python/Flask-Admin/flask_admin/tests/test_base.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/test_base.py	(working tree)
@@ -8,12 +8,29 @@ from flask.views import MethodView
 from flask_admin import base
 
 
+def load_test_templates():
+    result = {}
+    try:
+        from library.python import resource
+        for template in ['method.html', 'mock.html']:
+            path = 'resfs/file/contrib/python/Flask-Admin/flask_admin/tests/templates/{}'.format(template)
+            result['templates/bootstrap2/{}'.format(template)] = resource.find(path).decode('utf8')
+    except ImportError:
+        pass
+    return result
+
+
 class MockView(base.BaseView):
     # Various properties
     allow_call = True
     allow_access = True
     visible = True
 
+    def create_blueprint(self, admin):
+        blueprint = super(MockView, self).create_blueprint(admin)
+        blueprint.register_templates(load_test_templates())
+        return blueprint
+
     @base.expose('/')
     def index(self):
         return 'Success!'
@@ -42,6 +59,11 @@ class MockView(base.BaseView):
 
 
 class MockMethodView(base.BaseView):
+    def create_blueprint(self, admin):
+        blueprint = super(MockMethodView, self).create_blueprint(admin)
+        blueprint.register_templates(load_test_templates())
+        return blueprint
+
     @base.expose('/')
     def index(self):
         return 'Success!'
--- contrib/python/Flask-Admin/flask_admin/tests/test_form_upload.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/test_form_upload.py	(working tree)
@@ -5,16 +5,21 @@ from nose.tools import eq_, ok_
 
 from flask import Flask, url_for
 from flask_admin import form, helpers
+from library.python import resource
+
+
+def get_data_from_resource(fname):
+    return BytesIO(resource.find('resfs/file/contrib/python/Flask-Admin/flask_admin/tests/data/{}'.format(fname)))
 
 
 def _create_temp():
     path = op.join(op.dirname(__file__), 'tmp')
     if not op.exists(path):
-        os.mkdir(path)
+        os.makedirs(path)
 
     inner = op.join(path, 'inner')
     if not op.exists(inner):
-        os.mkdir(inner)
+        os.makedirs(inner)
 
     return path
 
@@ -145,35 +150,34 @@ def test_image_upload_field():
     dummy = Dummy()
 
     # Check upload
-    filename = op.join(op.dirname(__file__), 'data', 'copyleft.png')
 
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
-            my_form = TestForm(helpers.get_form_data())
+    fp = get_data_from_resource('copyleft.png')
+    with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
+        my_form = TestForm(helpers.get_form_data())
 
-            assert my_form.validate()
+        assert my_form.validate()
 
-            my_form.populate_obj(dummy)
+        my_form.populate_obj(dummy)
 
-            assert dummy.upload == 'test1.png'
-            assert op.exists(op.join(path, 'test1.png'))
-            assert op.exists(op.join(path, 'test1_thumb.png'))
+        assert dummy.upload == 'test1.png'
+        assert op.exists(op.join(path, 'test1.png'))
+        assert op.exists(op.join(path, 'test1_thumb.png'))
 
     # Check replace
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'test2.png')}):
-            my_form = TestForm(helpers.get_form_data())
+    fp = get_data_from_resource('copyleft.png')
+    with app.test_request_context(method='POST', data={'upload': (fp, 'test2.png')}):
+        my_form = TestForm(helpers.get_form_data())
 
-            assert my_form.validate()
+        assert my_form.validate()
 
-            my_form.populate_obj(dummy)
+        my_form.populate_obj(dummy)
 
-            assert dummy.upload == 'test2.png'
-            assert op.exists(op.join(path, 'test2.png'))
-            assert op.exists(op.join(path, 'test2_thumb.png'))
+        assert dummy.upload == 'test2.png'
+        assert op.exists(op.join(path, 'test2.png'))
+        assert op.exists(op.join(path, 'test2_thumb.png'))
 
-            assert not op.exists(op.join(path, 'test1.png'))
-            assert not op.exists(op.join(path, 'test1_thumb.jpg'))
+        assert not op.exists(op.join(path, 'test1.png'))
+        assert not op.exists(op.join(path, 'test1_thumb.jpg'))
 
     # Check delete
     with app.test_request_context(method='POST', data={'_upload-delete': 'checked'}):
@@ -188,62 +192,59 @@ def test_image_upload_field():
         assert not op.exists(op.join(path, 'test2_thumb.png'))
 
     # Check upload no-resize
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
-            my_form = TestNoResizeForm(helpers.get_form_data())
+    fp = get_data_from_resource('copyleft.png')
 
-            assert my_form.validate()
+    with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
+        my_form = TestNoResizeForm(helpers.get_form_data())
 
-            my_form.populate_obj(dummy)
+        assert my_form.validate()
 
-            assert dummy.upload == 'test1.png'
-            assert op.exists(op.join(path, 'test1.png'))
-            assert not op.exists(op.join(path, 'test1_thumb.png'))
+        my_form.populate_obj(dummy)
+
+        assert dummy.upload == 'test1.png'
+        assert op.exists(op.join(path, 'test1.png'))
+        assert not op.exists(op.join(path, 'test1_thumb.png'))
 
     # Check upload, auto-resize
-    filename = op.join(op.dirname(__file__), 'data', 'copyleft.png')
+    fp = get_data_from_resource('copyleft.png')
 
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
-            my_form = TestAutoResizeForm(helpers.get_form_data())
+    with app.test_request_context(method='POST', data={'upload': (fp, 'test1.png')}):
+        my_form = TestAutoResizeForm(helpers.get_form_data())
 
-            assert my_form.validate()
+        assert my_form.validate()
 
-            my_form.populate_obj(dummy)
+        my_form.populate_obj(dummy)
 
-            assert dummy.upload == 'test1.png'
-            assert op.exists(op.join(path, 'test1.png'))
+        assert dummy.upload == 'test1.png'
+        assert op.exists(op.join(path, 'test1.png'))
 
-    filename = op.join(op.dirname(__file__), 'data', 'copyleft.tiff')
+    fp = get_data_from_resource('copyleft.tiff')
 
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'test1.tiff')}):
-            my_form = TestAutoResizeForm(helpers.get_form_data())
+    with app.test_request_context(method='POST', data={'upload': (fp, 'test1.tiff')}):
+        my_form = TestAutoResizeForm(helpers.get_form_data())
 
-            assert my_form.validate()
+        assert my_form.validate()
 
-            my_form.populate_obj(dummy)
+        my_form.populate_obj(dummy)
 
-            assert dummy.upload == 'test1.jpg'
-            assert op.exists(op.join(path, 'test1.jpg'))
+        assert dummy.upload == 'test1.jpg'
+        assert op.exists(op.join(path, 'test1.jpg'))
 
     # check allowed extensions
     for extension in ('gif', 'jpg', 'jpeg', 'png', 'tiff'):
         filename = 'copyleft.' + extension
-        filepath = op.join(op.dirname(__file__), 'data', filename)
-        with open(filepath, 'rb') as fp:
-            with app.test_request_context(method='POST', data={'upload': (fp, filename)}):
-                my_form = TestNoResizeForm(helpers.get_form_data())
-                assert my_form.validate()
-                my_form.populate_obj(dummy)
-                assert dummy.upload == my_form.upload.data.filename
-
-    # check case-sensitivity for extensions
-    filename = op.join(op.dirname(__file__), 'data', 'copyleft.jpg')
-    with open(filename, 'rb') as fp:
-        with app.test_request_context(method='POST', data={'upload': (fp, 'copyleft.JPG')}):
+        fp = get_data_from_resource(filename)
+        with app.test_request_context(method='POST', data={'upload': (fp, filename)}):
             my_form = TestNoResizeForm(helpers.get_form_data())
             assert my_form.validate()
+            my_form.populate_obj(dummy)
+            assert dummy.upload == my_form.upload.data.filename
+
+    # check case-sensitivity for extensions
+    fp = get_data_from_resource('copyleft.jpg')
+    with app.test_request_context(method='POST', data={'upload': (fp, 'copyleft.JPG')}):
+        my_form = TestNoResizeForm(helpers.get_form_data())
+        assert my_form.validate()
 
 
 def test_relative_path():
--- contrib/python/Flask-Admin/flask_admin/tests/test_model.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/test_model.py	(working tree)
@@ -1,1 +1,2 @@
+import pytest
 import wtforms
@@ -133,2 +134,3 @@ def setup():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_mockview():
@@ -233,2 +235,3 @@ def test_permissions():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_templates():
@@ -255,2 +258,3 @@ def test_templates():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_list_columns():
@@ -274,2 +278,3 @@ def test_list_columns():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_exclude_columns():
@@ -460,2 +466,3 @@ def test_custom_form():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_modal_edit():
@@ -748,2 +755,3 @@ def test_export_csv():
 
+@pytest.mark.skip('Skip in Arcadia')
 def test_list_row_actions():
--- contrib/python/Flask-Admin/flask_admin/tests/test_base.py	(index)
+++ contrib/python/Flask-Admin/flask_admin/tests/test_base.py	(working tree)
@@ -7,6 +7,8 @@ from flask.views import MethodView
 
 from flask_admin import base
 
+import library.python.pytest.rewrite
+library.python.pytest.rewrite.AssertionRewritingHook.arcadia_source_finder = None
 
 def load_test_templates():
     result = {}
