--- contrib/python/asyncpg/ya.make	(index)
+++ contrib/python/asyncpg/ya.make	(working tree)
@@ -62,6 +61,6 @@ RESOURCE_FILES(
 
 END()
 
-RECURSE_FOR_TESTS(
-    tests
-)
+IF (NOT AUTOCHECK)
+    RECURSE_FOR_TESTS(tests) # needs local pg server instance
+ENDIF()
