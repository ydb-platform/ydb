# Generated by devtools/yamaker (pypi).

PY3_LIBRARY()

VERSION(0.4.0)

LICENSE(MIT)

PEERDIR(
    contrib/python/rich
    contrib/python/typer
)

NO_LINT()

SET_RESOURCE_URI_FROM_JSON(SANDBOX_RESOURCE_URI resources.json)
FROM_SANDBOX(${SANDBOX_RESOURCE_URI} OUT libcomplexipy.a)

IF (OS_DARWIN)
    LDFLAGS(
        -framework CoreFoundation
        -framework Security
    )
ENDIF()

PY_REGISTER(
    complexipy.rust
)

PY_SRCS(
    TOP_LEVEL
    complexipy/__init__.py
    complexipy/main.py
    complexipy/types.py
    complexipy/utils.py
)

RESOURCE_FILES(
    PREFIX contrib/python/complexipy/
    .dist-info/METADATA
    .dist-info/entry_points.txt
)

END()

RECURSE(
    bin
)
