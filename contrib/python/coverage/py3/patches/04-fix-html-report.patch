--- contrib/python/coverage/py3/coverage/html.py	(index)
+++ contrib/python/coverage/py3/coverage/html.py	(working tree)
@@ -431,14 +431,20 @@ class HtmlReporter:
         """Copy a static file into the output directory with cache busting."""
         data = get_htmlfiles_resource(os.path.basename(src))
         if data is not None:
-            dest = os.path.join(self.directory, os.path.basename(src))
+            # Put static files in a subdir named like the report dir so that
+            # hrefs "python.coverage.report/style.css" etc. resolve correctly
+            static_subdir = os.path.join(self.directory, os.path.basename(self.directory))
+            ensure_dir(static_subdir)
+            dest = os.path.join(static_subdir, os.path.basename(src))
             with open(dest, "wb") as afile:
                 afile.write(data)
+            static_href = os.path.join(os.path.basename(self.directory), os.path.basename(src))
         else:
             dest = copy_with_cache_bust(src, self.directory)
+            static_href = os.path.basename(dest)
         if not slug:
             slug = os.path.basename(src).replace(".", "_")
-        self.template_globals["statics"][slug] = dest  # type: ignore
+        self.template_globals["statics"][slug] = static_href  # type: ignore
 
     def make_local_static_report_files(self) -> None:
         """Make local instances of static files for HTML report."""
