# Generated by devtools/yamaker (pypi).

PY2_LIBRARY()

VERSION(1.6.4)

LICENSE(PSF-2.0)

NO_COMPILER_WARNINGS()

NO_LINT()

IF (OS_LINUX)
    CFLAGS(
        -DHAVE_FALLOCATE=1
        -DHAVE_POSIX_FALLOCATE=1
        -DHAVE_POSIX_FADVISE=1
    )
ENDIF()

IF (OS_DARWIN)
    CFLAGS(
        -DHAVE_APPLE_F_ALLOCATECONTIG=1
    )
ENDIF()

SRCS(
    fallocate/_fallocatemodule.c
)

PY_REGISTER(
    fallocate._fallocate
)

PY_SRCS(
    TOP_LEVEL
    fallocate/__init__.py
)

RESOURCE_FILES(
    PREFIX contrib/python/fallocate/py2/
    .dist-info/METADATA
    .dist-info/top_level.txt
)

END()

RECURSE_FOR_TESTS(
    tests
)
