--- contrib/python/gevent/py2/ya.make	(index)
+++ contrib/python/gevent/py2/ya.make	(working tree)
@@ -43,4 +43,12 @@ NO_CHECK_IMPORTS(
-CFLAGS(
-    -DCARES_EMBED
-    -DHAVE_NETDB_H
-)
+IF(OS_WINDOWS)
+    CFLAGS(
+        -DCARES_EMBED
+    )
+    SRCS(
+        hstrerror_stub.c
+    )
+ELSE()
+    CFLAGS(
+        -DHAVE_NETDB_H
+    )
+ENDIF()
