--- contrib/python/greenlet/py2/ya.make	(index)
+++ contrib/python/greenlet/py2/ya.make	(working tree)
@@ -15,3 +15,8 @@ NO_COMPILER_WARNINGS()
-CFLAGS(
-    -DMS_WIN64=1
-)
+IF (OS_WINDOWS)
+    CFLAGS(
+        -DMS_WIN64=1
+    )
+    SRCS(
+        greenlet/platform/switch_x64_masm.masm
+    )
+ENDIF()
