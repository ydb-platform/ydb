--- contrib/python/langflow-base/langflow/main.py	(7c3dba7f264dbbb3bd1d4ae8fe171a0e05d80faf)
+++ contrib/python/langflow-base/langflow/main.py	(index)
@@ -564,13 +564,21 @@ def setup_static_files(app: FastAPI, static_files_dir: Path) -> None:
     )
 
     @app.exception_handler(404)
-    async def custom_404_handler(_request, _exc):
+    async def custom_404_handler(request: Request, exc):
+        # For API routes, return JSON 404 response
+        if request.url.path.startswith("/api/"):
+            return JSONResponse(
+                status_code=404,
+                content={"detail": "Not Found"},
+            )
+        
+        # For non-API routes (SPA routing), serve index.html
         path = anyio.Path(static_files_dir) / "index.html"
 
         if not await path.exists():
             msg = f"File at path {path} does not exist."
             raise RuntimeError(msg)
-        return FileResponse(path)
+        return FileResponse(str(path))
 
 
 def get_static_files_dir():
--- contrib/python/langflow-base/langflow/alembic/env.py	(7c3dba7f264dbbb3bd1d4ae8fe171a0e05d80faf)
+++ contrib/python/langflow-base/langflow/alembic/env.py	(index)
@@ -118,20 +118,12 @@ def _do_run_migrations(connection):
         context.run_migrations()
 
 async def _run_async_migrations() -> None:
-    # Disable prepared statements for PostgreSQL (required for PgBouncer compatibility)
-    # SQLite doesn't support this parameter, so only add it for PostgreSQL
     config_section = config.get_section(config.config_ini_section, {})
-    db_url = config_section.get("sqlalchemy.url", "")
-
-    connect_args: dict[str, Any] = {}
-    if db_url and "postgresql" in db_url:
-        connect_args["prepare_threshold"] = None
 
     connectable = async_engine_from_config(
         config_section,
         prefix="sqlalchemy.",
         poolclass=pool.NullPool,
-        connect_args=connect_args,
     )
 
     if connectable.dialect.name == "sqlite":
--- contrib/python/langflow-base/langflow/initial_setup/setup.py	(7c3dba7f264dbbb3bd1d4ae8fe171a0e05d80faf)
+++ contrib/python/langflow-base/langflow/initial_setup/setup.py	(index)
@@ -567,7 +567,9 @@ async def copy_profile_pictures() -> None:
         raise ValueError(msg)
 
     # Setup source and target paths
-    origin = anyio.Path(__file__).parent / "profile_pictures"
+    import os
+    langflow_dir = Path(os.getenv("LANGFLOW_DIR")) or Path(__file__).parent.parent
+    origin = anyio.Path(langflow_dir / "initial_setup" / "profile_pictures")
     target = anyio.Path(config_dir) / "profile_pictures"
 
     if not await origin.exists():
--- contrib/python/langflow-base/langflow/services/database/service.py	(7c3dba7f264dbbb3bd1d4ae8fe171a0e05d80faf)
+++ contrib/python/langflow-base/langflow/services/database/service.py	(index)
@@ -52,7 +52,9 @@ class DatabaseService(Service):
 
         # This file is in langflow.services.database.manager.py
         # the ini is in langflow
-        langflow_dir = Path(__file__).parent.parent.parent
+
+        import os
+        langflow_dir = Path(os.getenv("LANGFLOW_DIR")).resolve() if os.getenv("LANGFLOW_DIR") else Path(__file__).parent.parent.parent
         self.script_location = langflow_dir / "alembic"
         self.alembic_cfg_path = langflow_dir / "alembic.ini"
 
@@ -119,7 +121,7 @@ class DatabaseService(Service):
                     "Use postgresql instead. "
                     "To avoid this warning, update the database URL."
                 )
-            driver = "postgresql+psycopg"
+            driver = "postgresql+asyncpg"
 
         self.database_url = f"{driver}://{url_components[1]}"
 
@@ -180,9 +182,10 @@ class DatabaseService(Service):
                 "check_same_thread": False,
                 "timeout": settings.db_connect_timeout,
             }
-        # For PostgreSQL, set the timezone to UTC
+        # For PostgreSQL with asyncpg, set server_settings instead of options
         if settings.database_url and settings.database_url.startswith(("postgresql", "postgres")):
-            return {"options": "-c timezone=utc"}
+            # asyncpg uses server_settings dict, not options string
+            return {"server_settings": {"timezone": "utc"}}
         return {}
 
     def on_connection(self, dbapi_connection, _connection_record) -> None:
