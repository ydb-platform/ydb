--- contrib/python/linkify-it-py/tests/utils.py	(index)
+++ contrib/python/linkify-it-py/tests/utils.py	(working tree)
@@ -9,6 +9,8 @@ def lget(src_list, index):
 
 
 def read_fixture_file(path):
+    import yatest.common
+    path = yatest.common.source_path(str(path))
     tests = []
     skip_next = False
     comment_re = re.compile(r"^%.*")
