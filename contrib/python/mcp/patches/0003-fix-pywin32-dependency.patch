--- contrib/python/mcp/mcp/os/win32/utilities.py	(index)
+++ contrib/python/mcp/mcp/os/win32/utilities.py	(working tree)
@@ -19,10 +19,17 @@ logger = logging.getLogger("client.stdio.win32")
 
 # Windows-specific imports for Job Objects
 if sys.platform == "win32":
-    import pywintypes
-    import win32api
-    import win32con
-    import win32job
+    try:
+        import pywintypes
+        import win32api
+        import win32con
+        import win32job
+    except ImportError:
+        # pywin32 packages are not available in Arcadia at the moment
+        pywintypes = None
+        win32api = None
+        win32con = None
+        win32job = None
 else:
     # Type stubs for non-Windows platforms
     win32api = None
