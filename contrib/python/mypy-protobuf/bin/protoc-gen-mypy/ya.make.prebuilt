SET_RESOURCE_URI_FROM_JSON(SANDBOX_RESOURCE_URI resources.json)

IF (SANDBOX_RESOURCE_URI != "")
    ENABLE(PREBUILT)

    PREBUILT_PROGRAM(protoc-gen-mypy)

	LICENSE(Apache-2.0)

    VERSION(Service-proxy-version)

    DECLARE_EXTERNAL_RESOURCE(MYPY_PROTOBUF ${SANDBOX_RESOURCE_URI})

    PRIMARY_OUTPUT(${MYPY_PROTOBUF_RESOURCE_GLOBAL}/protoc-gen-mypy${MODULE_SUFFIX})

    END()
ENDIF()
