# Generated by devtools/yamaker (pypi).

PY3_LIBRARY()

VERSION(0.11.0)

LICENSE(MIT)

NO_COMPILER_WARNINGS()

NO_LINT()

IF (OS_DARWIN)
    CFLAGS(
        -DENABLE_DTRACE
        -DMACOSX
        -DNETIFACES_VERSION=0.11.0
        -DHAVE_GETIFADDRS=1
        -DHAVE_GETNAMEINFO=1
        -DHAVE_NET_IF_DL_H=1
        -DHAVE_SOCKADDR_SA_LEN=1
        -DHAVE_PF_ROUTE=1
        -DHAVE_SYSCTL_CTL_NET=1
    )
ELSEIF (OS_LINUX)
    IF (NOT MUSL)
        CFLAGS(
            -DHAVE_NETASH_ASH_H=1
            -DHAVE_NETATALK_AT_H=1
            -DHAVE_NETAX25_AX25_H=1
            -DHAVE_NETECONET_EC_H=1
            -DHAVE_SOCKADDR_AX25=1
            -DHAVE_SOCKADDR_AT=1
            -DHAVE_SOCKADDR_ASH=1
            -DHAVE_SOCKADDR_EC=1
        )
    ENDIF()
    CFLAGS(
        -DNETIFACES_VERSION=0.11.0
        -DHAVE_GETIFADDRS=1
        -DHAVE_GETNAMEINFO=1
        -DHAVE_NETPACKET_PACKET_H=1
        -DHAVE_LINUX_ATM_H=1
        -DHAVE_LINUX_LLC_H=1
        -DHAVE_LINUX_TIPC_H=1
        -DHAVE_SOCKADDR_IN=1
        -DHAVE_SOCKADDR_IN6=1
        -DHAVE_SOCKADDR_UN=1
        -DHAVE_SOCKADDR_LL=1
        -DHAVE_SOCKADDR_ATMPVC=1
        -DHAVE_SOCKADDR_ATMSVC=1
        -DHAVE_SOCKADDR_LLC=1
        -DHAVE_PF_NETLINK=1
    )
ENDIF()

SRCS(
    netifaces.c
)

PY_REGISTER(
    netifaces
)

RESOURCE_FILES(
    PREFIX contrib/python/netifaces/py3/
    .dist-info/METADATA
    .dist-info/top_level.txt
)

END()
