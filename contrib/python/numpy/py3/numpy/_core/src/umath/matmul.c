#line 1 "../numpy/_core/src/umath/matmul.c.src"

/*
 *****************************************************************************
 **       This file was autogenerated from a template  DO NOT EDIT!!!!      **
 **       Changes should be made to the original source (.src) file         **
 *****************************************************************************
 */

#line 1
/* -*- c -*- */
#define PY_SSIZE_T_CLEAN
#include <Python.h>

#define _UMATHMODULE
#define _MULTIARRAYMODULE
#define NPY_NO_DEPRECATED_API NPY_API_VERSION

#include "npy_config.h"
#include "numpy/npy_common.h"
#include "numpy/arrayobject.h"
#include "numpy/ufuncobject.h"
#include "numpy/npy_math.h"
#include "numpy/halffloat.h"
#include "lowlevel_strided_loops.h"



#include "npy_cblas.h"
#include "arraytypes.h" /* For TYPE_dot functions */

#include <assert.h>

/*
 *****************************************************************************
 **                            BASICS                                       **
 *****************************************************************************
 */

#if defined(HAVE_CBLAS)
/*
 * -1 to be conservative, in case blas internally uses a for loop with an
 * inclusive upper bound
 */
#ifndef HAVE_BLAS_ILP64
#define BLAS_MAXSIZE (NPY_MAX_INT - 1)
#else
#define BLAS_MAXSIZE (NPY_MAX_INT64 - 1)
#endif

/*
 * Determine if a 2d matrix can be used by BLAS
 * 1. Strides must not alias or overlap
 * 2. The faster (second) axis must be contiguous
 * 3. The slower (first) axis stride, in unit steps, must be larger than
 *    the faster axis dimension
 */
static inline npy_bool
is_blasable2d(npy_intp byte_stride1, npy_intp byte_stride2,
              npy_intp d1, npy_intp d2,  npy_intp itemsize)
{
    npy_intp unit_stride1 = byte_stride1 / itemsize;
    if (byte_stride2 != itemsize) {
        return NPY_FALSE;
    }
    if ((byte_stride1 % itemsize ==0) &&
        (unit_stride1 >= d2) &&
        (unit_stride1 <= BLAS_MAXSIZE))
    {
        return NPY_TRUE;
    }
    return NPY_FALSE;
}

#if defined(_MSC_VER) && !defined(__INTEL_COMPILER)
static const npy_cdouble oneD = {1.0, 0.0}, zeroD = {0.0, 0.0};
static const npy_cfloat oneF = {1.0f, 0.0f}, zeroF = {0.0f, 0.0f};
#else
static const npy_cdouble oneD = 1.0, zeroD = 0.0;
static const npy_cfloat oneF = 1.0f, zeroF = 0.0f;
#endif

#line 82
NPY_NO_EXPORT void
FLOAT_gemv(void *ip1, npy_intp is1_m, npy_intp is1_n,
            void *ip2, npy_intp is2_n,
            void *op, npy_intp op_m,
            npy_intp m, npy_intp n)
{
    /*
     * Vector matrix multiplication -- Level 2 BLAS
     * arguments
     * ip1: contiguous data, m*n shape
     * ip2: data in c order, n*1 shape
     * op:  data in c order, m shape
     */
    enum CBLAS_ORDER order;
    CBLAS_INT M, N, lda;

    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE);
    assert (is_blasable2d(is2_n, sizeof(npy_float), n, 1, sizeof(npy_float)));
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_float))) {
        order = CblasColMajor;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_float));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        order = CblasRowMajor;
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_float)));
        lda = (CBLAS_INT)(is1_n / sizeof(npy_float));
    }
    CBLAS_FUNC(cblas_sgemv)(order, CblasTrans, N, M, 1.F, ip1, lda, ip2,
                                     is2_n / sizeof(npy_float), 0.F, op, op_m / sizeof(npy_float));
}

NPY_NO_EXPORT void
FLOAT_matmul_matrixmatrix(void *ip1, npy_intp is1_m, npy_intp is1_n,
                           void *ip2, npy_intp is2_n, npy_intp is2_p,
                           void *op, npy_intp os_m, npy_intp os_p,
                           npy_intp m, npy_intp n, npy_intp p)
{
    /*
     * matrix matrix multiplication -- Level 3 BLAS
     */
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT M, N, P, lda, ldb, ldc;
    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE && p <= BLAS_MAXSIZE);
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;
    P = (CBLAS_INT)p;

    assert(is_blasable2d(os_m, os_p, m, p, sizeof(npy_float)));
    ldc = (CBLAS_INT)(os_m / sizeof(npy_float));

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_float))) {
        trans1 = CblasNoTrans;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_float));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_float)));
        trans1 = CblasTrans;
        lda = (CBLAS_INT)(is1_n / sizeof(npy_float));
    }

    if (is_blasable2d(is2_n, is2_p, n, p, sizeof(npy_float))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_float));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is2_p, is2_n, p, n, sizeof(npy_float)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_p / sizeof(npy_float));
    }
    /*
     * Use syrk if we have a case of a matrix times its transpose.
     * Otherwise, use gemm for all other cases.
     */
    if (
        (ip1 == ip2) &&
        (m == p) &&
        (is1_m == is2_p) &&
        (is1_n == is2_n) &&
        (trans1 != trans2)
    ) {
        npy_intp i,j;
        if (trans1 == CblasNoTrans) {
            CBLAS_FUNC(cblas_ssyrk)(
                order, CblasUpper, trans1, P, N, 1.F,
                ip1, lda, 0.F, op, ldc);
        }
        else {
            CBLAS_FUNC(cblas_ssyrk)(
                order, CblasUpper, trans1, P, N, 1.F,
                ip1, ldb, 0.F, op, ldc);
        }
        /* Copy the triangle */
        for (i = 0; i < P; i++) {
            for (j = i + 1; j < P; j++) {
                ((npy_float*)op)[j * ldc + i] = ((npy_float*)op)[i * ldc + j];
            }
        }

    }
    else {
        CBLAS_FUNC(cblas_sgemm)(
            order, trans1, trans2, M, P, N, 1.F, ip1, lda,
            ip2, ldb, 0.F, op, ldc);
    }
}


#line 82
NPY_NO_EXPORT void
DOUBLE_gemv(void *ip1, npy_intp is1_m, npy_intp is1_n,
            void *ip2, npy_intp is2_n,
            void *op, npy_intp op_m,
            npy_intp m, npy_intp n)
{
    /*
     * Vector matrix multiplication -- Level 2 BLAS
     * arguments
     * ip1: contiguous data, m*n shape
     * ip2: data in c order, n*1 shape
     * op:  data in c order, m shape
     */
    enum CBLAS_ORDER order;
    CBLAS_INT M, N, lda;

    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE);
    assert (is_blasable2d(is2_n, sizeof(npy_double), n, 1, sizeof(npy_double)));
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_double))) {
        order = CblasColMajor;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_double));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        order = CblasRowMajor;
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_double)));
        lda = (CBLAS_INT)(is1_n / sizeof(npy_double));
    }
    CBLAS_FUNC(cblas_dgemv)(order, CblasTrans, N, M, 1., ip1, lda, ip2,
                                     is2_n / sizeof(npy_double), 0., op, op_m / sizeof(npy_double));
}

NPY_NO_EXPORT void
DOUBLE_matmul_matrixmatrix(void *ip1, npy_intp is1_m, npy_intp is1_n,
                           void *ip2, npy_intp is2_n, npy_intp is2_p,
                           void *op, npy_intp os_m, npy_intp os_p,
                           npy_intp m, npy_intp n, npy_intp p)
{
    /*
     * matrix matrix multiplication -- Level 3 BLAS
     */
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT M, N, P, lda, ldb, ldc;
    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE && p <= BLAS_MAXSIZE);
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;
    P = (CBLAS_INT)p;

    assert(is_blasable2d(os_m, os_p, m, p, sizeof(npy_double)));
    ldc = (CBLAS_INT)(os_m / sizeof(npy_double));

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_double))) {
        trans1 = CblasNoTrans;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_double));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_double)));
        trans1 = CblasTrans;
        lda = (CBLAS_INT)(is1_n / sizeof(npy_double));
    }

    if (is_blasable2d(is2_n, is2_p, n, p, sizeof(npy_double))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_double));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is2_p, is2_n, p, n, sizeof(npy_double)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_p / sizeof(npy_double));
    }
    /*
     * Use syrk if we have a case of a matrix times its transpose.
     * Otherwise, use gemm for all other cases.
     */
    if (
        (ip1 == ip2) &&
        (m == p) &&
        (is1_m == is2_p) &&
        (is1_n == is2_n) &&
        (trans1 != trans2)
    ) {
        npy_intp i,j;
        if (trans1 == CblasNoTrans) {
            CBLAS_FUNC(cblas_dsyrk)(
                order, CblasUpper, trans1, P, N, 1.,
                ip1, lda, 0., op, ldc);
        }
        else {
            CBLAS_FUNC(cblas_dsyrk)(
                order, CblasUpper, trans1, P, N, 1.,
                ip1, ldb, 0., op, ldc);
        }
        /* Copy the triangle */
        for (i = 0; i < P; i++) {
            for (j = i + 1; j < P; j++) {
                ((npy_double*)op)[j * ldc + i] = ((npy_double*)op)[i * ldc + j];
            }
        }

    }
    else {
        CBLAS_FUNC(cblas_dgemm)(
            order, trans1, trans2, M, P, N, 1., ip1, lda,
            ip2, ldb, 0., op, ldc);
    }
}


#line 82
NPY_NO_EXPORT void
CFLOAT_gemv(void *ip1, npy_intp is1_m, npy_intp is1_n,
            void *ip2, npy_intp is2_n,
            void *op, npy_intp op_m,
            npy_intp m, npy_intp n)
{
    /*
     * Vector matrix multiplication -- Level 2 BLAS
     * arguments
     * ip1: contiguous data, m*n shape
     * ip2: data in c order, n*1 shape
     * op:  data in c order, m shape
     */
    enum CBLAS_ORDER order;
    CBLAS_INT M, N, lda;

    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE);
    assert (is_blasable2d(is2_n, sizeof(npy_cfloat), n, 1, sizeof(npy_cfloat)));
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_cfloat))) {
        order = CblasColMajor;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_cfloat));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        order = CblasRowMajor;
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_cfloat)));
        lda = (CBLAS_INT)(is1_n / sizeof(npy_cfloat));
    }
    CBLAS_FUNC(cblas_cgemv)(order, CblasTrans, N, M, &oneF, ip1, lda, ip2,
                                     is2_n / sizeof(npy_cfloat), &zeroF, op, op_m / sizeof(npy_cfloat));
}

NPY_NO_EXPORT void
CFLOAT_matmul_matrixmatrix(void *ip1, npy_intp is1_m, npy_intp is1_n,
                           void *ip2, npy_intp is2_n, npy_intp is2_p,
                           void *op, npy_intp os_m, npy_intp os_p,
                           npy_intp m, npy_intp n, npy_intp p)
{
    /*
     * matrix matrix multiplication -- Level 3 BLAS
     */
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT M, N, P, lda, ldb, ldc;
    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE && p <= BLAS_MAXSIZE);
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;
    P = (CBLAS_INT)p;

    assert(is_blasable2d(os_m, os_p, m, p, sizeof(npy_cfloat)));
    ldc = (CBLAS_INT)(os_m / sizeof(npy_cfloat));

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_cfloat))) {
        trans1 = CblasNoTrans;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_cfloat));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_cfloat)));
        trans1 = CblasTrans;
        lda = (CBLAS_INT)(is1_n / sizeof(npy_cfloat));
    }

    if (is_blasable2d(is2_n, is2_p, n, p, sizeof(npy_cfloat))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_cfloat));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is2_p, is2_n, p, n, sizeof(npy_cfloat)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_p / sizeof(npy_cfloat));
    }
    /*
     * Use syrk if we have a case of a matrix times its transpose.
     * Otherwise, use gemm for all other cases.
     */
    if (
        (ip1 == ip2) &&
        (m == p) &&
        (is1_m == is2_p) &&
        (is1_n == is2_n) &&
        (trans1 != trans2)
    ) {
        npy_intp i,j;
        if (trans1 == CblasNoTrans) {
            CBLAS_FUNC(cblas_csyrk)(
                order, CblasUpper, trans1, P, N, &oneF,
                ip1, lda, &zeroF, op, ldc);
        }
        else {
            CBLAS_FUNC(cblas_csyrk)(
                order, CblasUpper, trans1, P, N, &oneF,
                ip1, ldb, &zeroF, op, ldc);
        }
        /* Copy the triangle */
        for (i = 0; i < P; i++) {
            for (j = i + 1; j < P; j++) {
                ((npy_cfloat*)op)[j * ldc + i] = ((npy_cfloat*)op)[i * ldc + j];
            }
        }

    }
    else {
        CBLAS_FUNC(cblas_cgemm)(
            order, trans1, trans2, M, P, N, &oneF, ip1, lda,
            ip2, ldb, &zeroF, op, ldc);
    }
}


#line 82
NPY_NO_EXPORT void
CDOUBLE_gemv(void *ip1, npy_intp is1_m, npy_intp is1_n,
            void *ip2, npy_intp is2_n,
            void *op, npy_intp op_m,
            npy_intp m, npy_intp n)
{
    /*
     * Vector matrix multiplication -- Level 2 BLAS
     * arguments
     * ip1: contiguous data, m*n shape
     * ip2: data in c order, n*1 shape
     * op:  data in c order, m shape
     */
    enum CBLAS_ORDER order;
    CBLAS_INT M, N, lda;

    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE);
    assert (is_blasable2d(is2_n, sizeof(npy_cdouble), n, 1, sizeof(npy_cdouble)));
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_cdouble))) {
        order = CblasColMajor;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_cdouble));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        order = CblasRowMajor;
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_cdouble)));
        lda = (CBLAS_INT)(is1_n / sizeof(npy_cdouble));
    }
    CBLAS_FUNC(cblas_zgemv)(order, CblasTrans, N, M, &oneD, ip1, lda, ip2,
                                     is2_n / sizeof(npy_cdouble), &zeroD, op, op_m / sizeof(npy_cdouble));
}

NPY_NO_EXPORT void
CDOUBLE_matmul_matrixmatrix(void *ip1, npy_intp is1_m, npy_intp is1_n,
                           void *ip2, npy_intp is2_n, npy_intp is2_p,
                           void *op, npy_intp os_m, npy_intp os_p,
                           npy_intp m, npy_intp n, npy_intp p)
{
    /*
     * matrix matrix multiplication -- Level 3 BLAS
     */
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT M, N, P, lda, ldb, ldc;
    assert(m <= BLAS_MAXSIZE && n <= BLAS_MAXSIZE && p <= BLAS_MAXSIZE);
    M = (CBLAS_INT)m;
    N = (CBLAS_INT)n;
    P = (CBLAS_INT)p;

    assert(is_blasable2d(os_m, os_p, m, p, sizeof(npy_cdouble)));
    ldc = (CBLAS_INT)(os_m / sizeof(npy_cdouble));

    if (is_blasable2d(is1_m, is1_n, m, n, sizeof(npy_cdouble))) {
        trans1 = CblasNoTrans;
        lda = (CBLAS_INT)(is1_m / sizeof(npy_cdouble));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is1_n, is1_m, n, m, sizeof(npy_cdouble)));
        trans1 = CblasTrans;
        lda = (CBLAS_INT)(is1_n / sizeof(npy_cdouble));
    }

    if (is_blasable2d(is2_n, is2_p, n, p, sizeof(npy_cdouble))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_cdouble));
    }
    else {
        /* If not ColMajor, caller should have ensured we are RowMajor */
        /* will not assert in release mode */
        assert(is_blasable2d(is2_p, is2_n, p, n, sizeof(npy_cdouble)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_p / sizeof(npy_cdouble));
    }
    /*
     * Use syrk if we have a case of a matrix times its transpose.
     * Otherwise, use gemm for all other cases.
     */
    if (
        (ip1 == ip2) &&
        (m == p) &&
        (is1_m == is2_p) &&
        (is1_n == is2_n) &&
        (trans1 != trans2)
    ) {
        npy_intp i,j;
        if (trans1 == CblasNoTrans) {
            CBLAS_FUNC(cblas_zsyrk)(
                order, CblasUpper, trans1, P, N, &oneD,
                ip1, lda, &zeroD, op, ldc);
        }
        else {
            CBLAS_FUNC(cblas_zsyrk)(
                order, CblasUpper, trans1, P, N, &oneD,
                ip1, ldb, &zeroD, op, ldc);
        }
        /* Copy the triangle */
        for (i = 0; i < P; i++) {
            for (j = i + 1; j < P; j++) {
                ((npy_cdouble*)op)[j * ldc + i] = ((npy_cdouble*)op)[i * ldc + j];
            }
        }

    }
    else {
        CBLAS_FUNC(cblas_zgemm)(
            order, trans1, trans2, M, P, N, &oneD, ip1, lda,
            ip2, ldb, &zeroD, op, ldc);
    }
}


#endif

/*
 * matmul loops
 * signature is (m?,n),(n,p?)->(m?,p?)
 */

#line 221

NPY_NO_EXPORT void
LONGDOUBLE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetreal((npy_longdouble *)op, 0.0);
            npy_csetimag((npy_longdouble *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_longdouble *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_longdouble val1 = (*(npy_longdouble *)ip1);
                npy_longdouble val2 = (*(npy_longdouble *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetreal((npy_longdouble *) op, npy_creal(*((npy_longdouble *) op)) + ((npy_creal(val1) * npy_creal(val2)) -
                                       (npy_cimag(val1) * npy_cimag(val2))));
                npy_csetimag((npy_longdouble *) op, npy_cimag(*((npy_longdouble *) op)) + ((npy_creal(val1) * npy_cimag(val2)) +
                                       (npy_cimag(val1) * npy_creal(val2))));
#else
                *(npy_longdouble *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_longdouble *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
FLOAT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetreal((npy_float *)op, 0.0);
            npy_csetimag((npy_float *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_float *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_float val1 = (*(npy_float *)ip1);
                npy_float val2 = (*(npy_float *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetreal((npy_float *) op, npy_creal(*((npy_float *) op)) + ((npy_creal(val1) * npy_creal(val2)) -
                                       (npy_cimag(val1) * npy_cimag(val2))));
                npy_csetimag((npy_float *) op, npy_cimag(*((npy_float *) op)) + ((npy_creal(val1) * npy_cimag(val2)) +
                                       (npy_cimag(val1) * npy_creal(val2))));
#else
                *(npy_float *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_float *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
DOUBLE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetreal((npy_double *)op, 0.0);
            npy_csetimag((npy_double *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_double *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_double val1 = (*(npy_double *)ip1);
                npy_double val2 = (*(npy_double *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetreal((npy_double *) op, npy_creal(*((npy_double *) op)) + ((npy_creal(val1) * npy_creal(val2)) -
                                       (npy_cimag(val1) * npy_cimag(val2))));
                npy_csetimag((npy_double *) op, npy_cimag(*((npy_double *) op)) + ((npy_creal(val1) * npy_cimag(val2)) +
                                       (npy_cimag(val1) * npy_creal(val2))));
#else
                *(npy_double *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_double *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
HALF_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetreal((npy_half *)op, 0.0);
            npy_csetimag((npy_half *)op, 0.0);
#elif 1
            float sum = 0;
#else
            *(npy_half *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_half val1 = (*(npy_half *)ip1);
                npy_half val2 = (*(npy_half *)ip2);
#if 1
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetreal((npy_half *) op, npy_creal(*((npy_half *) op)) + ((npy_creal(val1) * npy_creal(val2)) -
                                       (npy_cimag(val1) * npy_cimag(val2))));
                npy_csetimag((npy_half *) op, npy_cimag(*((npy_half *) op)) + ((npy_creal(val1) * npy_cimag(val2)) +
                                       (npy_cimag(val1) * npy_creal(val2))));
#else
                *(npy_half *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 1
            *(npy_half *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
CFLOAT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 1 == 1
            npy_csetrealf((npy_cfloat *)op, 0.0);
            npy_csetimagf((npy_cfloat *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_cfloat *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_cfloat val1 = (*(npy_cfloat *)ip1);
                npy_cfloat val2 = (*(npy_cfloat *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 1 == 1
                npy_csetrealf((npy_cfloat *) op, npy_crealf(*((npy_cfloat *) op)) + ((npy_crealf(val1) * npy_crealf(val2)) -
                                       (npy_cimagf(val1) * npy_cimagf(val2))));
                npy_csetimagf((npy_cfloat *) op, npy_cimagf(*((npy_cfloat *) op)) + ((npy_crealf(val1) * npy_cimagf(val2)) +
                                       (npy_cimagf(val1) * npy_crealf(val2))));
#else
                *(npy_cfloat *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_cfloat *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
CDOUBLE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 1 == 1
            npy_csetreal((npy_cdouble *)op, 0.0);
            npy_csetimag((npy_cdouble *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_cdouble *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_cdouble val1 = (*(npy_cdouble *)ip1);
                npy_cdouble val2 = (*(npy_cdouble *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 1 == 1
                npy_csetreal((npy_cdouble *) op, npy_creal(*((npy_cdouble *) op)) + ((npy_creal(val1) * npy_creal(val2)) -
                                       (npy_cimag(val1) * npy_cimag(val2))));
                npy_csetimag((npy_cdouble *) op, npy_cimag(*((npy_cdouble *) op)) + ((npy_creal(val1) * npy_cimag(val2)) +
                                       (npy_cimag(val1) * npy_creal(val2))));
#else
                *(npy_cdouble *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_cdouble *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
CLONGDOUBLE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 1 == 1
            npy_csetreall((npy_clongdouble *)op, 0.0);
            npy_csetimagl((npy_clongdouble *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_clongdouble *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_clongdouble val1 = (*(npy_clongdouble *)ip1);
                npy_clongdouble val2 = (*(npy_clongdouble *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 1 == 1
                npy_csetreall((npy_clongdouble *) op, npy_creall(*((npy_clongdouble *) op)) + ((npy_creall(val1) * npy_creall(val2)) -
                                       (npy_cimagl(val1) * npy_cimagl(val2))));
                npy_csetimagl((npy_clongdouble *) op, npy_cimagl(*((npy_clongdouble *) op)) + ((npy_creall(val1) * npy_cimagl(val2)) +
                                       (npy_cimagl(val1) * npy_creall(val2))));
#else
                *(npy_clongdouble *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_clongdouble *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
UBYTE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_ubyte *)op, 0.0);
            npy_csetimagx((npy_ubyte *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_ubyte *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_ubyte val1 = (*(npy_ubyte *)ip1);
                npy_ubyte val2 = (*(npy_ubyte *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_ubyte *) op, npy_crealx(*((npy_ubyte *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_ubyte *) op, npy_cimagx(*((npy_ubyte *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_ubyte *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_ubyte *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
USHORT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_ushort *)op, 0.0);
            npy_csetimagx((npy_ushort *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_ushort *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_ushort val1 = (*(npy_ushort *)ip1);
                npy_ushort val2 = (*(npy_ushort *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_ushort *) op, npy_crealx(*((npy_ushort *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_ushort *) op, npy_cimagx(*((npy_ushort *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_ushort *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_ushort *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
UINT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_uint *)op, 0.0);
            npy_csetimagx((npy_uint *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_uint *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_uint val1 = (*(npy_uint *)ip1);
                npy_uint val2 = (*(npy_uint *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_uint *) op, npy_crealx(*((npy_uint *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_uint *) op, npy_cimagx(*((npy_uint *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_uint *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_uint *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
ULONG_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_ulong *)op, 0.0);
            npy_csetimagx((npy_ulong *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_ulong *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_ulong val1 = (*(npy_ulong *)ip1);
                npy_ulong val2 = (*(npy_ulong *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_ulong *) op, npy_crealx(*((npy_ulong *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_ulong *) op, npy_cimagx(*((npy_ulong *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_ulong *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_ulong *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
ULONGLONG_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_ulonglong *)op, 0.0);
            npy_csetimagx((npy_ulonglong *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_ulonglong *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_ulonglong val1 = (*(npy_ulonglong *)ip1);
                npy_ulonglong val2 = (*(npy_ulonglong *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_ulonglong *) op, npy_crealx(*((npy_ulonglong *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_ulonglong *) op, npy_cimagx(*((npy_ulonglong *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_ulonglong *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_ulonglong *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
BYTE_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_byte *)op, 0.0);
            npy_csetimagx((npy_byte *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_byte *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_byte val1 = (*(npy_byte *)ip1);
                npy_byte val2 = (*(npy_byte *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_byte *) op, npy_crealx(*((npy_byte *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_byte *) op, npy_cimagx(*((npy_byte *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_byte *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_byte *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
SHORT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_short *)op, 0.0);
            npy_csetimagx((npy_short *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_short *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_short val1 = (*(npy_short *)ip1);
                npy_short val2 = (*(npy_short *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_short *) op, npy_crealx(*((npy_short *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_short *) op, npy_cimagx(*((npy_short *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_short *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_short *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
INT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_int *)op, 0.0);
            npy_csetimagx((npy_int *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_int *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_int val1 = (*(npy_int *)ip1);
                npy_int val2 = (*(npy_int *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_int *) op, npy_crealx(*((npy_int *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_int *) op, npy_cimagx(*((npy_int *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_int *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_int *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
LONG_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_long *)op, 0.0);
            npy_csetimagx((npy_long *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_long *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_long val1 = (*(npy_long *)ip1);
                npy_long val2 = (*(npy_long *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_long *) op, npy_crealx(*((npy_long *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_long *) op, npy_cimagx(*((npy_long *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_long *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_long *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 221

NPY_NO_EXPORT void
LONGLONG_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib1_n, ib2_n, ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib1_n = is1_n * dn;
    ib2_n = is2_n * dn;
    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
#if 0 == 1
            npy_csetrealx((npy_longlong *)op, 0.0);
            npy_csetimagx((npy_longlong *)op, 0.0);
#elif 0
            float sum = 0;
#else
            *(npy_longlong *)op = 0;
#endif
            for (n = 0; n < dn; n++) {
                npy_longlong val1 = (*(npy_longlong *)ip1);
                npy_longlong val2 = (*(npy_longlong *)ip2);
#if 0
                sum += npy_half_to_float(val1) * npy_half_to_float(val2);
#elif 0 == 1
                npy_csetrealx((npy_longlong *) op, npy_crealx(*((npy_longlong *) op)) + ((npy_crealx(val1) * npy_crealx(val2)) -
                                       (npy_cimagx(val1) * npy_cimagx(val2))));
                npy_csetimagx((npy_longlong *) op, npy_cimagx(*((npy_longlong *) op)) + ((npy_crealx(val1) * npy_cimagx(val2)) +
                                       (npy_cimagx(val1) * npy_crealx(val2))));
#else
                *(npy_longlong *)op += val1 * val2;
#endif
                ip2 += is2_n;
                ip1 += is1_n;
            }
#if 0
            *(npy_longlong *)op = npy_float_to_half(sum);
#endif
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


NPY_NO_EXPORT void
BOOL_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)

{
    npy_intp m, n, p;
    npy_intp ib2_p, ob_p;
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    ib2_p = is2_p * dp;
    ob_p  = os_p * dp;

    for (m = 0; m < dm; m++) {
        for (p = 0; p < dp; p++) {
            char *ip1tmp = ip1;
            char *ip2tmp = ip2;
            *(npy_bool *)op = NPY_FALSE;
            for (n = 0; n < dn; n++) {
                npy_bool val1 = (*(npy_bool *)ip1tmp);
                npy_bool val2 = (*(npy_bool *)ip2tmp);
                if (val1 != 0 && val2 != 0) {
                    *(npy_bool *)op = NPY_TRUE;
                    break;
                }
                ip2tmp += is2_n;
                ip1tmp += is1_n;
            }
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}

NPY_NO_EXPORT void
OBJECT_matmul_inner_noblas(void *_ip1, npy_intp is1_m, npy_intp is1_n,
                           void *_ip2, npy_intp is2_n, npy_intp is2_p,
                           void *_op, npy_intp os_m, npy_intp os_p,
                           npy_intp dm, npy_intp dn, npy_intp dp)
{
    char *ip1 = (char *)_ip1, *ip2 = (char *)_ip2, *op = (char *)_op;

    npy_intp ib1_n = is1_n * dn;
    npy_intp ib2_n = is2_n * dn;
    npy_intp ib2_p = is2_p * dp;
    npy_intp ob_p  = os_p * dp;

    PyObject *product, *sum_of_products = NULL;

    for (npy_intp m = 0; m < dm; m++) {
        for (npy_intp p = 0; p < dp; p++) {
            if ( 0 == dn ) {
                sum_of_products = PyLong_FromLong(0);
                if (sum_of_products == NULL) {
                    return;
                }
            }

            for (npy_intp n = 0; n < dn; n++) {
                PyObject *obj1 = *(PyObject**)ip1, *obj2 = *(PyObject**)ip2;
                if (obj1 == NULL) {
                    obj1 = Py_None;
                }
                if (obj2 == NULL) {
                    obj2 = Py_None;
                }

                product = PyNumber_Multiply(obj1, obj2);
                if (product == NULL) {
                    Py_XDECREF(sum_of_products);
                    return;
                }

                if (n == 0) {
                    sum_of_products = product;
                }
                else {
                    Py_SETREF(sum_of_products, PyNumber_Add(sum_of_products, product));
                    Py_DECREF(product);
                    if (sum_of_products == NULL) {
                        return;
                    }
                }

                ip2 += is2_n;
                ip1 += is1_n;
            }

            *((PyObject **)op) = sum_of_products;
            ip1 -= ib1_n;
            ip2 -= ib2_n;
            op  +=  os_p;
            ip2 += is2_p;
        }
        op -= ob_p;
        ip2 -= ib2_p;
        ip1 += is1_m;
        op  +=  os_m;
    }
}


#line 401


NPY_NO_EXPORT void
FLOAT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 1 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_float);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            FLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                FLOAT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                FLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                FLOAT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                FLOAT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                FLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                FLOAT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                FLOAT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                FLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        FLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
DOUBLE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 1 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_double);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            DOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                DOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                DOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                DOUBLE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                DOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                DOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                DOUBLE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                DOUBLE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                DOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        DOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
LONGDOUBLE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_longdouble);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            LONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                LONGDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                LONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                LONGDOUBLE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                LONGDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                LONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                LONGDOUBLE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                LONGDOUBLE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                LONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        LONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
HALF_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_half);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            HALF_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                HALF_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                HALF_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                HALF_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                HALF_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                HALF_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                HALF_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                HALF_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                HALF_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        HALF_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
CFLOAT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 1 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_cfloat);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            CFLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                CFLOAT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                CFLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                CFLOAT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                CFLOAT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                CFLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                CFLOAT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                CFLOAT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                CFLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        CFLOAT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
CDOUBLE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 1 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_cdouble);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            CDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                CDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                CDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                CDOUBLE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                CDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                CDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                CDOUBLE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                CDOUBLE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                CDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        CDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
CLONGDOUBLE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_clongdouble);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            CLONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                CLONGDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                CLONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                CLONGDOUBLE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                CLONGDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                CLONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                CLONGDOUBLE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                CLONGDOUBLE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                CLONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        CLONGDOUBLE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
UBYTE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_ubyte);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            UBYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                UBYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                UBYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                UBYTE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                UBYTE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                UBYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                UBYTE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                UBYTE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                UBYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        UBYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
USHORT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_ushort);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            USHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                USHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                USHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                USHORT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                USHORT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                USHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                USHORT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                USHORT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                USHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        USHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
UINT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_uint);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            UINT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                UINT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                UINT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                UINT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                UINT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                UINT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                UINT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                UINT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                UINT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        UINT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
ULONG_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_ulong);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            ULONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                ULONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                ULONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                ULONG_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                ULONG_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                ULONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                ULONG_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                ULONG_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                ULONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        ULONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
ULONGLONG_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_ulonglong);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            ULONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                ULONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                ULONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                ULONGLONG_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                ULONGLONG_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                ULONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                ULONGLONG_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                ULONGLONG_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                ULONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        ULONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
BYTE_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_byte);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            BYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                BYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                BYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                BYTE_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                BYTE_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                BYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                BYTE_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                BYTE_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                BYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        BYTE_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
SHORT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_short);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            SHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                SHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                SHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                SHORT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                SHORT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                SHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                SHORT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                SHORT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                SHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        SHORT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
INT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_int);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            INT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                INT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                INT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                INT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                INT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                INT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                INT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                INT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                INT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        INT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
LONG_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_long);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            LONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                LONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                LONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                LONG_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                LONG_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                LONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                LONG_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                LONG_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                LONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        LONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
LONGLONG_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_longlong);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            LONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                LONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                LONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                LONGLONG_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                LONGLONG_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                LONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                LONGLONG_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                LONGLONG_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                LONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        LONGLONG_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
BOOL_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_bool);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            BOOL_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                BOOL_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                BOOL_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                BOOL_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                BOOL_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                BOOL_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                BOOL_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                BOOL_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                BOOL_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        BOOL_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}


#line 401


NPY_NO_EXPORT void
OBJECT_matmul(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp dOuter = *dimensions++;
    npy_intp iOuter;
    npy_intp s0 = *steps++;
    npy_intp s1 = *steps++;
    npy_intp s2 = *steps++;
    npy_intp dm = dimensions[0];
    npy_intp dn = dimensions[1];
    npy_intp dp = dimensions[2];
    npy_intp is1_m=steps[0], is1_n=steps[1], is2_n=steps[2], is2_p=steps[3],
         os_m=steps[4], os_p=steps[5];
#if 0 && defined(HAVE_CBLAS)
    npy_intp sz = sizeof(npy_object);
    npy_bool special_case = (dm == 1 || dn == 1 || dp == 1);
    npy_bool any_zero_dim = (dm == 0 || dn == 0 || dp == 0);
    npy_bool scalar_out = (dm == 1 && dp == 1);
    npy_bool scalar_vec = (dn == 1 && (dp == 1 || dm == 1));
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE ||
                                 dp > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sz);
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_p, dn, dp, sz);
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sz);
    npy_bool i2_f_blasable = is_blasable2d(is2_p, is2_n, dp, dn, sz);
    npy_bool i1blasable = i1_c_blasable || i1_f_blasable;
    npy_bool i2blasable = i2_c_blasable || i2_f_blasable;
    npy_bool o_c_blasable = is_blasable2d(os_m, os_p, dm, dp, sz);
    npy_bool o_f_blasable = is_blasable2d(os_p, os_m, dp, dm, sz);
    npy_bool vector_matrix = ((dm == 1) && i2blasable &&
                              is_blasable2d(is1_n, sz, dn, 1, sz));
    npy_bool matrix_vector = ((dp == 1)  && i1blasable &&
                              is_blasable2d(is2_n, sz, dn, 1, sz));
#endif

    for (iOuter = 0; iOuter < dOuter; iOuter++,
                         args[0] += s0, args[1] += s1, args[2] += s2) {
        void *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        /*
         * TODO: refactor this out to a inner_loop_selector, in
         * PyUFunc_MatmulLoopSelector. But that call does not have access to
         * n, m, p and strides.
         */
        if (too_big_for_blas || any_zero_dim) {
            OBJECT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                       ip2, is2_n, is2_p,
                                       op, os_m, os_p, dm, dn, dp);
        }
        else if (special_case) {
            /* Special case variants that have a 1 in the core dimensions */
            if (scalar_out) {
                /* row @ column, 1,1 output */
                OBJECT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
            } else if (scalar_vec){
                /*
                 * 1,1d @ vector or vector @ 1,1d
                 * could use cblas_Xaxy, but that requires 0ing output
                 * and would not be faster (XXX prove it)
                 */
                OBJECT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            } else if (vector_matrix) {
                /* vector @ matrix, switch ip1, ip2, p and m */
                OBJECT_gemv(ip2, is2_p, is2_n, ip1, is1_n,
                            op, os_p, dp, dn);
            } else if  (matrix_vector) {
                /* matrix @ vector */
                OBJECT_gemv(ip1, is1_m, is1_n, ip2, is2_n,
                            op, os_m, dm, dn);
            } else {
                /* column @ row, 2d output, no blas needed or non-blas-able input */
                OBJECT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        } else {
            /* matrix @ matrix */
            if (i1blasable && i2blasable && o_c_blasable) {
                OBJECT_matmul_matrixmatrix(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p,
                                           dm, dn, dp);
            } else if (i1blasable && i2blasable && o_f_blasable) {
                /*
                 * Use transpose equivalence:
                 * matmul(a, b, o) == matmul(b.T, a.T, o.T)
                 */
                OBJECT_matmul_matrixmatrix(ip2, is2_p, is2_n,
                                           ip1, is1_n, is1_m,
                                           op, os_p, os_m,
                                           dp, dn, dm);
            } else {
                /*
                 * If parameters are castable to int and we copy the
                 * non-blasable (or non-ccontiguous output)
                 * we could still use BLAS, see gh-12365.
                 */
                OBJECT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                           ip2, is2_n, is2_p,
                                           op, os_m, os_p, dm, dn, dp);
            }
        }
#else
        OBJECT_matmul_inner_noblas(ip1, is1_m, is1_n,
                                   ip2, is2_n, is2_p,
                                   op, os_m, os_p, dm, dn, dp);

#endif
    }
}




/*
 * vecdot loops
 * signature is (n),(n)->()
 * We need to redefine complex and object dot from arraytypes.c.src to ensure
 * we take the complex conjugate of the first argument (for cblas, this means
 * using the dotc functions instead of dotu).
 */

#line 536
NPY_NO_EXPORT void
CFLOAT_dotc(char *ip1, npy_intp is1, char *ip2, npy_intp is2,
            char *op, npy_intp n, void *NPY_UNUSED(ignore))
{
#if defined(HAVE_CBLAS) && 1
    CBLAS_INT is1b = blas_stride(is1, sizeof(npy_cfloat));
    CBLAS_INT is2b = blas_stride(is2, sizeof(npy_cfloat));

    if (is1b && is2b) {
        npy_double sum[2] = {0., 0.};  /* at least double for stability */

        while (n > 0) {
            CBLAS_INT chunk = n < NPY_CBLAS_CHUNK ? n : NPY_CBLAS_CHUNK;
            npy_float tmp[2];

            CBLAS_FUNC(cblas_cdotc_sub)(
                    (CBLAS_INT)chunk, ip1, is1b, ip2, is2b, tmp);
            sum[0] += (npy_double)tmp[0];
            sum[1] += (npy_double)tmp[1];
            /* use char strides here */
            ip1 += chunk * is1;
            ip2 += chunk * is2;
            n -= chunk;
        }
        ((npy_float *)op)[0] = (npy_float)sum[0];
        ((npy_float *)op)[1] = (npy_float)sum[1];
    }
    else
#endif
    {
        npy_double sumr = 0., sumi = 0.;  /* at least double for stability */
        for (npy_intp i = 0; i < n; i++, ip1 += is1, ip2 += is2) {
            const npy_float ip1r = ((npy_float *)ip1)[0];
            const npy_float ip1i = ((npy_float *)ip1)[1];
            const npy_float ip2r = ((npy_float *)ip2)[0];
            const npy_float ip2i = ((npy_float *)ip2)[1];
            /* ip1.conj() * ip2 */
            sumr += (npy_double)(ip1r * ip2r + ip1i * ip2i);
            sumi += (npy_double)(ip1r * ip2i - ip1i * ip2r);
        }
        ((npy_float *)op)[0] = (npy_float)sumr;
        ((npy_float *)op)[1] = (npy_float)sumi;
    }
}

#line 536
NPY_NO_EXPORT void
CDOUBLE_dotc(char *ip1, npy_intp is1, char *ip2, npy_intp is2,
            char *op, npy_intp n, void *NPY_UNUSED(ignore))
{
#if defined(HAVE_CBLAS) && 1
    CBLAS_INT is1b = blas_stride(is1, sizeof(npy_cdouble));
    CBLAS_INT is2b = blas_stride(is2, sizeof(npy_cdouble));

    if (is1b && is2b) {
        npy_double sum[2] = {0., 0.};  /* at least double for stability */

        while (n > 0) {
            CBLAS_INT chunk = n < NPY_CBLAS_CHUNK ? n : NPY_CBLAS_CHUNK;
            npy_double tmp[2];

            CBLAS_FUNC(cblas_zdotc_sub)(
                    (CBLAS_INT)chunk, ip1, is1b, ip2, is2b, tmp);
            sum[0] += (npy_double)tmp[0];
            sum[1] += (npy_double)tmp[1];
            /* use char strides here */
            ip1 += chunk * is1;
            ip2 += chunk * is2;
            n -= chunk;
        }
        ((npy_double *)op)[0] = (npy_double)sum[0];
        ((npy_double *)op)[1] = (npy_double)sum[1];
    }
    else
#endif
    {
        npy_double sumr = 0., sumi = 0.;  /* at least double for stability */
        for (npy_intp i = 0; i < n; i++, ip1 += is1, ip2 += is2) {
            const npy_double ip1r = ((npy_double *)ip1)[0];
            const npy_double ip1i = ((npy_double *)ip1)[1];
            const npy_double ip2r = ((npy_double *)ip2)[0];
            const npy_double ip2i = ((npy_double *)ip2)[1];
            /* ip1.conj() * ip2 */
            sumr += (npy_double)(ip1r * ip2r + ip1i * ip2i);
            sumi += (npy_double)(ip1r * ip2i - ip1i * ip2r);
        }
        ((npy_double *)op)[0] = (npy_double)sumr;
        ((npy_double *)op)[1] = (npy_double)sumi;
    }
}

#line 536
NPY_NO_EXPORT void
CLONGDOUBLE_dotc(char *ip1, npy_intp is1, char *ip2, npy_intp is2,
            char *op, npy_intp n, void *NPY_UNUSED(ignore))
{
#if defined(HAVE_CBLAS) && 0
    CBLAS_INT is1b = blas_stride(is1, sizeof(npy_clongdouble));
    CBLAS_INT is2b = blas_stride(is2, sizeof(npy_clongdouble));

    if (is1b && is2b) {
        npy_longdouble sum[2] = {0., 0.};  /* at least double for stability */

        while (n > 0) {
            CBLAS_INT chunk = n < NPY_CBLAS_CHUNK ? n : NPY_CBLAS_CHUNK;
            npy_longdouble tmp[2];

            CBLAS_FUNC(cblas_0dotc_sub)(
                    (CBLAS_INT)chunk, ip1, is1b, ip2, is2b, tmp);
            sum[0] += (npy_longdouble)tmp[0];
            sum[1] += (npy_longdouble)tmp[1];
            /* use char strides here */
            ip1 += chunk * is1;
            ip2 += chunk * is2;
            n -= chunk;
        }
        ((npy_longdouble *)op)[0] = (npy_longdouble)sum[0];
        ((npy_longdouble *)op)[1] = (npy_longdouble)sum[1];
    }
    else
#endif
    {
        npy_longdouble sumr = 0., sumi = 0.;  /* at least double for stability */
        for (npy_intp i = 0; i < n; i++, ip1 += is1, ip2 += is2) {
            const npy_longdouble ip1r = ((npy_longdouble *)ip1)[0];
            const npy_longdouble ip1i = ((npy_longdouble *)ip1)[1];
            const npy_longdouble ip2r = ((npy_longdouble *)ip2)[0];
            const npy_longdouble ip2i = ((npy_longdouble *)ip2)[1];
            /* ip1.conj() * ip2 */
            sumr += (npy_longdouble)(ip1r * ip2r + ip1i * ip2i);
            sumi += (npy_longdouble)(ip1r * ip2i - ip1i * ip2r);
        }
        ((npy_longdouble *)op)[0] = (npy_longdouble)sumr;
        ((npy_longdouble *)op)[1] = (npy_longdouble)sumi;
    }
}


static void
OBJECT_dotc(char *ip1, npy_intp is1, char *ip2, npy_intp is2, char *op, npy_intp n,
            void *NPY_UNUSED(ignore))
{
    npy_intp i;
    PyObject *result = NULL;

    for (i = 0; i < n; i++, ip1 += is1, ip2 += is2) {
        PyObject *obj1 = *(PyObject**)ip1, *obj2 = *(PyObject**)ip2;
        if (obj1 == NULL) {
            obj1 = Py_None;  /* raise useful error message at conjugate */
        }
        if (obj2 == NULL) {
            obj2 = Py_None;  /* raise at multiply (unless obj1 handles None) */
        }
        PyObject *obj1_conj = PyObject_CallMethod(obj1, "conjugate", NULL);
        if (obj1_conj == NULL) {
            goto fail;
        }
        PyObject *prod = PyNumber_Multiply(obj1_conj, obj2);
        Py_DECREF(obj1_conj);
        if (prod == NULL) {
            goto fail;
        }
        if (i == 0) {
            result = prod;
        }
        else {
            Py_SETREF(result, PyNumber_Add(result, prod));
            Py_DECREF(prod);
            if (result == NULL) {
                goto fail;
            }
        }
    }
    Py_XSETREF(*((PyObject **)op), result);
    return;

  fail:
    Py_XDECREF(result);
    return;
}

#line 633
NPY_NO_EXPORT void
FLOAT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        FLOAT_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
DOUBLE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        DOUBLE_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
LONGDOUBLE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        LONGDOUBLE_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
HALF_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        HALF_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
UBYTE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        UBYTE_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
USHORT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        USHORT_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
UINT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        UINT_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
ULONG_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        ULONG_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
ULONGLONG_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        ULONGLONG_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
BYTE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        BYTE_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
SHORT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        SHORT_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
INT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        INT_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
LONG_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        LONG_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
LONGLONG_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        LONGLONG_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
BOOL_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        BOOL_dot(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
CFLOAT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        CFLOAT_dotc(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
CDOUBLE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        CDOUBLE_dotc(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
CLONGDOUBLE_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        CLONGDOUBLE_dotc(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 0
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}

#line 633
NPY_NO_EXPORT void
OBJECT_vecdot(char **args, npy_intp const *dimensions, npy_intp const *steps,
              void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp n_inner = dimensions[1];
    npy_intp is1=steps[3], is2=steps[4];

    for (npy_intp i = 0; i < n_outer; i++, args[0] += s0, args[1] += s1, args[2] += s2) {
        /*
         * TODO: use new API to select inner loop with get_loop and
         * improve error treatment.
         */
        char *ip1=args[0], *ip2=args[1], *op=args[2];
        OBJECT_dotc(ip1, is1, ip2, is2, op, n_inner, NULL);
#if 1
        if (PyErr_Occurred()) {
            return;
        }
#endif
    }
}


#if defined(HAVE_CBLAS)
/*
 * Blas complex vector-matrix product via gemm (gemv cannot conjugate the vector).
 */
#line 670
NPY_NO_EXPORT void
CFLOAT_vecmat_via_gemm(void *ip1, npy_intp is1_n,
                       void *ip2, npy_intp is2_n, npy_intp is2_m,
                       void *op, npy_intp os_m,
                       npy_intp n, npy_intp m)
{
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT N, M, lda, ldb, ldc;
    assert(n <= BLAS_MAXSIZE && m <= BLAS_MAXSIZE);
    N = (CBLAS_INT)n;
    M = (CBLAS_INT)m;

    assert(os_m == sizeof(npy_cfloat));
    ldc = (CBLAS_INT)m;

    assert(is_blasable2d(is1_n, sizeof(npy_cfloat), n, 1, sizeof(npy_cfloat)));
    trans1 = CblasConjTrans;
    lda = (CBLAS_INT)(is1_n / sizeof(npy_cfloat));

    if (is_blasable2d(is2_n, is2_m, n, m, sizeof(npy_cfloat))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_cfloat));
    }
    else {
        assert(is_blasable2d(is2_m, is2_n, m, n, sizeof(npy_cfloat)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_m / sizeof(npy_cfloat));
    }
    CBLAS_FUNC(cblas_cgemm)(
        order, trans1, trans2, 1, M, N, &oneF, ip1, lda,
        ip2, ldb, &zeroF, op, ldc);
}

#line 670
NPY_NO_EXPORT void
CDOUBLE_vecmat_via_gemm(void *ip1, npy_intp is1_n,
                       void *ip2, npy_intp is2_n, npy_intp is2_m,
                       void *op, npy_intp os_m,
                       npy_intp n, npy_intp m)
{
    enum CBLAS_ORDER order = CblasRowMajor;
    enum CBLAS_TRANSPOSE trans1, trans2;
    CBLAS_INT N, M, lda, ldb, ldc;
    assert(n <= BLAS_MAXSIZE && m <= BLAS_MAXSIZE);
    N = (CBLAS_INT)n;
    M = (CBLAS_INT)m;

    assert(os_m == sizeof(npy_cdouble));
    ldc = (CBLAS_INT)m;

    assert(is_blasable2d(is1_n, sizeof(npy_cdouble), n, 1, sizeof(npy_cdouble)));
    trans1 = CblasConjTrans;
    lda = (CBLAS_INT)(is1_n / sizeof(npy_cdouble));

    if (is_blasable2d(is2_n, is2_m, n, m, sizeof(npy_cdouble))) {
        trans2 = CblasNoTrans;
        ldb = (CBLAS_INT)(is2_n / sizeof(npy_cdouble));
    }
    else {
        assert(is_blasable2d(is2_m, is2_n, m, n, sizeof(npy_cdouble)));
        trans2 = CblasTrans;
        ldb = (CBLAS_INT)(is2_m / sizeof(npy_cdouble));
    }
    CBLAS_FUNC(cblas_zgemm)(
        order, trans1, trans2, 1, M, N, &oneD, ip1, lda,
        ip2, ldb, &zeroD, op, ldc);
}

#endif

/*
 * matvec loops, using blas gemv if possible, and TYPE_dot implementations otherwise.
 * signature is (m,n),(n)->(m)
 */
#line 724
NPY_NO_EXPORT void
FLOAT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_float));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_float));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_float), dn, 1, sizeof(npy_float));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
            FLOAT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            FLOAT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
DOUBLE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_double));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_double));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_double), dn, 1, sizeof(npy_double));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
            DOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            DOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
LONGDOUBLE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_longdouble));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_longdouble));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_longdouble), dn, 1, sizeof(npy_longdouble));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            LONGDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            LONGDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
HALF_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_half));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_half));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_half), dn, 1, sizeof(npy_half));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            HALF_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            HALF_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
CFLOAT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_cfloat));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_cfloat));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_cfloat), dn, 1, sizeof(npy_cfloat));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
            CFLOAT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            CFLOAT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
CDOUBLE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_cdouble));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_cdouble));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_cdouble), dn, 1, sizeof(npy_cdouble));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
            CDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            CDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
CLONGDOUBLE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_clongdouble));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_clongdouble));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_clongdouble), dn, 1, sizeof(npy_clongdouble));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            CLONGDOUBLE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            CLONGDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
UBYTE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_ubyte));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_ubyte));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_ubyte), dn, 1, sizeof(npy_ubyte));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            UBYTE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            UBYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
USHORT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_ushort));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_ushort));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_ushort), dn, 1, sizeof(npy_ushort));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            USHORT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            USHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
UINT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_uint));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_uint));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_uint), dn, 1, sizeof(npy_uint));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            UINT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            UINT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
ULONG_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_ulong));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_ulong));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_ulong), dn, 1, sizeof(npy_ulong));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            ULONG_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            ULONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
ULONGLONG_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_ulonglong));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_ulonglong));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_ulonglong), dn, 1, sizeof(npy_ulonglong));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            ULONGLONG_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            ULONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
BYTE_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_byte));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_byte));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_byte), dn, 1, sizeof(npy_byte));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            BYTE_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            BYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
SHORT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_short));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_short));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_short), dn, 1, sizeof(npy_short));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            SHORT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            SHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
INT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_int));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_int));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_int), dn, 1, sizeof(npy_int));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            INT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            INT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
LONG_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_long));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_long));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_long), dn, 1, sizeof(npy_long));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            LONG_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            LONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
LONGLONG_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_longlong));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_longlong));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_longlong), dn, 1, sizeof(npy_longlong));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            LONGLONG_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            LONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
BOOL_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_bool));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_bool));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_bool), dn, 1, sizeof(npy_bool));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            BOOL_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            BOOL_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 724
NPY_NO_EXPORT void
OBJECT_matvec(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dm = dimensions[1], dn = dimensions[2];
    npy_intp is1_m=steps[3], is1_n=steps[4], is2_n=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_c_blasable = is_blasable2d(is1_m, is1_n, dm, dn, sizeof(npy_object));
    npy_bool i1_f_blasable = is_blasable2d(is1_n, is1_m, dn, dm, sizeof(npy_object));
    npy_bool i2_blasable = is_blasable2d(is2_n, sizeof(npy_object), dn, 1, sizeof(npy_object));
    npy_bool blasable = ((i1_c_blasable || i1_f_blasable) && i2_blasable
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
            OBJECT_gemv(ip1, is1_m, is1_n, ip2, is2_n, op, os_m, dm, dn);
            continue;
        }
#endif
        /*
         * Dot the different matrix rows with the vector to get output elements.
         * (no conjugation for complex, unlike vecdot and vecmat)
         */
        for (npy_intp j = 0; j < dm; j++, ip1 += is1_m, op += os_m) {
            OBJECT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 1
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}


/*
 * vecmat loops, using blas gemv for float and gemm for complex if possible,
 * and TYPE_dot[c] implementations otherwise.
 * Note that we cannot use gemv for complex, since we need to conjugate the vector.
 * signature is (n),(n,m)->(m)
 */
#line 786
NPY_NO_EXPORT void
FLOAT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_float), dn, 1, sizeof(npy_float));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_float));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_float));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            FLOAT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            FLOAT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            FLOAT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
DOUBLE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_double), dn, 1, sizeof(npy_double));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_double));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_double));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            DOUBLE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            DOUBLE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            DOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
LONGDOUBLE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_longdouble), dn, 1, sizeof(npy_longdouble));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_longdouble));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_longdouble));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            LONGDOUBLE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            LONGDOUBLE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            LONGDOUBLE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
HALF_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_half), dn, 1, sizeof(npy_half));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_half));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_half));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            HALF_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            HALF_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            HALF_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
CFLOAT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_cfloat), dn, 1, sizeof(npy_cfloat));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_cfloat));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_cfloat));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
#if 1
            /* For complex, use gemm so we can conjugate the vector */
            CFLOAT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            CFLOAT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            CFLOAT_dotc(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
CDOUBLE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 1 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_cdouble), dn, 1, sizeof(npy_cdouble));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_cdouble));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_cdouble));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 1 && defined(HAVE_CBLAS)
        if (blasable) {
#if 1
            /* For complex, use gemm so we can conjugate the vector */
            CDOUBLE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            CDOUBLE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            CDOUBLE_dotc(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
CLONGDOUBLE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_clongdouble), dn, 1, sizeof(npy_clongdouble));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_clongdouble));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_clongdouble));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 1
            /* For complex, use gemm so we can conjugate the vector */
            CLONGDOUBLE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            CLONGDOUBLE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            CLONGDOUBLE_dotc(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
UBYTE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_ubyte), dn, 1, sizeof(npy_ubyte));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_ubyte));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_ubyte));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            UBYTE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            UBYTE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            UBYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
USHORT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_ushort), dn, 1, sizeof(npy_ushort));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_ushort));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_ushort));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            USHORT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            USHORT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            USHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
UINT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_uint), dn, 1, sizeof(npy_uint));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_uint));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_uint));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            UINT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            UINT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            UINT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
ULONG_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_ulong), dn, 1, sizeof(npy_ulong));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_ulong));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_ulong));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            ULONG_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            ULONG_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            ULONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
ULONGLONG_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_ulonglong), dn, 1, sizeof(npy_ulonglong));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_ulonglong));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_ulonglong));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            ULONGLONG_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            ULONGLONG_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            ULONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
BYTE_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_byte), dn, 1, sizeof(npy_byte));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_byte));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_byte));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            BYTE_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            BYTE_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            BYTE_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
SHORT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_short), dn, 1, sizeof(npy_short));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_short));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_short));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            SHORT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            SHORT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            SHORT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
INT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_int), dn, 1, sizeof(npy_int));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_int));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_int));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            INT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            INT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            INT_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
LONG_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_long), dn, 1, sizeof(npy_long));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_long));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_long));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            LONG_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            LONG_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            LONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
LONGLONG_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_longlong), dn, 1, sizeof(npy_longlong));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_longlong));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_longlong));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            LONGLONG_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            LONGLONG_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            LONGLONG_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
BOOL_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_bool), dn, 1, sizeof(npy_bool));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_bool));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_bool));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 0
            /* For complex, use gemm so we can conjugate the vector */
            BOOL_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            BOOL_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            BOOL_dot(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 0
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}

#line 786
NPY_NO_EXPORT void
OBJECT_vecmat(char **args, npy_intp const *dimensions, npy_intp const *steps, void *NPY_UNUSED(func))
{
    npy_intp n_outer = dimensions[0];
    npy_intp s0=steps[0], s1=steps[1], s2=steps[2];
    npy_intp dn = dimensions[1], dm = dimensions[2];
    npy_intp is1_n=steps[3], is2_n=steps[4], is2_m=steps[5], os_m=steps[6];
#if 0 && defined(HAVE_CBLAS)
    npy_bool too_big_for_blas = (dm > BLAS_MAXSIZE || dn > BLAS_MAXSIZE);
    npy_bool i1_blasable = is_blasable2d(is1_n, sizeof(npy_object), dn, 1, sizeof(npy_object));
    npy_bool i2_c_blasable = is_blasable2d(is2_n, is2_m, dn, dm, sizeof(npy_object));
    npy_bool i2_f_blasable = is_blasable2d(is2_m, is2_n, dm, dn, sizeof(npy_object));
    npy_bool blasable = (i1_blasable && (i2_c_blasable || i2_f_blasable)
                         && !too_big_for_blas && dn > 1 && dm > 1);
#endif
    for (npy_intp i = 0; i < n_outer; i++,
             args[0] += s0, args[1] += s1, args[2] += s2) {
        char *ip1=args[0], *ip2=args[1], *op=args[2];
#if 0 && defined(HAVE_CBLAS)
        if (blasable) {
#if 1
            /* For complex, use gemm so we can conjugate the vector */
            OBJECT_vecmat_via_gemm(ip1, is1_n, ip2, is2_n, is2_m, op, os_m, dn, dm);
#else
            /* For float, use gemv (hence flipped order) */
            OBJECT_gemv(ip2, is2_m, is2_n, ip1, is1_n, op, os_m, dm, dn);
#endif
            continue;
        }
#endif
        /* Dot the vector with different matrix columns to get output elements. */
        for (npy_intp j = 0; j < dm; j++, ip2 += is2_m, op += os_m) {
            OBJECT_dotc(ip1, is1_n, ip2, is2_n, op, dn, NULL);
#if 1
            if (PyErr_Occurred()) {
                return;
            }
#endif
        }
    }
}


