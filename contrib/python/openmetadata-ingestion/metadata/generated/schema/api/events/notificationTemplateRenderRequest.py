# generated by datamodel-codegen:
#   filename:  api/events/notificationTemplateRenderRequest.json
#   timestamp: 2026-02-24T11:26:02+00:00

from __future__ import annotations

from typing import Optional

from pydantic import ConfigDict, Field
from typing_extensions import Annotated

from metadata.ingestion.models.custom_pydantic import BaseModel

from ...type import changeEventType


class NotificationTemplateRenderRequest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    templateSubject: Annotated[
        str,
        Field(
            description='Handlebars template for notification subject line',
            max_length=255,
            min_length=1,
        ),
    ]
    templateBody: Annotated[
        str,
        Field(
            description='Handlebars template content for rendering notifications',
            max_length=10240,
            min_length=1,
        ),
    ]
    resource: Annotated[
        str,
        Field(
            description="Entity type to mock (e.g., 'table', 'dashboard', 'task'). Falls back to generic fixture if not found.",
            max_length=128,
            min_length=1,
        ),
    ]
    eventType: Annotated[
        Optional[changeEventType.EventType],
        Field(
            None,
            description="Optional. Specific event type to test (e.g., 'entityUpdated'). When omitted, generates a neutral ChangeEvent with no event-specific fields.",
        ),
    ]
