# Generated by devtools/yamaker/ym2

PY3_LIBRARY()

LICENSE(
    Apache-2.0 AND
    LicenseRef-scancode-unknown-license-reference AND
    MIT
)

LICENSE_TEXTS(.yandex_meta/licenses.list.txt)

VERSION(1.12.0)

ORIGINAL_SOURCE(https://github.com/aviramha/ormsgpack/archive/refs/tags/1.12.0.tar.gz)

IF (FALSE)
ELSEIF (OS_DARWIN AND ARCH_ARM64)
    COPY_FILE(a/aarch64-apple-darwin/release/libormsgpack.a libormsgpack.a)
ELSEIF (OS_DARWIN AND ARCH_X86_64)
    COPY_FILE(a/x86_64-apple-darwin/release/libormsgpack.a libormsgpack.a)
ELSEIF (OS_LINUX AND ARCH_AARCH64)
    COPY_FILE(a/aarch64-unknown-linux-gnu/release/libormsgpack.a libormsgpack.a)
ELSEIF (OS_LINUX AND ARCH_X86_64 AND MUSL)
    COPY_FILE(a/x86_64-unknown-linux-musl/release/libormsgpack.a libormsgpack.a)
ELSEIF (OS_LINUX AND ARCH_X86_64 AND NOT MUSL)
    COPY_FILE(a/x86_64-unknown-linux-gnu/release/libormsgpack.a libormsgpack.a)
ELSE()
    MESSAGE(FATAL_ERROR "We don't have ormsgpack for this platform")
ENDIF()

PY_REGISTER(
    ormsgpack
)

NO_COMPILER_WARNINGS()

NO_UTIL()

PY_SRCS(
    ormsgpack/__init__.pyi
)

END()
