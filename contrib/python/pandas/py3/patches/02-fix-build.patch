--- contrib/python/pandas/py3/pandas/_libs/include/pandas/datetime/pd_datetime.h	(index)
+++ contrib/python/pandas/py3/pandas/_libs/include/pandas/datetime/pd_datetime.h	(working tree)
@@ -72 +72 @@ static PandasDateTime_CAPI *PandasDateTimeAPI = NULL;
-  PandasDateTimeAPI->npy_datetimestruct_to_datetime((NPY_DATETIMEUNIT),        \
+  PandasDateTimeAPI->_pandas_npy_datetimestruct_to_datetime((NPY_DATETIMEUNIT),        \
@@ -93 +93 @@ static PandasDateTime_CAPI *PandasDateTimeAPI = NULL;
-  PandasDateTimeAPI->cmp_npy_datetimestruct((a), (b))
+  PandasDateTimeAPI->_pandas_cmp_npy_datetimestruct((a), (b))
@@ -95 +95 @@ static PandasDateTime_CAPI *PandasDateTimeAPI = NULL;
-  PandasDateTimeAPI->get_datetime_metadata_from_dtype((dtype))
+  PandasDateTimeAPI->_pandas_get_datetime_metadata_from_dtype((dtype))
@@ -99 +99 @@ static PandasDateTime_CAPI *PandasDateTimeAPI = NULL;
-  PandasDateTimeAPI->parse_iso_8601_datetime(                                  \
+  PandasDateTimeAPI->_pandas_parse_iso_8601_datetime(                                  \
--- contrib/python/pandas/py3/pandas/_libs/include/pandas/parser/pd_parser.h	(index)
+++ contrib/python/pandas/py3/pandas/_libs/include/pandas/parser/pd_parser.h	(working tree)
@@ -62,3 +62,3 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->floatify((str), (result), (maybe_int))
-#define new_rd_source(obj) PandasParserAPI->new_rd_source((obj))
-#define del_rd_source(src) PandasParserAPI->del_rd_source((src))
+  PandasParserAPI->_pandas_floatify((str), (result), (maybe_int))
+#define new_rd_source(obj) PandasParserAPI->_pandas_new_rd_source((obj))
+#define del_rd_source(src) PandasParserAPI->_pandas_del_rd_source((src))
@@ -66 +66 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->buffer_rd_bytes((source), (nbytes), (bytes_read), (status), \
+  PandasParserAPI->_pandas_buffer_rd_bytes((source), (nbytes), (bytes_read), (status), \
@@ -68,2 +68,2 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-#define uint_state_init(self) PandasParserAPI->uint_state_init((self))
-#define uint64_conflict(self) PandasParserAPI->uint64_conflict((self))
+#define uint_state_init(self) PandasParserAPI->_pandas_uint_state_init((self))
+#define uint64_conflict(self) PandasParserAPI->_pandas_uint64_conflict((self))
@@ -71,5 +71,5 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->coliter_setup((self), (parser), (i), (start))
-#define parser_new PandasParserAPI->parser_new
-#define parser_init(self) PandasParserAPI->parser_init((self))
-#define parser_free(self) PandasParserAPI->parser_free((self))
-#define parser_del(self) PandasParserAPI->parser_del((self))
+  PandasParserAPI->_pandas_coliter_setup((self), (parser), (i), (start))
+#define parser_new PandasParserAPI->_pandas_parser_new
+#define parser_init(self) PandasParserAPI->_pandas_parser_init((self))
+#define parser_free(self) PandasParserAPI->_pandas_parser_free((self))
+#define parser_del(self) PandasParserAPI->_pandas_parser_del((self))
@@ -77 +77 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->parser_add_skiprow((self), (row))
+  PandasParserAPI->_pandas_parser_add_skiprow((self), (row))
@@ -79 +79 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->parser_set_skipfirstnrows((self), (nrows))
+  PandasParserAPI->_pandas_parser_set_skipfirstnrows((self), (nrows))
@@ -81 +81 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->parser_set_default_options((self))
+  PandasParserAPI->_pandas_parser_set_default_options((self))
@@ -83 +83 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->parser_consume_rows((self), (nrows))
+  PandasParserAPI->_pandas_parser_consume_rows((self), (nrows))
@@ -85 +85 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-#define parser_trim_buffers(self) PandasParserAPI->parser_trim_buffers((self))
+#define parser_trim_buffers(self) PandasParserAPI->_pandas_parser_trim_buffers((self))
@@ -87 +87 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->tokenize_all_rows((self), (encoding_errors))
+  PandasParserAPI->_pandas_tokenize_all_rows((self), (encoding_errors))
@@ -89 +89 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->tokenize_nrows((self), (nrows), (encoding_errors))
+  PandasParserAPI->_pandas_tokenize_nrows((self), (nrows), (encoding_errors))
@@ -91 +91 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->str_to_int64((p_item), (int_min), (int_max), (error),       \
+  PandasParserAPI->_pandas_str_to_int64((p_item), (int_min), (int_max), (error),       \
@@ -94 +94 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->str_to_uint64((state), (p_item), (int_max), (uint_max),     \
+  PandasParserAPI->_pandas_str_to_uint64((state), (p_item), (int_max), (uint_max),     \
@@ -97 +97 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->xstrtod((p), (q), (decimal), (sci), (tsep),                 \
+  PandasParserAPI->_pandas_xstrtod((p), (q), (decimal), (sci), (tsep),                 \
@@ -101 +101 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->precise_xstrtod((p), (q), (decimal), (sci), (tsep),         \
+  PandasParserAPI->_pandas_precise_xstrtod((p), (q), (decimal), (sci), (tsep),         \
@@ -104 +104 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-  PandasParserAPI->round_trip((p), (q), (decimal), (sci), (tsep),              \
+  PandasParserAPI->_pandas_round_trip((p), (q), (decimal), (sci), (tsep),              \
@@ -106 +106 @@ static PandasParser_CAPI *PandasParserAPI = NULL;
-#define to_boolean(item, val) PandasParserAPI->to_boolean((item), (val))
+#define to_boolean(item, val) PandasParserAPI->_pandas_to_boolean((item), (val))
