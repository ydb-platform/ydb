--- contrib/python/panel/panel/io/notebook.py	(index)
+++ contrib/python/panel/panel/io/notebook.py	(working tree)
@@ -6,6 +6,7 @@ from __future__ import annotations
 
 import json
 import os
+import pkgutil
 import sys
 import uuid
 
@@ -390,1 +391,1 @@ def load_notebook(
-    CSS = (PANEL_DIR / '_templates' / 'jupyter.css').read_text(encoding='utf-8')
+    CSS = pkgutil.get_data('panel', '_templates/jupyter.css').decode()
--- contrib/python/panel/panel/io/resources.py	(index)
+++ contrib/python/panel/panel/io/resources.py	(working tree)
@@ -10,6 +10,7 @@ import logging
 import mimetypes
 import os
 import pathlib
+import pkgutil
 import re
 import textwrap
 
@@ -33,1 +34,1 @@ from bokeh.models import ImportedStyleSheet
-from jinja2.loaders import FileSystemLoader
+from jinja2.loaders import PackageLoader, ChoiceLoader
@@ -54,3 +55,3 @@ logger = logging.getLogger(__name__)
-with open(Path(__file__).parent.parent / 'package.json') as f:
-    package_json = json.load(f)
-    JS_VERSION = package_json['version'].split('+')[0]
+package_json_data = pkgutil.get_data('panel', 'package.json')
+package_json = json.loads(package_json_data)
+JS_VERSION = package_json['version'].split('+')[0]
@@ -59,3 +59,1 @@ logger = logging.getLogger(__name__)
-    return Environment(loader=FileSystemLoader([
-        str(internal_path.resolve()), str(template_path.resolve())
-    ]))
+    return Environment(loader=ChoiceLoader([PackageLoader('panel', '_templates'), PackageLoader('panel', 'templates')]))
@@ -660,1 +660,1 @@ class Resources(BkResources):
-                (DIST_DIR / css.replace(CDN_DIST, '')).read_text(encoding='utf-8')
+                pkgutil.get_data('panel', f"dist/{css.replace(CDN_DIST, '')}").decode()
@@ -674,1 +674,1 @@ class Resources(BkResources):
-            loading_base = (DIST_DIR / "css" / "loading.css").read_text(encoding='utf-8').replace(
+            loading_base = pkgutil.get_data('panel', 'dist/css/loading.css').decode().replace(
@@ -756,1 +756,1 @@ class Resources(BkResources):
-            (DIST_DIR / js.replace(CDN_DIST, '')).read_text(encoding='utf-8')
+            pkgutil.get_data('panel', f"dist/{js.replace(CDN_DIST, '')}").decode()
@@ -773,1 +773,1 @@ class Resources(BkResources):
-                (DIST_DIR / js.replace(CDN_DIST, '')).read_text(encoding='utf-8')
+                pkgutil.get_data('panel', f"dist/{js.replace(CDN_DIST, '')}").decode()
--- contrib/python/panel/panel/layout/flex.py	(index)
+++ contrib/python/panel/panel/layout/flex.py	(working tree)
@@ -1,3 +1,4 @@
+import pkgutil
 from pathlib import Path
 
 import param
@@ -54,1 +55,1 @@ class FlexBox(ListLike, ReactiveHTML):
-    _template = (Path(__file__).parent / 'flexbox.html').read_text('utf-8')
+    _template = pkgutil.get_data(__package__, 'flexbox.html').decode()
