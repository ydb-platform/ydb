# Generated by devtools/yamaker/ym2

PY3_LIBRARY()

LICENSE(MIT)

LICENSE_TEXTS(.yandex_meta/licenses.list.txt)

VERSION(3.1.0)

ORIGINAL_SOURCE(https://github.com/python-pendulum/pendulum/archive/refs/tags/3.1.0.tar.gz)

IF (FALSE)
ELSEIF (OS_DARWIN AND ARCH_ARM64)
    COPY_FILE(a/aarch64-apple-darwin/release/lib_pendulum.a lib_pendulum.a)
ELSEIF (OS_DARWIN AND ARCH_X86_64)
    COPY_FILE(a/x86_64-apple-darwin/release/lib_pendulum.a lib_pendulum.a)
ELSEIF (OS_LINUX AND ARCH_X86_64 AND NOT MUSL)
    COPY_FILE(a/x86_64-unknown-linux-gnu/release/lib_pendulum.a lib_pendulum.a)
ELSE()
    MESSAGE(FATAL_ERROR "We don't have _pendulum for this platform")
ENDIF()

PY_REGISTER(
    _pendulum
)

NO_COMPILER_WARNINGS()

NO_UTIL()

END()
