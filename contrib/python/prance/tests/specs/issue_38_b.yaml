openapi: 3.0.0
info:
  title: Minimal test for refs in sequences
  version: 0.0.1
servers:
  - url: https://localhost/
paths:
  /api/v2/vms:
    get:
      operationId: list_vm
      description: get all
