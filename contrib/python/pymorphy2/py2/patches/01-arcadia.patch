--- contrib/python/pymorphy2/py2/pymorphy2/opencorpora_dict/storage.py	(index)
+++ contrib/python/pymorphy2/py2/pymorphy2/opencorpora_dict/storage.py	(working tree)
@@ -8,9 +8,11 @@ import datetime
 import os
 import logging
 import collections
+import io
 import itertools
 import array
 import struct
+import sys
 
 try:
     izip = itertools.izip
@@ -22,6 +24,8 @@ from pymorphy2 import tagset
 from pymorphy2 import dawg
 from pymorphy2.utils import json_write, json_read
 
+from library.python.resource import resfs_read
+
 logger = logging.getLogger(__name__)
 
 CURRENT_FORMAT_VERSION = '2.4'
@@ -67,7 +71,7 @@ def load_dict(path, gramtab_format='opencorpora-int'):
     prediction_suffixes_dawgs = []
     for prefix_id in range(len(paradigm_prefixes)):
         fn = _f('prediction-suffixes-%s.dawg' % prefix_id)
-        assert os.path.exists(fn)
+        #assert os.path.exists(fn)
         prediction_suffixes_dawgs.append(dawg.PredictionSuffixesDAWG().load(fn))
 
     return LoadedDictionary(
@@ -199,16 +203,25 @@ def _load_gramtab(meta, gramtab_format, path):
 def _load_paradigms(filename):
     """ Load paradigms data """
     paradigms = []
-    with open(filename, 'rb') as f:
-        paradigms_count = struct.unpack(str("<H"), f.read(2))[0]
+    data = resfs_read(filename)
+    if data:
+        f = io.BytesIO(data)
+    else:
+        f = open(filename, 'rb')
+
+    paradigms_count = struct.unpack(str("<H"), f.read(2))[0]
+
+    for x in range(paradigms_count):
+        paradigm_len = struct.unpack(str("<H"), f.read(2))[0]
 
-        for x in range(paradigms_count):
-            paradigm_len = struct.unpack(str("<H"), f.read(2))[0]
+        para = array.array(str("H"))
+        if sys.version_info > (3, 8):
+            para.frombytes(f.read(paradigm_len * 2))
+        else:
+            para.fromstring(f.read(paradigm_len * 2))
 
-            para = array.array(str("H"))
-            para.fromfile(f, paradigm_len)
+        paradigms.append(para)
 
-            paradigms.append(para)
     return paradigms
 
 
--- contrib/python/pymorphy2/py2/pymorphy2/utils.py	(index)
+++ contrib/python/pymorphy2/py2/pymorphy2/utils.py	(working tree)
@@ -8,6 +8,8 @@ import itertools
 import codecs
 import json
 
+from library.python.resource import resfs_read
+
 
 def get_mem_usage():
     """
@@ -85,6 +87,11 @@ def json_write(filename, obj, **json_options):
 
 def json_read(filename, **json_options):
     """ Read an object from a json file ``filename`` """
+    data = resfs_read(filename)
+    if data:
+        data = data.decode('utf-8')
+        return json.loads(data, **json_options)
+
     with codecs.open(filename, 'r', 'utf8') as f:
         return json.load(f, **json_options)
 
