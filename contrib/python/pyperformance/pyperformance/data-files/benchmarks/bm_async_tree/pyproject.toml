[project]
name = "pyperformance_bm_async_tree"
requires-python = ">=3.8"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "async_tree"
tags = "asyncio"
extra_opts = ["none"]
