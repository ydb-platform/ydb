--- contrib/python/python-barcode/barcode/writer.py	(index)
+++ contrib/python/python-barcode/barcode/writer.py	(working tree)
@@ -51,7 +51,6 @@ def create_svg_object(with_doctype=False):
 
 SIZE = "{0:.3f}mm"
 COMMENT = f"Autogenerated with python-barcode {version}"
-PATH = os.path.dirname(os.path.abspath(__file__))
 
 
 class BaseWriter:
@@ -94,7 +93,7 @@ class BaseWriter:
         }
         self.module_width = 10
         self.module_height = 10
-        self.font_path = os.path.join(PATH, "fonts", "DejaVuSansMono.ttf")
+        self.font_path = os.path.join("fonts", "DejaVuSansMono.ttf")
         self.font_size = 10
         self.quiet_zone = 6.5
         self.background = "white"
@@ -436,1 +435,4 @@ else:
-            font = ImageFont.truetype(self.font_path, font_size)
+            import io
+            import pkgutil
+            with io.BytesIO(pkgutil.get_data(__package__, self.font_path)) as font_io:
+                font = ImageFont.truetype(font_io, font_size)
