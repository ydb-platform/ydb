commit 72cfb4c6b6a0f3041d05ad991a9482d8a5ee3cd0
merge: 0dc983708100cff128c20dc55ed79ff038008554 95d636b886b47ebcee6a0e18ee1510d78199ded1
author: pabozin
date: 2023-11-15T11:15:40+03:00
revision: 12898117

    VARIOQUB-895: remove resource and deprecated warnings
    
    remove resource and deprecated warnings
    
    REVIEW: 4854671

--- contrib/python/selenium/py3/selenium/webdriver/remote/remote_connection.py	(0dc983708100cff128c20dc55ed79ff038008554)
+++ contrib/python/selenium/py3/selenium/webdriver/remote/remote_connection.py	(72cfb4c6b6a0f3041d05ad991a9482d8a5ee3cd0)
@@ -398,8 +398,8 @@ class RemoteConnection(object):
 
             statuscode = resp.status
         else:
-            http = urllib3.PoolManager(timeout=self._timeout)
-            resp = http.request(method, url, body=body, headers=headers)
+            with urllib3.PoolManager(timeout=self._timeout) as http:
+                resp = http.request(method, url, body=body, headers=headers)
 
             statuscode = resp.status
             if not hasattr(resp, 'getheader'):
@@ -415,8 +415,8 @@ class RemoteConnection(object):
             if 399 < statuscode <= 500:
                 return {'status': statuscode, 'value': data}
             content_type = []
-            if resp.getheader('Content-Type') is not None:
-                content_type = resp.getheader('Content-Type').split(';')
+            if resp.headers.get('Content-Type') is not None:
+                content_type = resp.headers.get('Content-Type').split(';')
             if not any([x.startswith('image/png') for x in content_type]):
 
                 try:
