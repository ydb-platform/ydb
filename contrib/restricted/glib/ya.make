# Generated by devtools/yamaker from nixpkgs 24.05.

LIBRARY()

WITHOUT_LICENSE_TEXTS()

VERSION(2.72.2)

ORIGINAL_SOURCE(https://gitlab.gnome.org/api/v4/projects/GNOME%2FGLib/repository/archive.tar.gz?sha=2.72.2)

LICENSE(Service-Dll-Harness)

NO_RUNTIME()

IF (USE_DYNAMIC_GLIB)
    PEERDIR(
        contrib/restricted/glib/dynamic
    )
ELSE()
    PEERDIR(
        contrib/restricted/glib/static
    )
ENDIF()

END()

RECURSE(
    include
    static
)

IF (NOT OS_WINDOWS)
    RECURSE(
        dynamic
    )
ENDIF()
