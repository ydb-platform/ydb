# Generated by devtools/yamaker.

IF (NOT USE_STL_SYSTEM)
    IF (NOT USE_SYSTEM_PYTHON OR NOT _SYSTEM_PYTHON27)
        IF (OS_LINIX OR OS_DARWIN OR OS_WINDOWS)
            IF (ARCH_X86_64 OR ARCH_ARM64)
                RECURSE(
                    compare
                )
            ENDIF()
        ENDIF()
    ENDIF()
ENDIF()
