# Generated by devtools/yamaker.

VERSION(3.5.2)

RECURSE(
    libffi.bhaible
    libffi.call
    libffi.closures
)

IF (NOT OS_IOS AND NOT OS_EMSCRIPTEN)
    RECURSE(
        libffi.go
    )
ENDIF()

IF (NOT OS_WINDOWS AND NOT ARCH_PPC64LE AND NOT OS_EMSCRIPTEN)
    RECURSE(
        libffi.complex
    )
ENDIF()

IF (NOT OS_WINDOWS)
    RECURSE(
        libffi.threads/tsan
    )
ENDIF()
