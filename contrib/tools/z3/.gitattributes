# Preserve pinned upstream and generated bytes verbatim.
CMakeLists.txt -whitespace
LICENSE.txt -whitespace
README-CMake.md -whitespace
README.md -whitespace
RELEASE_NOTES.md -whitespace
cmake/** -whitespace
scripts/** -whitespace
src/** -whitespace
generated/src/** -whitespace

# Its Setext heading is seven '=' characters, which resembles a conflict marker.
src/api/ml/README.md -diff
