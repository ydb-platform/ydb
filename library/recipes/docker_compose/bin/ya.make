PACKAGE()

IF (OS_LINUX)
    FROM_SANDBOX(FILE 770480022 OUT docker-compose)
ENDIF()

IF (OS_DARWIN)
    FROM_SANDBOX(FILE 771555567 OUT docker-compose)
ENDIF()

IF (OS_WINDOWS)
    FROM_SANDBOX(FILE 771617877 OUT docker-compose.exe)
ENDIF()

END()
