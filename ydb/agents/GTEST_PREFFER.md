# C++ test framework preference

- If the module **already has** C++ unit tests, use the **same** framework
  (`UNITTEST` or `GTEST`) as the existing tests in that module.
- If the module has **no** C++ unit tests yet, prefer **GTEST**.

Test types and how to run them: [`TESTS.md`](TESTS.md).
