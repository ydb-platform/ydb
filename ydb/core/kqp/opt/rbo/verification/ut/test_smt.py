import os
import subprocess
import unittest
from itertools import combinations
from unittest import mock

try:
    import yatest.common as yatest_common
except ImportError:
    yatest_common = None

from ydb.core.kqp.opt.rbo.verification.rbo_verifier import smt


SOLVER = (
    yatest_common.binary_path("contrib/tools/z3/z3")
    if yatest_common is not None
    else os.environ.get("RBO_Z3")
)


def _deep_shared_term(leaf, depth=2000):
    term = leaf
    for _ in range(depth):
        term = smt.Term(leaf.sort, "deep", (term, term))
    return term


def _structurally_repeated_successor_comparison(name):
    first = smt.add(smt.symbol(name, smt.INT), smt.ONE)
    second = smt.add(
        smt.symbol(name, smt.INT),
        smt.int_value(1),
    )
    return smt.lt(first, smt.add(second, smt.ONE))


class SmtTest(unittest.TestCase):
    def test_term_structural_hash_is_cached_for_deep_shared_dags(self):
        first = _deep_shared_term(smt.symbol("value", smt.INT))
        second = _deep_shared_term(smt.symbol("value", smt.INT))

        self.assertEqual(hash(first), hash(second))
        self.assertEqual(first, second)
        self.assertEqual(len({first, second}), 1)

    def test_term_structural_equality_resolves_deep_hash_collisions(self):
        first = _deep_shared_term(smt.int_value(-1))
        second = _deep_shared_term(smt.int_value(-2))

        self.assertEqual(hash(first), hash(second))
        self.assertNotEqual(first, second)
        self.assertEqual(len({first, second}), 2)

    def test_script_timeout_requires_a_positive_exact_integer(self):
        for value in (0, -1, True, 1.0):
            with self.subTest(value=value):
                with self.assertRaisesRegex(smt.SmtError, "positive integer"):
                    smt.Script(value)

    def test_literal_constructors_require_exact_python_types(self):
        for value in (None, 0, 1):
            with self.subTest(constructor="bool", value=value):
                with self.assertRaisesRegex(smt.SmtError, "must be bool"):
                    smt.bool_value(value)
        for value in (True, False, 1.0):
            with self.subTest(constructor="int", value=value):
                with self.assertRaisesRegex(smt.SmtError, "must be int"):
                    smt.int_value(value)

    def test_distinct_is_typed_compact_and_constant_folded(self):
        left = smt.symbol("left", smt.INT)
        right = smt.symbol("right", smt.INT)

        self.assertIs(smt.distinct(), smt.TRUE)
        self.assertIs(smt.distinct(left), smt.TRUE)
        self.assertIs(smt.distinct(left, left), smt.FALSE)
        self.assertIs(
            smt.distinct(smt.int_value(1), smt.int_value(2)),
            smt.TRUE,
        )
        self.assertEqual(
            smt.distinct(left, right).render(),
            "(distinct left right)",
        )
        with self.assertRaisesRegex(smt.SmtError, "expected Int, got Bool"):
            smt.distinct(left, smt.TRUE)

    def test_script_is_typed_and_deterministic(self):
        script = smt.Script(timeout_ms=100)
        value = script.fresh_constant("ignored user hint", smt.INT)
        predicate = script.fresh_function("predicate", (smt.INT,), smt.BOOL)
        script.assert_term(predicate(value))
        self.assertEqual(
            script.render(),
            "; Generated by the YDB new-RBO bounded equivalence verifier.\n"
            "(set-option :timeout 100)\n"
            "(set-option :produce-models true)\n"
            "(set-logic ALL)\n"
            '; v_0: "ignored user hint"\n'
            "(declare-fun v_0 () Int)\n"
            '; f_1: "predicate"\n'
            "(declare-fun f_1 (Int) Bool)\n"
            "(assert (f_1 v_0))\n"
            "(check-sat)\n",
        )

    def test_product_and_exact_definition_render_deterministically(self):
        script = smt.Script()
        row = script.fresh_product_sort(
            "sorting row",
            (smt.BOOL, smt.INT),
        )
        before = script.fresh_defined_function(
            "live row precedes rank",
            (row, smt.INT),
            smt.BOOL,
            lambda parameters: smt.and_(
                row.select(parameters[0], 0),
                smt.lt(row.select(parameters[0], 1), parameters[1]),
            ),
        )
        script.assert_term(
            before(
                row.pack(smt.TRUE, smt.int_value(7)),
                smt.int_value(8),
            )
        )

        self.assertEqual(
            script.render(),
            "; Generated by the YDB new-RBO bounded equivalence verifier.\n"
            "(set-option :produce-models true)\n"
            "(set-logic ALL)\n"
            '; d_0: "sorting row"\n'
            "(declare-datatypes () ((d_0 "
            "(mk_d_0 (d_0_f0 Bool) (d_0_f1 Int)))))\n"
            '; df_1: "live row precedes rank"\n'
            "(define-fun df_1 ((df_1_p0 d_0) (df_1_p1 Int)) Bool "
            "(and (d_0_f0 df_1_p0) (< (d_0_f1 df_1_p0) df_1_p1)))\n"
            "(assert (df_1 (mk_d_0 true 7) 8))\n"
            "(check-sat)\n",
        )

    def test_product_api_is_narrow_and_typed(self):
        script = smt.Script()
        row = script.fresh_product_sort("row", (smt.BOOL, smt.INT))

        with self.assertRaisesRegex(smt.SmtError, "wrong argument sorts"):
            row.pack(smt.TRUE)
        with self.assertRaisesRegex(smt.SmtError, "wrong argument sorts"):
            row.pack(smt.ONE, smt.ONE)
        with self.assertRaisesRegex(smt.SmtError, "expected d_0, got Int"):
            row.select(smt.ONE, 0)
        for index in (-1, 2, True):
            with self.subTest(index=index):
                with self.assertRaisesRegex(smt.SmtError, "out of range"):
                    row.select(
                        row.pack(smt.TRUE, smt.ONE),
                        index,
                    )

        with self.assertRaisesRegex(smt.SmtError, "at least one field"):
            script.fresh_product_sort("empty", ())
        with self.assertRaisesRegex(smt.SmtError, "unsupported SMT sort"):
            script.fresh_product_sort("real row", ("Real",))
        with self.assertRaisesRegex(smt.SmtError, "unsupported SMT sort"):
            script.fresh_constant("custom constant", row.sort)
        with self.assertRaisesRegex(smt.SmtError, "unsupported SMT sort"):
            script.fresh_function("custom function", (row.sort,), smt.BOOL)
        with self.assertRaisesRegex(
            smt.SmtError,
            "pass its owning ProductSort",
        ):
            script.fresh_defined_function(
                "unowned row name",
                (row.sort,),
                smt.BOOL,
                lambda parameters: smt.TRUE,
            )

        other = smt.Script()
        with self.assertRaisesRegex(
            smt.SmtError,
            "belongs to another SMT script",
        ):
            other.fresh_defined_function(
                "foreign row",
                (row,),
                smt.BOOL,
                lambda parameters: smt.TRUE,
            )

    def test_exact_definition_rejects_free_symbols_and_bad_result_sorts(self):
        script = smt.Script()
        free = script.fresh_constant("database value", smt.INT)

        with self.assertRaisesRegex(
            smt.SmtError,
            "free symbols outside its parameters: v_0:Int",
        ):
            script.fresh_defined_function(
                "captures database value",
                (smt.INT,),
                smt.BOOL,
                lambda parameters: smt.eq(parameters[0], free),
            )
        with self.assertRaisesRegex(smt.SmtError, "expected Bool, got Int"):
            script.fresh_defined_function(
                "wrong result",
                (smt.INT,),
                smt.BOOL,
                lambda parameters: parameters[0],
            )
        with self.assertRaisesRegex(smt.SmtError, "body builder must be callable"):
            script.fresh_defined_function(
                "not callable",
                (),
                smt.BOOL,
                smt.TRUE,  # type: ignore[arg-type]
            )

    def test_exact_definition_rejects_quantifiers_and_nullary_captures(self):
        script = smt.Script()
        local = smt.symbol("local_rank", smt.INT)
        with self.assertRaisesRegex(
            smt.SmtError,
            "must be quantifier-free",
        ):
            script.fresh_defined_function(
                "some lower rank exists",
                (smt.INT,),
                smt.BOOL,
                lambda parameters: smt.exists(
                    (local,),
                    smt.lt(local, parameters[0]),
                ),
            )

        captured = script.fresh_function("captured", (), smt.BOOL)
        with self.assertRaisesRegex(
            smt.SmtError,
            "captures a nullary declaration",
        ):
            script.fresh_defined_function(
                "captures nullary declaration",
                (),
                smt.BOOL,
                lambda parameters: captured(),
            )

    def test_exact_definition_rejects_foreign_product_operations(self):
        local_script = smt.Script()
        local = local_script.fresh_product_sort(
            "local row",
            (smt.BOOL,),
        )
        foreign_script = smt.Script()
        foreign = foreign_script.fresh_product_sort(
            "foreign row",
            (smt.INT,),
        )

        with self.assertRaisesRegex(
            smt.SmtError,
            "another SMT script",
        ):
            local_script.fresh_defined_function(
                "foreign selector",
                (local,),
                smt.INT,
                lambda parameters: foreign.select(parameters[0], 0),
            )

    def test_nullary_uninterpreted_function_is_a_symbol(self):
        script = smt.Script()
        function = script.fresh_function("constant", (), smt.INT)
        script.assert_term(smt.eq(function(), smt.int_value(1)))
        self.assertIn("(assert (= f_0 1))", script.render())

    def test_get_value_parser(self):
        expressions = smt.parse_sexpressions('sat\n((v_0 true) (v_1 (- 3)) (v_2 "a""b"))\n')
        bindings = expressions[1]
        self.assertEqual(smt.atom_value(bindings[0][1]), True)
        self.assertEqual(smt.atom_value(bindings[1][1]), -3)
        self.assertEqual(smt.atom_value(bindings[2][1]), 'a"b')

    def test_string_literals_receive_byte_ordered_ranks_when_sealed(self):
        script = smt.Script()
        accented = script.string_atom("é")
        ascii_text = script.string_atom("u{e9}")
        self.assertIs(script.string_atom("é"), accented)
        self.assertNotEqual(accented, ascii_text)

        formula = script.render()

        self.assertIn("(assert (= v_0 1))", formula)
        self.assertIn("(assert (= v_1 0))", formula)
        self.assertEqual(script.string_literals, {0: "u{e9}", 1: "é"})

    def test_known_string_atom_equality_is_exact_and_partial(self):
        script = smt.Script()
        first = script.string_atom("first")
        same = script.string_atom("first")
        different = script.string_atom("second")
        symbolic = script.fresh_constant("source_string", smt.INT)

        self.assertIs(script.known_string_atom_equality(first, same), smt.TRUE)
        self.assertIs(
            script.known_string_atom_equality(first, different),
            smt.FALSE,
        )
        self.assertIs(
            script.known_string_atom_equality(different, first),
            smt.FALSE,
        )
        self.assertIsNone(
            script.known_string_atom_equality(first, symbolic)
        )
        self.assertIsNone(
            script.known_string_atom_equality(symbolic, first)
        )
        self.assertIsNone(
            script.known_string_atom_equality(symbolic, symbolic)
        )

    def test_known_string_atom_equality_rejects_cross_script_name_collision(self):
        local = smt.Script()
        foreign = smt.Script()
        local_atom = local.string_atom("same")
        foreign_atom = foreign.string_atom("different")

        self.assertEqual(local_atom, foreign_atom)
        self.assertIsNone(
            local.known_string_atom_equality(local_atom, foreign_atom)
        )
        self.assertIsNone(
            local.known_string_atom_equality(foreign_atom, local_atom)
        )

    def test_known_string_atom_equality_is_stable_after_registration_and_sealing(self):
        script = smt.Script()
        first = script.string_atom("first")
        second = script.string_atom("second")
        self.assertIs(
            script.known_string_atom_equality(first, second),
            smt.FALSE,
        )

        script.string_atom("earlier in byte order")
        symbolic = script.fresh_constant("source_string", smt.INT)
        script.register_string_term(symbolic)
        script.render()

        self.assertIs(
            script.known_string_atom_equality(first, first),
            smt.TRUE,
        )
        self.assertIs(
            script.known_string_atom_equality(first, second),
            smt.FALSE,
        )
        self.assertIsNone(
            script.known_string_atom_equality(first, symbolic)
        )

    def test_observed_string_terms_are_bounded_to_replayable_ranks(self):
        script = smt.Script()
        value = script.fresh_constant("source_string", smt.INT)
        script.register_string_term(value)
        script.string_atom("a")
        script.string_atom("c")

        formula = script.render()

        self.assertIn("(assert (and (not (< v_0 0)) (< v_0 5)))", formula)
        self.assertEqual(
            script.string_literals,
            {0: "\0", 1: "a", 2: "a\0", 3: "c", 4: "c\0"},
        )

    def test_string_registration_fails_closed_after_sealing(self):
        script = smt.Script()
        existing = script.string_atom("a")
        script.render()
        self.assertIs(script.string_atom("a"), existing)
        with self.assertRaisesRegex(smt.SmtError, "after.*sealed"):
            script.string_atom("b")
        with self.assertRaisesRegex(smt.SmtError, "after.*sealed"):
            script.register_string_term(smt.symbol("late", smt.INT))

    def test_choice_dependent_string_terms_get_guarded_finite_domains(self):
        script = smt.Script()
        choice = script.fresh_constant("ordinal", smt.INT)
        function = script.fresh_function("string_result", (smt.INT,), smt.INT)
        script.register_quantified_choice(choice, 3)
        script.register_string_term(function(choice))

        formula = script.render()

        self.assertIn("(forall ((v_0 Int))", formula)
        self.assertIn("(< v_0 3)", formula)
        self.assertEqual(
            tuple(script.string_literals.values()),
            ("", "\0", "\0\0"),
        )

        reverse = smt.Script()
        later_choice = reverse.fresh_constant("ordinal", smt.INT)
        later_function = reverse.fresh_function("string_result", (smt.INT,), smt.INT)
        reverse.register_string_term(later_function(later_choice))
        reverse.register_quantified_choice(later_choice, 2)
        reverse.seal_string_order()
        self.assertEqual(len(reverse.string_literals), 2)

        sealed = smt.Script()
        sealed_choice = sealed.fresh_constant("ordinal", smt.INT)
        sealed_function = sealed.fresh_function(
            "string_result",
            (smt.INT,),
            smt.INT,
        )
        sealed.register_string_term(sealed_function(sealed_choice))
        sealed.seal_string_order()
        with self.assertRaisesRegex(smt.SmtError, "after.*sealed"):
            sealed.register_quantified_choice(sealed_choice, 2)

    def test_global_invariants_depending_on_quantified_choices_fail_closed(self):
        script = smt.Script()
        choice = script.fresh_constant("ordinal", smt.INT)
        function = script.fresh_function("bounded_result", (smt.INT,), smt.INT)
        script.register_quantified_choice(choice, 2)
        with self.assertRaisesRegex(smt.SmtError, "global invariant.*quantified"):
            script.assert_global(smt.lt(function(choice), smt.int_value(10)))

        reverse = smt.Script()
        later_choice = reverse.fresh_constant("ordinal", smt.INT)
        later_function = reverse.fresh_function("bounded_result", (smt.INT,), smt.INT)
        reverse.assert_global(smt.lt(later_function(later_choice), smt.int_value(10)))
        with self.assertRaisesRegex(smt.SmtError, "global invariant.*quantified"):
            reverse.register_quantified_choice(later_choice, 2)

    def test_quantified_choice_batch_preserves_reverse_dependency_audit(self):
        script = smt.Script()
        first = script.fresh_constant("first", smt.INT)
        second = script.fresh_constant("second", smt.INT)
        script.register_quantified_choices(((first, 2), (second, 3)))
        self.assertEqual(script.quantified_choice_bound(first), 2)
        self.assertEqual(script.quantified_choice_bound(second), 3)

        reverse = smt.Script()
        dependent = reverse.fresh_constant("dependent", smt.INT)
        independent = reverse.fresh_constant("independent", smt.INT)
        reverse.assert_global(smt.lt(dependent, smt.int_value(2)))
        with self.assertRaisesRegex(smt.SmtError, "global invariant.*quantified"):
            reverse.register_quantified_choices(
                ((dependent, 2), (independent, 2))
            )

    def test_choice_invariant_is_universally_guarded_by_registered_bounds(self):
        script = smt.Script()
        first = script.fresh_constant("first", smt.INT)
        second = script.fresh_constant("second", smt.INT)
        function = script.fresh_function(
            "bounded_result",
            (smt.INT, smt.INT),
            smt.INT,
        )
        script.register_quantified_choice(first, 2)
        script.register_quantified_choice(second, 3)
        invariant = smt.lt(function(first, second), smt.int_value(10))

        script.assert_choice_invariant(invariant)

        quantified = script.assertions[0]
        self.assertEqual(quantified.operation, "forall")
        self.assertEqual(quantified.arguments[:-1], (first, second))
        formula = script.render()
        self.assertIn("(forall ((v_0 Int) (v_1 Int))", formula)
        self.assertIn("(< v_0 2)", formula)
        self.assertIn("(< v_1 3)", formula)

    def test_quantified_choice_bounds_are_positive_and_immutable(self):
        script = smt.Script()
        choice = script.fresh_constant("ordinal", smt.INT)
        script.register_quantified_choice(choice, 2)
        assert isinstance(choice.atom, str)
        script.register_quantified_choice(
            smt.symbol(choice.atom, choice.sort),
            2,
        )
        with self.assertRaisesRegex(smt.SmtError, "inconsistent bounds"):
            script.register_quantified_choice(choice, 3)
        for bound in (0, -1, True):
            with self.subTest(bound=bound):
                other = script.fresh_constant("other", smt.INT)
                with self.assertRaisesRegex(smt.SmtError, "positive integer"):
                    script.register_quantified_choice(other, bound)

    def test_dependency_audit_is_iterative_and_matches_copied_symbols(self):
        script = smt.Script()
        choice = script.fresh_constant("ordinal", smt.INT)
        script.register_quantified_choice(choice, 2)
        independent = _deep_shared_term(smt.symbol("independent", smt.INT))
        script.assert_global(smt.Term(smt.BOOL, "predicate", (independent,)))

        assert isinstance(choice.atom, str)
        copied_choice = smt.symbol(choice.atom, choice.sort)
        dependent = _deep_shared_term(copied_choice)
        with self.assertRaisesRegex(smt.SmtError, "global invariant.*quantified"):
            script.assert_global(smt.Term(smt.BOOL, "predicate", (dependent,)))

        self.assertFalse(
            smt._depends_on(
                smt.symbol(choice.atom, smt.BOOL),
                {smt._symbol_key(choice)},
            )
        )

        reverse = smt.Script()
        later_choice = reverse.fresh_constant("ordinal", smt.INT)
        assert isinstance(later_choice.atom, str)
        copied_later = smt.symbol(later_choice.atom, later_choice.sort)
        reverse.assert_global(
            smt.Term(
                smt.BOOL,
                "predicate",
                (_deep_shared_term(copied_later),),
            )
        )
        with self.assertRaisesRegex(smt.SmtError, "global invariant.*quantified"):
            reverse.register_quantified_choice(later_choice, 2)

    def test_deep_string_terms_use_identity_storage_and_exact_choice_capacity(self):
        independent = smt.Script()
        choice = independent.fresh_constant("ordinal", smt.INT)
        value = _deep_shared_term(smt.symbol("independent_string", smt.INT))
        independent.register_string_term(value)
        independent.register_string_term(value)
        self.assertEqual(len(independent._string_terms), 1)
        independent.register_quantified_choice(choice, 2)

        deduplicated = smt.Script()
        left = _deep_shared_term(smt.symbol("same_string", smt.INT))
        right = _deep_shared_term(smt.symbol("same_string", smt.INT))
        deduplicated.register_string_term(left)
        deduplicated.register_string_term(right)
        self.assertEqual(len(deduplicated._string_terms), 2)
        deduplicated.seal_string_order()
        self.assertEqual(len(deduplicated._string_terms), 1)
        self.assertEqual(len(deduplicated.string_literals), 1)

        forward = smt.Script()
        forward_choice = forward.fresh_constant("ordinal", smt.INT)
        forward.register_quantified_choice(forward_choice, 3)
        assert isinstance(forward_choice.atom, str)
        forward_copy = smt.symbol(forward_choice.atom, forward_choice.sort)
        forward.register_string_term(_deep_shared_term(forward_copy))
        forward.seal_string_order()
        self.assertEqual(len(forward.string_literals), 3)

        reverse = smt.Script()
        reverse_choice = reverse.fresh_constant("ordinal", smt.INT)
        assert isinstance(reverse_choice.atom, str)
        reverse_copy = smt.symbol(reverse_choice.atom, reverse_choice.sort)
        reverse.register_string_term(_deep_shared_term(reverse_copy))
        reverse.register_quantified_choice(reverse_choice, 2)
        reverse.seal_string_order()
        self.assertEqual(len(reverse.string_literals), 2)

        bounded = smt.Script()
        with mock.patch.object(smt, "MAX_REPRESENTATIVES", 1):
            bounded.register_string_term(smt.symbol("first_string", smt.INT))
            with self.assertRaisesRegex(smt.SmtError, "at least 2 ranks.*limit is 1"):
                bounded.register_string_term(
                    smt.symbol("second_string", smt.INT)
                )

    def test_string_choice_capacity_sums_products_after_structural_compaction(self):
        script = smt.Script()
        first = script.fresh_constant("first", smt.INT)
        second = script.fresh_constant("second", smt.INT)
        function = script.fresh_function(
            "choice_string",
            (smt.INT, smt.INT),
            smt.INT,
        )
        script.register_quantified_choice(first, 2)
        script.register_quantified_choice(second, 3)
        script.register_string_term(function(first, second))
        script.register_string_term(function(first, second))
        script.register_string_term(smt.symbol("independent_string", smt.INT))

        script.seal_string_order()

        self.assertEqual(len(script._string_terms), 2)
        self.assertEqual(len(script.string_literals), 7)

        too_large = smt.Script()
        choice = too_large.fresh_constant("choice", smt.INT)
        result = too_large.fresh_function(
            "choice_string",
            (smt.INT,),
            smt.INT,
        )
        too_large.register_quantified_choice(choice, 2)
        too_large.register_string_term(result(choice))
        with (
            mock.patch.object(smt, "MAX_REPRESENTATIVES", 1),
            self.assertRaisesRegex(smt.SmtError, "at least 2 ranks.*limit is 1"),
        ):
            too_large.seal_string_order()

    def test_global_invariants_render_before_ordinary_obligations(self):
        script = smt.Script()
        ordinary = script.fresh_constant("ordinary", smt.BOOL)
        global_invariant = script.fresh_constant("global", smt.BOOL)
        script.assert_term(ordinary)
        script.assert_global(global_invariant)

        formula = script.render()

        self.assertEqual(script.assertions, (ordinary, global_invariant))
        self.assertLess(
            formula.index("(assert v_1)"),
            formula.index("(assert v_0)"),
        )

    def test_render_branch_replaces_only_the_marked_obligation(self):
        script = smt.Script()
        before = script.fresh_constant("before", smt.BOOL)
        obligation = script.fresh_constant("obligation", smt.BOOL)
        after = script.fresh_constant("after", smt.BOOL)
        branch = script.fresh_constant("branch", smt.BOOL)
        script.assert_term(before)
        script.assert_obligation(obligation)
        script.assert_term(after)

        formula = script.render_branch(branch)

        assertions = [
            line
            for line in formula.splitlines()
            if line.startswith("(assert ")
        ]
        self.assertEqual(
            assertions,
            ["(assert v_0)", "(assert v_3)", "(assert v_2)"],
        )
        self.assertIn("(assert v_1)", script.render())

    def test_deferred_string_domains_render_before_the_obligation(self):
        script = smt.Script()
        value = script.fresh_constant("string", smt.INT)
        obligation = script.fresh_constant("obligation", smt.BOOL)
        script.register_string_term(value)
        script.string_atom("literal")
        script.assert_term(obligation)

        formula = script.render()

        self.assertLess(
            formula.index("(assert (= v_2"),
            formula.index("(assert v_1)"),
        )
        self.assertLess(
            formula.index("(assert (and (not (< v_0 0))"),
            formula.index("(assert v_1)"),
        )

    def test_boolean_connectives_are_typed_and_compact_wide_junctions(self):
        first = smt.symbol("first", smt.BOOL)
        second = smt.symbol("second", smt.BOOL)
        equal_copy = smt.symbol("first", smt.BOOL)
        repeated = (first,) * smt._BOOLEAN_COMPACTION_WIDTH
        for connective, neutral, terminal in (
            (smt.and_, smt.TRUE, smt.FALSE),
            (smt.or_, smt.FALSE, smt.TRUE),
        ):
            with self.subTest(connective=connective.__name__):
                self.assertIs(connective(), neutral)
                self.assertIs(connective(first, neutral), first)
                self.assertIs(connective(first, terminal), terminal)
                self.assertEqual(len(connective(*repeated).arguments), len(repeated))
                self.assertIs(connective(*repeated, first), first)
                result = connective(
                    *repeated, connective(second, first), second, equal_copy,
                )
                self.assertEqual(
                    tuple(map(id, result.arguments)),
                    tuple(map(id, (first, second, equal_copy))),
                )
                self.assertIs(connective(terminal, smt.ONE), terminal)
                for invalid in (True, smt.ONE):
                    for args in ((invalid,), (first, invalid), (invalid, terminal)):
                        with self.assertRaises(smt.SmtError):
                            connective(*args)  # type: ignore[arg-type]

    def test_integer_modulo_is_typed_and_constant_folded(self):
        value = smt.symbol("value", smt.INT)
        self.assertEqual(smt.mod(value, 8).render(), "(mod value 8)")
        self.assertEqual(smt.mod(smt.int_value(-1), 8), smt.int_value(7))
        with self.assertRaises(smt.SmtError):
            smt.mod(value, 0)

    def test_positive_constant_integer_division_is_exact_and_typed(self):
        value = smt.symbol("value", smt.INT)
        self.assertEqual(smt.div(value, 10).render(), "(div value 10)")
        self.assertEqual(smt.div(value, 1), value)
        self.assertEqual(smt.div(smt.int_value(9), 2), smt.int_value(4))
        self.assertEqual(smt.div(smt.int_value(-9), 2), smt.int_value(-5))
        for divisor in (0, -1, True):
            with self.subTest(divisor=divisor):
                with self.assertRaises(smt.SmtError):
                    smt.div(value, divisor)
        with self.assertRaises(smt.SmtError):
            smt.div(smt.TRUE, 2)

    def test_nonnegative_by_positive_symbolic_division_is_narrow_and_exact(self):
        dividend = smt.symbol("dividend", smt.INT)
        divisor = smt.symbol("divisor", smt.INT)
        self.assertEqual(
            smt.div_nonnegative_by_positive(dividend, divisor).render(),
            "(div dividend divisor)",
        )
        self.assertEqual(
            smt.div_nonnegative_by_positive(smt.int_value(17), smt.int_value(5)),
            smt.int_value(3),
        )
        self.assertIs(
            smt.div_nonnegative_by_positive(dividend, smt.ONE),
            dividend,
        )
        for invalid_dividend, invalid_divisor in (
            (smt.int_value(-1), divisor),
            (dividend, smt.ZERO),
            (dividend, smt.int_value(-1)),
            (smt.TRUE, divisor),
            (dividend, smt.TRUE),
        ):
            with self.subTest(
                dividend=invalid_dividend,
                divisor=invalid_divisor,
            ):
                with self.assertRaises(smt.SmtError):
                    smt.div_nonnegative_by_positive(
                        invalid_dividend,
                        invalid_divisor,
                    )

    def test_integer_arithmetic_is_typed_and_constant_folded(self):
        left = smt.symbol("left", smt.INT)
        right = smt.symbol("right", smt.INT)
        self.assertEqual(smt.add(left, right).render(), "(+ left right)")
        self.assertEqual(smt.sub(left, right).render(), "(- left right)")
        self.assertEqual(smt.mul(left, right).render(), "(* left right)")
        self.assertEqual(smt.add(smt.int_value(3), smt.int_value(5)), smt.int_value(8))
        self.assertEqual(smt.sub(smt.int_value(3), smt.int_value(5)), smt.int_value(-2))
        self.assertEqual(smt.mul(smt.int_value(-3), smt.int_value(5)), smt.int_value(-15))
        with self.assertRaises(smt.SmtError):
            smt.add(smt.TRUE, right)
        with self.assertRaises(smt.SmtError):
            smt.sub(smt.TRUE, right)
        with self.assertRaises(smt.SmtError):
            smt.mul(left, smt.TRUE)

    def test_integer_less_than_is_typed_and_constant_folded(self):
        left = smt.symbol("left", smt.INT)
        right = smt.symbol("right", smt.INT)
        self.assertEqual(smt.lt(left, right).render(), "(< left right)")
        self.assertEqual(smt.lt(smt.int_value(1), smt.int_value(2)), smt.TRUE)
        self.assertEqual(smt.lt(left, left), smt.FALSE)
        with self.assertRaises(smt.SmtError):
            smt.lt(smt.TRUE, right)

    def test_equality_is_typed_and_constant_folded(self):
        value = smt.symbol("value", smt.INT)
        self.assertEqual(smt.eq(value, smt.int_value(1)).render(), "(= value 1)")
        self.assertEqual(smt.eq(smt.int_value(1), smt.int_value(2)), smt.FALSE)
        self.assertEqual(smt.eq(smt.FALSE, smt.TRUE), smt.FALSE)
        with self.assertRaises(smt.SmtError):
            smt.eq(smt.TRUE, value)

    def test_quantifiers_render_typed_shadowing_exactly(self):
        script = smt.Script()
        rank = script.fresh_constant("sort rank", smt.INT)
        tie = script.fresh_constant("tie choice", smt.BOOL)
        body = smt.or_(tie, smt.lt(rank, smt.int_value(2)))
        script.assert_term(smt.forall((rank, tie), body))

        self.assertEqual(
            script.render(),
            "; Generated by the YDB new-RBO bounded equivalence verifier.\n"
            "(set-option :produce-models true)\n"
            "(set-logic ALL)\n"
            '; v_0: "sort rank"\n'
            "(declare-fun v_0 () Int)\n"
            '; v_1: "tie choice"\n'
            "(declare-fun v_1 () Bool)\n"
            "(assert (forall ((v_0 Int) (v_1 Bool)) (or v_1 (< v_0 2))))\n"
            "(check-sat)\n",
        )
        self.assertEqual(
            smt.exists((rank,), smt.eq(rank, smt.int_value(1))).render(),
            "(exists ((v_0 Int)) (= v_0 1))",
        )

    def test_shared_dag_renders_as_dependency_ordered_lets(self):
        left = smt.symbol("left", smt.INT)
        right = smt.symbol("right", smt.INT)
        shared_sum = smt.add(left, right)
        shared_product = smt.mul(shared_sum, shared_sum)
        term = smt.lt(shared_product, smt.add(shared_product, smt.ONE))

        self.assertEqual(
            term.render_shared(),
            "(let ((rbo_let_0 (+ left right))) "
            "(let ((rbo_let_1 (* rbo_let_0 rbo_let_0))) "
            "(< rbo_let_1 (+ rbo_let_1 1))))",
        )
        self.assertEqual(term.render_shared(), term.render_shared())

    def test_structurally_equal_compounds_share_one_let(self):
        first = smt.add(
            smt.symbol("left", smt.INT),
            smt.symbol("right", smt.INT),
        )
        second = smt.add(
            smt.symbol("left", smt.INT),
            smt.symbol("right", smt.INT),
        )

        self.assertIsNot(first, second)
        self.assertEqual(first, second)
        self.assertEqual(
            smt.add(first, second).render_shared(),
            "(let ((rbo_let_0 (+ left right))) "
            "(+ rbo_let_0 rbo_let_0))",
        )

    def test_structural_cse_runs_at_and_just_below_scope_cap(self):
        first = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        second = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        term = smt.add(first, second, smt.symbol("tail", smt.INT))
        expected = (
            "(let ((rbo_let_1 (+ rbo_let_0 right))) "
            "(+ rbo_let_1 rbo_let_1 tail))"
        )

        # This scope has eight distinct object identities.  The exact cap and
        # one spare slot must both retain structural sharing.
        for cap in (8, 9):
            with self.subTest(cap=cap), mock.patch.object(
                smt,
                "_MAX_STRUCTURAL_CSE_SCOPE_NODES",
                cap,
            ):
                self.assertEqual(term.render_shared(), expected)
                self.assertEqual(term.render_shared(), expected)

    def test_quantified_scope_above_cap_uses_identity_only_sharing(self):
        first = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        second = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        shared = smt.mul(
            smt.symbol("left", smt.INT),
            smt.symbol("factor", smt.INT),
        )
        body = smt.Term(
            smt.BOOL,
            "predicate",
            (first, second, shared, shared),
        )
        term = smt.exists((smt.symbol("rank", smt.INT),), body)
        expected = (
            "(exists ((rank Int)) "
            "(let ((rbo_let_1 (* left factor))) "
            "(predicate (+ rbo_let_0 right) (+ rbo_let_0 right) "
            "rbo_let_1 rbo_let_1)))"
        )

        # The quantifier is opaque in its one-node outer scope.  Its body has
        # ten identities, so cap nine exercises the identity-only fallback.
        with mock.patch.object(
            smt,
            "_MAX_STRUCTURAL_CSE_SCOPE_NODES",
            9,
        ):
            self.assertEqual(term.render_shared(), expected)
            self.assertEqual(term.render_shared(), expected)

    def test_oversized_scope_does_not_disable_nested_structural_cse(self):
        first = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        second = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.symbol("right", smt.INT),
        )
        shared = smt.mul(
            smt.symbol("left", smt.INT),
            smt.symbol("factor", smt.INT),
        )
        nested_body = smt.Term(
            smt.BOOL,
            "nested",
            (
                smt.add(smt.symbol("rank", smt.INT), smt.ONE),
                smt.add(
                    smt.symbol("rank", smt.INT),
                    smt.int_value(1),
                ),
            ),
        )
        nested = smt.exists(
            (smt.symbol("rank", smt.INT),),
            nested_body,
        )
        term = smt.Term(
            smt.BOOL,
            "outer",
            (first, second, shared, shared, nested),
        )
        expected = (
            "(let ((rbo_let_1 (* left factor))) "
            "(outer (+ rbo_let_0 right) (+ rbo_let_0 right) "
            "rbo_let_1 rbo_let_1 (exists ((rank Int)) "
            "(let ((rbo_let_2 (+ rank 1))) "
            "(nested rbo_let_2 rbo_let_2)))))"
        )

        # The outer scope has eleven identities and falls back at cap ten;
        # the seven-node quantified body independently keeps structural CSE.
        with mock.patch.object(
            smt,
            "_MAX_STRUCTURAL_CSE_SCOPE_NODES",
            10,
        ):
            self.assertEqual(term.render_shared(), expected)
            self.assertEqual(term.render_shared(), expected)

    def test_structural_cse_preserves_argument_order_and_exact_leaves(self):
        left = smt.symbol("left", smt.INT)
        right = smt.symbol("right", smt.INT)
        cases = (
            (
                smt.add(left, right),
                smt.add(
                    smt.symbol("right", smt.INT),
                    smt.symbol("left", smt.INT),
                ),
                "(* (+ left right) (+ right left))",
            ),
            (
                smt.add(left, right),
                smt.add(
                    smt.symbol("left", smt.INT),
                    smt.symbol("other", smt.INT),
                ),
                "(* (+ left right) (+ left other))",
            ),
        )

        for first, second, expected in cases:
            with self.subTest(expected=expected):
                self.assertNotEqual(first, second)
                self.assertEqual(
                    smt.mul(first, second).render_shared(),
                    expected,
                )

    def test_structural_cse_separates_deep_exact_hash_collisions(self):
        first = _deep_shared_term(smt.int_value(-1), depth=256)
        second = _deep_shared_term(smt.int_value(-2), depth=256)
        term = smt.add(first, first, second, second)

        self.assertEqual(hash(first), hash(second))
        self.assertNotEqual(first, second)
        rendered = term.render_shared()
        self.assertEqual(rendered.count("(let ("), 256)
        self.assertEqual(rendered.count("(deep "), 512)
        self.assertIn(
            "(rbo_let_0 (deep (- 1) (- 1))) "
            "(rbo_let_1 (deep (- 2) (- 2)))",
            rendered,
        )
        self.assertIn(
            "(+ rbo_let_510 rbo_let_510 "
            "rbo_let_511 rbo_let_511)",
            rendered,
        )

    def test_structural_cse_keeps_foreign_function_owners_distinct(self):
        first_script = smt.Script()
        second_script = smt.Script()
        first_function = first_script.fresh_function(
            "first owner",
            (smt.INT,),
            smt.INT,
        )
        second_function = second_script.fresh_function(
            "second owner",
            (smt.INT,),
            smt.INT,
        )
        first = first_function(smt.ONE)
        second = second_function(smt.ONE)

        self.assertEqual(first.operation, second.operation)
        self.assertNotEqual(first, second)
        self.assertEqual(
            smt.add(first, first, second, second).render_shared(),
            "(let ((rbo_let_0 (f_0 1)) (rbo_let_1 (f_0 1))) "
            "(+ rbo_let_0 rbo_let_0 rbo_let_1 rbo_let_1))",
        )

    def test_structural_alias_order_is_dependency_then_discovery_order(self):
        first_product = smt.mul(
            smt.add(
                smt.symbol("a", smt.INT),
                smt.symbol("b", smt.INT),
            ),
            smt.add(
                smt.symbol("a", smt.INT),
                smt.symbol("b", smt.INT),
            ),
        )
        second_product = smt.mul(
            smt.add(
                smt.symbol("a", smt.INT),
                smt.symbol("b", smt.INT),
            ),
            smt.add(
                smt.symbol("a", smt.INT),
                smt.symbol("b", smt.INT),
            ),
        )
        first_difference = smt.sub(
            smt.symbol("c", smt.INT),
            smt.symbol("d", smt.INT),
        )
        second_difference = smt.sub(
            smt.symbol("c", smt.INT),
            smt.symbol("d", smt.INT),
        )
        term = smt.add(
            first_product,
            second_product,
            first_difference,
            second_difference,
        )
        expected = (
            "(let ((rbo_let_0 (+ a b)) (rbo_let_1 (- c d))) "
            "(let ((rbo_let_2 (* rbo_let_0 rbo_let_0))) "
            "(+ rbo_let_2 rbo_let_2 rbo_let_1 rbo_let_1)))"
        )

        self.assertEqual(term.render_shared(), expected)
        self.assertEqual(term.render_shared(), expected)

    def test_deep_unshared_term_renders_without_python_recursion(self):
        term = smt.symbol("leaf", smt.INT)
        for _ in range(2_000):
            term = smt.Term(smt.INT, "step", (term,))

        rendered = term.render_shared()

        self.assertEqual(rendered, term.render_shared())
        self.assertEqual(rendered.count("(step "), 2_000)
        self.assertTrue(rendered.startswith("(step (step "))
        self.assertTrue(rendered.endswith(")" * 2_000))

    def test_deep_shared_quantified_dag_renders_stack_safely(self):
        rank = smt.symbol("rank", smt.INT)
        shared = _deep_shared_term(rank)
        quantified = smt.exists(
            (rank,),
            smt.lt(shared, smt.ONE),
        )

        rendered = quantified.render_shared()

        self.assertEqual(rendered, quantified.render_shared())
        self.assertEqual(rendered.count("(let ("), 1_999)
        self.assertTrue(rendered.startswith(
            "(exists ((rank Int)) "
            "(let ((rbo_let_0 (deep rank rank))) ",
        ))
        self.assertIn("rbo_let_1998", rendered)

    def test_deep_independently_equal_dags_share_stack_safely(self):
        first = _deep_shared_term(smt.symbol("leaf", smt.INT))
        second = _deep_shared_term(smt.symbol("leaf", smt.INT))
        term = smt.add(first, second)

        rendered = term.render_shared()

        self.assertEqual(rendered, term.render_shared())
        self.assertEqual(rendered.count("(let ("), 2_000)
        self.assertEqual(rendered.count("(deep "), 2_000)
        self.assertTrue(rendered.startswith(
            "(let ((rbo_let_0 (deep leaf leaf))) ",
        ))
        self.assertIn("(+ rbo_let_1999 rbo_let_1999)", rendered)

    def test_shared_quantified_body_is_not_hoisted_past_shadowing(self):
        rank = smt.symbol("rank", smt.INT)
        successor = smt.add(rank, smt.ONE)
        body = smt.lt(successor, smt.add(successor, smt.ONE))
        quantified = smt.exists((rank,), body)

        self.assertEqual(
            quantified.render_shared(),
            "(exists ((rank Int)) "
            "(let ((rbo_let_0 (+ rank 1))) "
            "(< rbo_let_0 (+ rbo_let_0 1))))",
        )

    def test_structural_cse_stays_inside_global_and_shadowed_scopes(self):
        rank = smt.symbol("rank", smt.INT)
        term = smt.and_(
            _structurally_repeated_successor_comparison("rank"),
            smt.exists(
                (rank,),
                _structurally_repeated_successor_comparison("rank"),
            ),
        )

        self.assertEqual(
            term.render_shared(),
            "(let ((rbo_let_0 (+ rank 1))) "
            "(and (< rbo_let_0 (+ rbo_let_0 1)) "
            "(exists ((rank Int)) "
            "(let ((rbo_let_1 (+ rank 1))) "
            "(< rbo_let_1 (+ rbo_let_1 1))))))",
        )

    def test_sibling_quantifiers_have_separate_structural_cse_scopes(self):
        rank = smt.symbol("rank", smt.INT)
        term = smt.and_(
            smt.exists(
                (rank,),
                _structurally_repeated_successor_comparison("rank"),
            ),
            smt.forall(
                (smt.symbol("rank", smt.INT),),
                _structurally_repeated_successor_comparison("rank"),
            ),
        )

        self.assertEqual(
            term.render_shared(),
            "(and (exists ((rank Int)) "
            "(let ((rbo_let_0 (+ rank 1))) "
            "(< rbo_let_0 (+ rbo_let_0 1)))) "
            "(forall ((rank Int)) "
            "(let ((rbo_let_1 (+ rank 1))) "
            "(< rbo_let_1 (+ rbo_let_1 1)))))",
        )

    def test_nested_quantifiers_have_separate_structural_cse_scopes(self):
        rank = smt.symbol("rank", smt.INT)
        term = smt.forall(
            (rank,),
            smt.and_(
                _structurally_repeated_successor_comparison("rank"),
                smt.exists(
                    (smt.symbol("rank", smt.INT),),
                    _structurally_repeated_successor_comparison("rank"),
                ),
            ),
        )

        self.assertEqual(
            term.render_shared(),
            "(forall ((rank Int)) "
            "(let ((rbo_let_0 (+ rank 1))) "
            "(and (< rbo_let_0 (+ rbo_let_0 1)) "
            "(exists ((rank Int)) "
            "(let ((rbo_let_1 (+ rank 1))) "
            "(< rbo_let_1 (+ rbo_let_1 1)))))))",
        )

    def test_defined_function_parameters_have_local_structural_cse_scopes(self):
        def body(parameters):
            parameter = parameters[0]
            assert isinstance(parameter.atom, str)
            first = smt.add(parameter, smt.ONE)
            second = smt.add(
                smt.symbol(parameter.atom, parameter.sort),
                smt.int_value(1),
            )
            return smt.lt(first, smt.add(second, smt.ONE))

        script = smt.Script()
        first = script.fresh_defined_function(
            "first comparison",
            (smt.INT,),
            smt.BOOL,
            body,
        )
        second = script.fresh_defined_function(
            "second comparison",
            (smt.INT,),
            smt.BOOL,
            body,
        )
        script.assert_term(smt.and_(first(smt.ZERO), second(smt.ZERO)))

        formula = script.render()

        self.assertIn(
            "(define-fun df_0 ((df_0_p0 Int)) Bool "
            "(let ((rbo_let_0 (+ df_0_p0 1))) "
            "(< rbo_let_0 (+ rbo_let_0 1))))",
            formula,
        )
        self.assertIn(
            "(define-fun df_1 ((df_1_p0 Int)) Bool "
            "(let ((rbo_let_0 (+ df_1_p0 1))) "
            "(< rbo_let_0 (+ rbo_let_0 1))))",
            formula,
        )
        self.assertEqual(formula.count("(let ((rbo_let_0 "), 2)

    @unittest.skipUnless(SOLVER, "run through ya or set RBO_Z3")
    def test_z3_observes_global_and_shadowed_structural_cse(self):
        script = smt.Script(timeout_ms=10_000)
        rank = script.fresh_constant("global rank", smt.INT)
        global_value = smt.eq(
            smt.add(rank, smt.ONE),
            smt.ONE,
        )
        global_copy = smt.eq(
            smt.add(smt.symbol("v_0", smt.INT), smt.int_value(1)),
            smt.int_value(1),
        )
        local_value = smt.eq(
            smt.add(rank, smt.ONE),
            smt.int_value(2),
        )
        local_copy = smt.eq(
            smt.add(smt.symbol("v_0", smt.INT), smt.int_value(1)),
            smt.int_value(2),
        )
        script.assert_term(smt.and_(
            global_value,
            global_copy,
            smt.exists((rank,), smt.and_(local_value, local_copy)),
        ))

        solved = subprocess.run(
            (SOLVER, "-in"),
            input=script.render(),
            text=True,
            capture_output=True,
            check=False,
            timeout=15,
        )

        self.assertEqual(solved.returncode, 0, solved.stderr)
        self.assertEqual(solved.stdout.strip(), "sat")

    @unittest.skipUnless(SOLVER, "run through ya or set RBO_Z3")
    def test_z3_preserves_independent_structural_cse_arithmetic(self):
        script = smt.Script(timeout_ms=10_000)
        left = script.fresh_constant("left", smt.INT)
        right = script.fresh_constant("right", smt.INT)
        first = smt.add(left, right)
        second = smt.add(
            smt.symbol("v_0", smt.INT),
            smt.symbol("v_1", smt.INT),
        )
        third = smt.add(
            smt.symbol("v_0", smt.INT),
            smt.symbol("v_1", smt.INT),
        )
        script.assert_term(smt.not_(smt.eq(
            smt.add(first, second),
            smt.mul(smt.int_value(2), third),
        )))

        formula = script.render()
        self.assertIn(
            "(let ((rbo_let_0 (+ v_0 v_1)))",
            formula,
        )
        solved = subprocess.run(
            (SOLVER, "-in"),
            input=formula,
            text=True,
            capture_output=True,
            check=False,
            timeout=15,
        )

        self.assertEqual(solved.returncode, 0, solved.stderr)
        self.assertEqual(solved.stdout.strip(), "unsat")

    @unittest.skipUnless(SOLVER, "run through ya or set RBO_Z3")
    def test_z3_preserves_identity_fallback_arithmetic(self):
        script = smt.Script(timeout_ms=10_000)
        left = script.fresh_constant("left", smt.INT)
        right = script.fresh_constant("right", smt.INT)
        first = smt.add(left, right)
        second = smt.add(
            smt.symbol("v_0", smt.INT),
            smt.symbol("v_1", smt.INT),
        )
        shared_product = smt.mul(left, right)
        actual = smt.add(first, second, shared_product, shared_product)
        expected_sum = smt.add(
            smt.symbol("v_0", smt.INT),
            smt.symbol("v_1", smt.INT),
        )
        expected_product = smt.mul(
            smt.symbol("v_0", smt.INT),
            smt.symbol("v_1", smt.INT),
        )
        expected = smt.add(
            smt.mul(smt.int_value(2), expected_sum),
            smt.mul(smt.int_value(2), expected_product),
        )
        script.assert_term(smt.not_(smt.eq(actual, expected)))

        with mock.patch.object(
            smt,
            "_MAX_STRUCTURAL_CSE_SCOPE_NODES",
            1,
        ):
            formula = script.render()
            self.assertEqual(script.render(), formula)
        self.assertIn(
            "(let ((rbo_let_0 (* v_0 v_1))) "
            "(not (= (+ (+ v_0 v_1) (+ v_0 v_1) "
            "rbo_let_0 rbo_let_0)",
            formula,
        )
        solved = subprocess.run(
            (SOLVER, "-in"),
            input=formula,
            text=True,
            capture_output=True,
            check=False,
            timeout=15,
        )

        self.assertEqual(solved.returncode, 0, solved.stderr)
        self.assertEqual(solved.stdout.strip(), "unsat")

    @unittest.skipUnless(SOLVER, "run through ya or set RBO_Z3")
    def test_z3_observes_product_selectors_in_exact_definition(self):
        script = smt.Script(timeout_ms=10_000)
        row = script.fresh_product_sort(
            "sorting row",
            (smt.BOOL, smt.INT),
        )
        before = script.fresh_defined_function(
            "live row precedes rank",
            (row, smt.INT),
            smt.BOOL,
            lambda parameters: smt.and_(
                row.select(parameters[0], 0),
                smt.lt(row.select(parameters[0], 1), parameters[1]),
            ),
        )
        script.assert_term(
            smt.and_(
                before(
                    row.pack(smt.TRUE, smt.int_value(4)),
                    smt.int_value(5),
                ),
                smt.not_(
                    before(
                        row.pack(smt.FALSE, smt.int_value(4)),
                        smt.int_value(5),
                    )
                ),
                smt.not_(
                    before(
                        row.pack(smt.TRUE, smt.int_value(6)),
                        smt.int_value(5),
                    )
                ),
            )
        )

        solved = subprocess.run(
            (SOLVER, "-in"),
            input=script.render(),
            text=True,
            capture_output=True,
            check=False,
            timeout=15,
        )

        self.assertEqual(solved.returncode, 0, solved.stderr)
        self.assertEqual(solved.stdout.strip(), "sat")

    @unittest.skipUnless(SOLVER, "run through ya or set RBO_Z3")
    def test_string_capacity_covers_the_product_of_choice_domains(self):
        script = smt.Script(timeout_ms=10_000)
        first = script.fresh_constant("first choice", smt.INT)
        second = script.fresh_constant("second choice", smt.INT)
        function = script.fresh_function(
            "choice string",
            (smt.INT, smt.INT),
            smt.INT,
        )
        script.register_quantified_choice(first, 2)
        script.register_quantified_choice(second, 3)
        script.register_string_term(function(first, second))
        applications = tuple(
            function(smt.int_value(left), smt.int_value(right))
            for left in range(2)
            for right in range(3)
        )
        for left, right in combinations(applications, 2):
            script.assert_term(smt.not_(smt.eq(left, right)))

        solved = subprocess.run(
            (SOLVER, "-in"),
            input=script.render(),
            text=True,
            capture_output=True,
            check=False,
            timeout=15,
        )

        self.assertEqual(solved.returncode, 0, solved.stderr)
        self.assertEqual(solved.stdout.strip(), "sat")
        self.assertEqual(len(script.string_literals), 6)

    def test_let_aliases_are_hygienic(self):
        collision = smt.symbol("rbo_let_0", smt.INT)
        first = smt.add(collision, smt.ONE)
        second = smt.add(
            smt.symbol("rbo_let_0", smt.INT),
            smt.int_value(1),
        )

        self.assertEqual(
            smt.add(first, second).render_shared(),
            "(let ((rbo_let_1 (+ rbo_let_0 1))) "
            "(+ rbo_let_1 rbo_let_1))",
        )

    def test_zero_variable_quantifier_is_the_body(self):
        body = smt.symbol("condition", smt.BOOL)
        self.assertIs(smt.forall((), body), body)
        self.assertIs(smt.exists((), body), body)

    def test_quantifier_rejects_invalid_or_duplicate_binders(self):
        value = smt.symbol("value", smt.INT)
        predicate = smt.Function("predicate", (smt.INT,), smt.BOOL)(value)
        invalid_sort = smt.Term("Real", "symbol", atom="real_value")
        same_name_other_sort = smt.symbol("value", smt.BOOL)

        for variables in (
            (smt.int_value(1),),
            (predicate,),
            (invalid_sort,),
            (value, value),
            (value, same_name_other_sort),
        ):
            with self.subTest(variables=variables):
                with self.assertRaises(smt.SmtError):
                    smt.forall(variables, smt.TRUE)
        with self.assertRaises(smt.SmtError):
            smt.forall((value,), smt.ONE)


if __name__ == "__main__":
    unittest.main()
