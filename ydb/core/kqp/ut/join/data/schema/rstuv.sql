CREATE TABLE `/Root/R` (
    id Int32 not null,
    payload1 String,
    ts Date,
    PRIMARY KEY (id)
);

CREATE TABLE `/Root/S` (
    id Int32 not null,
    payload2 String,
    PRIMARY KEY (id)
);

CREATE TABLE `/Root/T` (
    id Int32 not null,
    payload3 String,
    PRIMARY KEY (id)
);

CREATE TABLE `/Root/U` (
    id Int32 not null,
    payload4 String,
    PRIMARY KEY (id)
);

CREATE TABLE `/Root/V` (
    id Int32 not null,
    payload5 String,
    PRIMARY KEY (id)
);
