// Auto-generated by {{ generator }}, do not modify.
// Driven by op_handler_overrides.yaml.
#pragma once

#include <ydb/core/tx/schemeshard/schemeshard__operation_part.h>

#include <ydb/core/protos/flat_scheme_op.pb.h>

#include <util/generic/string.h>
#include <util/generic/vector.h>

#include <optional>
#include <type_traits>

namespace NKikimr::NSchemeShard {

struct TOperation;
struct TOperationContext;

namespace NGenerated::NOpHandlers {

template <NKikimrSchemeOp::EOperationType Op>
struct IsRegistered : std::false_type {};

{% for op in migratedOps %}
template <>
struct IsRegistered<NKikimrSchemeOp::{{op}}> : std::true_type {};
{% endfor %}

template <NKikimrSchemeOp::EOperationType Op>
inline constexpr bool IsRegistered_v = IsRegistered<Op>::value;

{% for op in migratedOps %}
TVector<ISubOperation::TPtr> MakeOperationParts_{{op}}(
    const TOperation&, const NKikimrSchemeOp::TModifyScheme&, TOperationContext&);

void CollectChangingPaths_{{op}}(
    const NKikimrSchemeOp::TModifyScheme&, TVector<TString>&);
{% endfor %}

inline std::optional<TVector<ISubOperation::TPtr>> TryMakeOperationParts(
    NKikimrSchemeOp::EOperationType opType,
    const TOperation& op,
    const NKikimrSchemeOp::TModifyScheme& tx,
    TOperationContext& context)
{
    switch (opType) {
{% for op in migratedOps %}
    case NKikimrSchemeOp::{{op}}:
        return MakeOperationParts_{{op}}(op, tx, context);
{% endfor %}
    default:
        return std::nullopt;
    }
}

inline std::optional<TVector<TString>> TryCollectChangingPaths(
    const NKikimrSchemeOp::TModifyScheme& tx)
{
    TVector<TString> result;
    switch (tx.GetOperationType()) {
{% for op in migratedOps %}
    case NKikimrSchemeOp::{{op}}:
        CollectChangingPaths_{{op}}(tx, result);
        return result;
{% endfor %}
    default:
        return std::nullopt;
    }
}

// Runtime mirror of IsRegistered<>::value, usable in both runtime contexts
// (ss_tool walking the proto enum) and constexpr contexts (static_assert in
// tests). Single entry point for "is this op migrated?"
inline constexpr bool IsOpRegistered(NKikimrSchemeOp::EOperationType opType) {
    switch (opType) {
{% for op in migratedOps %}
    case NKikimrSchemeOp::{{op}}:
        return true;
{% endfor %}
    default:
        return false;
    }
}

} // namespace NGenerated::NOpHandlers
} // namespace NKikimr::NSchemeShard

// Case labels for every migrated op, for use in legacy switches that retain
// a fall-through path. The dispatch (TryMakeOperationParts /
// TryCollectChangingPaths) returns early for these ops, so reaching the
// switch body for them is a control-flow bug — but the labels are needed
// to silence -Wswitch.
//
// Usage:
//   switch (tx.GetOperationType()) {
//   case ESchemeOpMkDir: ...; break;
//   ...
//   Y_SS_OP_HANDLERS_MIGRATED_CASES break;
//   }
#define Y_SS_OP_HANDLERS_MIGRATED_CASES{% for op in migratedOps %} \
    case ::NKikimrSchemeOp::EOperationType::{{op}}:{% endfor %}

