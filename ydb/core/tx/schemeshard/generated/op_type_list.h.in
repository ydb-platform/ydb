// Auto-generated by {{ generator }}, do not modify.
#pragma once

#include <ydb/core/protos/flat_scheme_op.pb.h>

// Every EOperationType value, straight from the enum descriptor. Unlike DispatchOp this
// carries no default: label, so it can be used to assert at compile time that a table
// keyed by operation type covers the whole enum.
#define SS_FOR_EACH_OP_TYPE(X) \
{% for opType in opTypes %}    X({{ opType }}) \
{% endfor %}    /* end */
