# This file was generated by the YaTool build system (https://github.com/yandex/yatool),
# from a source YaTool build configuration provided in ya.make files.
#
# If the repository supports both CMake and ya build configurations, please modify both of them.
#
# If only CMake build configuration is supported then modify only CMake files and note that only
# simple modifications are allowed like adding source-files to targets or adding simple properties
# like target_include_directories. These modifications will be ported to original ya.make files
# by maintainers. Any complex modifications which can't be easily ported back to the ya build
# system may be rejected.
#
# Please refer to the build instructions in the repository for more information about manual
# changes in this file.

add_executable(ydb-core-tx-schemeshard-ut_data_erasure_reboots)


target_compile_options(ydb-core-tx-schemeshard-ut_data_erasure_reboots PRIVATE
  -DUSE_CURRENT_UDF_ABI_VERSION
)

target_include_directories(ydb-core-tx-schemeshard-ut_data_erasure_reboots PRIVATE
  ${PROJECT_SOURCE_DIR}/ydb/core/tx/schemeshard
)

target_link_libraries(ydb-core-tx-schemeshard-ut_data_erasure_reboots PUBLIC
  contrib-libs-cxxsupp
  yutil
  library-cpp-cpuid_check
  cpp-testing-unittest_main
  core-tx-schemeshard
  library-cpp-getopt
  cpp-regex-pcre
  ydb-core-cms
  core-testlib-default
  ydb-core-tx
  tx-schemeshard-ut_helpers
  core-wrappers-ut_helpers
)

target_allocator(ydb-core-tx-schemeshard-ut_data_erasure_reboots
  system_allocator
)

target_link_options(ydb-core-tx-schemeshard-ut_data_erasure_reboots PRIVATE
  -Wl,-platform_version,macos,11.0,11.0
  -fPIC
  -fPIC
  -framework
  CoreFoundation
)

target_sources(ydb-core-tx-schemeshard-ut_data_erasure_reboots PRIVATE
  ${PROJECT_SOURCE_DIR}/ydb/core/tx/schemeshard/ut_data_erasure_reboots/ut_data_erasure_reboots.cpp
)


set_property(
  TARGET
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  PROPERTY
  SPLIT_FACTOR
  10
)

add_yunittest(
  NAME
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  TEST_TARGET
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  TEST_ARG
  --print-before-suite
  --print-before-test
  --fork-tests
  --print-times
  --show-fails
)

set_yunittest_property(
  TEST
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  PROPERTY
  LABELS
  LARGE
)

set_yunittest_property(
  TEST
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  PROPERTY
  ENVIRONMENT
)

set_yunittest_property(
  TEST
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  PROPERTY
  TIMEOUT
  900
)

vcs_info(ydb-core-tx-schemeshard-ut_data_erasure_reboots)

set_yunittest_property(
  TEST
  ydb-core-tx-schemeshard-ut_data_erasure_reboots
  PROPERTY
  PROCESSORS
  1
)
