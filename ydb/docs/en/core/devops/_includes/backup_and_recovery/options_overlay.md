* Backup to files and recovery using the {{ ydb-short-name }} CLI.
* Backup to S3-compatible storage and recovery using the {{ ydb-short-name }} CLI.
* Backup of system tablets to the local file system of cluster hosts.
