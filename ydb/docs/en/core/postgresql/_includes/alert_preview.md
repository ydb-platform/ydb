{% note warning %}

PostgreSQL syntax support in {{ ydb-name }} is **under development**. Using it in production environments is **not recommended**. PostgreSQL queries may execute several times slower compared to similar YQL queries.

The main scenario that can be tested is running analytical queries against data stored in {{ ydb-name }}.

{% endnote %}
