
{% note warning %}

At the moment, {{ ydb-short-name }}'s compatibility with PostgreSQL **is under development**, so not all PostgreSQL constructs and [functions](../functions.md) are supported yet. PostgreSQL syntax compatibility is available for testing in the Docker image described in [these instructions](../docker-connect.md).

{% endnote %}
