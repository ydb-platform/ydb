
{% note warning %}

At the moment, {{ ydb-short-name }}'s compatibility with PostgreSQL **is under development**, so not all PostgreSQL constructs and [functions](../functions.md) are supported yet. The PostgreSQL wire protocol listener (pgwire) is disabled by default.

{% endnote %}
