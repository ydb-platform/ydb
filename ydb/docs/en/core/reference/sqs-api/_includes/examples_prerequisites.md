Before running the examples, configure access to the SQS API (in the Open Source version — disable the authentication requirement, see the [authentication](../auth.md) section).

If you use AWS CLI, make sure it does not require configured AWS credentials: either add `--no-sign-request` to commands or set any values for `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`.
