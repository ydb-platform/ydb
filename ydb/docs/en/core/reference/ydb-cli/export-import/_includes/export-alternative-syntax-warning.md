{% note warning %}

Exports made using the alternate syntax will not contain a list of objects as part of the backup, so some features may not be available for them (such as encrypted backups), and imports are possible only using the corresponding alternate syntax of import.

{% endnote %}
