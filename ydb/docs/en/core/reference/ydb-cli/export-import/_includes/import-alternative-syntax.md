For backward compatibility, an alternate syntax is supported to specify the list of objects:

`--item STRING`: Description of the item to import. You can specify the `--item` parameter multiple times if you need to import several items. If neither `--item` nor `--include` is specified, all objects present in the specified export are imported. `STRING` is set in `<property>=<value>,...` format with the following mandatory properties:
