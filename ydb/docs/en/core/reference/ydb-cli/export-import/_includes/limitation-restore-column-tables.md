{% note warning %}

Currently, this command does not restore column table data (such data is not included in the backup). To import data into column tables, you can use [external data sources](../../../../concepts/datamodel/external_data_source.md). For more information, see [{#T}](../../../../concepts/query_execution/federated_query/s3/external_table.md).

{% endnote %}
