{% note warning %}

Currently, this command does not restore column table data. To import column tables, use the [`import s3`](../import-s3.md) command with a backup created by [`export s3`](../export-s3.md).

{% endnote %}
