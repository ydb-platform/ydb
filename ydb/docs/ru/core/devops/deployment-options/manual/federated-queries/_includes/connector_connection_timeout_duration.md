Значение должно состоять из числового литерала и суффикса, обозначающего единицы измерения (наносекунды `ns`, микросекунды `us`, миллисекунды `ms`, секунды `s`, минуты `m`, часы `h`). Примеры значений: `100ms`, `5s`, `3h`.