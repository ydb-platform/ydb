#include "markov_model_evaluator.h"

#include <library/cpp/streams/factory/open_by_signature/factory.h>

#include <util/stream/file.h>
#include <util/string/cast.h>
#include <util/string/split.h>

namespace NYdbWorkload {
    TMarkovModelEvaluator TMarkovModelEvaluator::LoadFromFile(const TString& path) {
        TMarkovModelEvaluator mc;
        auto input = OpenOwnedMaybeCompressedInput(MakeHolder<TFileInput>(path));
        TString line;
        while (input->ReadLine(line)) {
            if (line.StartsWith("#")) {
                TStringBuf buf(line);
                buf.SkipPrefix("#");
                TStringBuf key, value;
                if (buf.TrySplit('=', key, value) && key == "order") {
                    TryFromString(value, mc.Order);
                }
                continue;
            }
            TVector<TStringBuf> parts;
            StringSplitter(line).Split('\t').SkipEmpty().Collect(&parts);
            if (parts.size() < 2) {
                continue;
            }
            TString key(parts[0]);
            auto& trans = mc.Chain[key];
            for (size_t i = 1; i < parts.size(); ++i) {
                TStringBuf entry = parts[i];
                size_t colonPos = entry.rfind(':');
                if (colonPos == TStringBuf::npos) {
                    continue;
                }
                TString successor(entry.SubStr(0, colonPos));
                int weight = 0;
                if (!TryFromString(entry.SubStr(colonPos + 1), weight) || weight <= 0) {
                    continue;
                }
                trans.Successors.push_back({std::move(successor), weight});
                trans.TotalWeight += weight;
            }
        }
        if (mc.Chain.empty()) {
            ythrow yexception() << "Markov chain model loaded from '" << path
                                << "' is empty. Make sure the file is in TSV.GZ format "
                                << "generated by 'ydb workload fulltext model'.";
        }
        if (mc.Order < 1) {
            ythrow yexception() << "Markov chain model loaded from '" << path
                                << "' has invalid order=" << mc.Order << ". Expected order >= 1.";
        }
        if (mc.Order == 1) {
            for (const auto& [key, _] : mc.Chain) {
                if (!key.StartsWith(TString(START_TOKEN))) {
                    continue;
                }
                TVector<TStringBuf> words;
                StringSplitter(key).Split(' ').SkipEmpty().Collect(&words);
                bool allStart = true;
                for (const auto& w : words) {
                    if (w != START_TOKEN) {
                        allStart = false;
                        break;
                    }
                }
                if (allStart && !words.empty()) {
                    mc.Order = static_cast<int>(words.size());
                    break;
                }
            }
        }
        return mc;
    }

    TString TMarkovModelEvaluator::NextWord(const TString& contextKey, std::mt19937& rng) const {
        auto it = Chain.find(contextKey);
        if (it == Chain.end() || it->second.Successors.empty()) {
            return TString(END_TOKEN);
        }
        const auto& trans = it->second;
        std::uniform_int_distribution<int> dist(1, trans.TotalWeight);
        int roll = dist(rng);
        for (const auto& s : trans.Successors) {
            roll -= s.Weight;
            if (roll <= 0) {
                return s.Word;
            }
        }
        return trans.Successors.back().Word;
    }

    TString TMarkovModelEvaluator::MakeKey(const TDeque<TString>& context) {
        TStringBuilder sb;
        bool first = true;
        for (const TString& w : context) {
            if (!first) {
                sb << " ";
            }
            first = false;
            sb << w;
        }
        return sb;
    }

    TString TMarkovModelEvaluator::GenerateSentence(size_t targetLen, std::mt19937& rng) const {
        TDeque<TString> context;
        for (int i = 0; i < Order; ++i) {
            context.push_back(TString(START_TOKEN));
        }

        TStringBuilder text;
        size_t wordsWritten = 0;

        while (wordsWritten < targetLen) {
            TString key = MakeKey(context);
            TString next = NextWord(key, rng);
            if (next == END_TOKEN || next == START_TOKEN) {
                break;
            }
            if (wordsWritten > 0) {
                text << " ";
            }
            text << next;
            ++wordsWritten;
            context.pop_front();
            context.push_back(next);
        }
        return text;
    }

} // namespace NYdbWorkload
