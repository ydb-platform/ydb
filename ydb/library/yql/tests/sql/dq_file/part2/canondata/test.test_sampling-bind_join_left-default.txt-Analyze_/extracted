<tmp_path>/program.sql:<main>: Info: DQ cannot execute the query

    <tmp_path>/program.sql:<main>: Info: Optimization
    
        <tmp_path>/program.sql:<main>:11:51: Info: Cannot execute self join in DQ
        	select * from (select a.value, b.value from $a as a inner join Input as b using(subkey)) tablesample bernoulli(25);
	                                                  ^