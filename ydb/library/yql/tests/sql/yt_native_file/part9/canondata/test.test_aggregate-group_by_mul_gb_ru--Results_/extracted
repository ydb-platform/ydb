<tmp_path>/program.sql:<main>: Warning: Type annotation

    <tmp_path>/program.sql:<main>:3:1: Warning: At function: RemovePrefixMembers, At function: Sort, At function: UnionAll, At function: PersistableRepr, At function: SqlProject, At function: AssumeColumnOrderPartial, At function: Aggregate, At function: FlatMap, At function: AsList, At function: AddMember, At function: ForceRemoveMember, At function: AddMember
    	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	^
        <tmp_path>/program.sql:<main>:3:1: Warning: At function: ForceRemoveMember, At function: AddMember
        	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	^
            <tmp_path>/program.sql:<main>:3:182: Warning: At function: /
            	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	                                                                                                                                                                                     ^
                <tmp_path>/program.sql:<main>:3:182: Warning: Integral type implicit bitcast: Optional<Uint32> and Int32
                	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	                                                                                                                                                                                     ^
        <tmp_path>/program.sql:<main>:3:150: Warning: At function: %
        	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	                                                                                                                                                     ^
            <tmp_path>/program.sql:<main>:3:150: Warning: Integral type implicit bitcast: Optional<Uint32> and Int32
            	select sum(length(value)), vf, kf, kl, grouping(vf, kf, kl) as ggg3 from plato.Input group by Substring(value, 0, 1) as vf, cube(cast(key as uint32) % 10 as kl, cast(key as uint32) / 100 as kf)
	                                                                                                                                                     ^