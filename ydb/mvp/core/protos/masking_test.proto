syntax = "proto3";

package mvp.masking;

import "ydb/public/api/client/nc_private/annotations.proto";

message TestMask {
  string name = 1;
  string no_value = 2; // field doesn't set for testing
  string cred = 3 [(nebius.credentials) = true];
  string sens = 4 [(nebius.sensitive) = true];
}
