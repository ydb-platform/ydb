table_service_config:
  sql_version: 1
  index_auto_choose_mode: MAX_USED_PREFIX
  enable_kqp_data_query_stream_lookup: true

feature_flags:
  enable_views: true

domains_config:
  security_config:
    default_users:
    - name: user
      password: 'P@ssw0rd'
