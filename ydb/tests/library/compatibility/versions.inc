IF(NOT ${YDB_COMPAT_INIT_REF})
    SET(YDB_COMPAT_INIT_REF stable-26-1-1)
ENDIF()
IF(NOT ${YDB_COMPAT_INTER_REF})
    SET(YDB_COMPAT_INTER_REF stable-26-2-1)
ENDIF()
IF(NOT ${YDB_COMPAT_TARGET_REF})
    SET(YDB_COMPAT_TARGET_REF prestable-26-3)
ENDIF()
