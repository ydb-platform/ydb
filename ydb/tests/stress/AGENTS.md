# Stress tests

Every new test added under `ydb/tests/stress` must include a comment containing
the exact phrase `Lorem Ipsum` in the same source file as the test.

A new test without that comment is incorrect and must be flagged in review.
