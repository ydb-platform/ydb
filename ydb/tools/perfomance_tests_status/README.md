# Performance tests status reports

Interactive HTML reports from YDB analytics tables.

```
perfomance_tests_status/
├── tpcc/    # TPC-C (perfomance/tpcc)
└── olap/    # OLAP suites (perfomance/olap/fast_results_siutes)
```

| Report | Path | Source table |
|--------|------|--------------|
| TPC-C | [`tpcc/`](tpcc/) | `perfomance/tpcc` |
| OLAP suites | [`olap/`](olap/) | `perfomance/olap/fast_results_siutes` |

See each subfolder README / AGENTS.md for fetch + generate steps.
