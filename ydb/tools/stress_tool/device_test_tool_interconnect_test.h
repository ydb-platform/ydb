#pragma once

#include "defs.h"

#include <ydb/core/base/blobstorage.h>
#include <ydb/core/base/counters.h>
#include <ydb/core/base/tabletid.h>

#include <ydb/core/load_test/service_actor.h>

#include <ydb/core/tablet/bootstrapper.h>

#include <ydb/library/services/services.pb.h>
#include <ydb/library/actors/protos/services_common.pb.h>

#include <ydb/library/actors/core/actor_bootstrapped.h>
#include <ydb/library/actors/core/event_local.h>
#include <ydb/library/actors/core/events.h>
#include <ydb/library/actors/core/executor_pool_basic.h>
#include <ydb/library/actors/core/executor_pool_io.h>
#include <ydb/library/actors/core/hfunc.h>
#include <ydb/library/actors/core/log.h>
#include <ydb/library/actors/core/mon.h>
#include <ydb/library/actors/core/scheduler_basic.h>
#include <ydb/library/actors/interconnect/interconnect.h>
#include <ydb/library/actors/interconnect/load.h>

#include <util/generic/string.h>
#include <util/string/printf.h>
#include <util/system/event.h>

#include "device_test_tool.h"

namespace NKikimr {

static TSystemEvent InterconnectDoneEvent(TSystemEvent::rAuto);
static TSystemEvent InterconnectResultsPrintedEvent(TSystemEvent::rAuto);

// Runs a sequence of interconnect load tests (NKikimr::TEvLoadTestRequest::TInterconnectLoad)
// inside a single actor system, one after another, printing a short summary for each.
//
// The actual throughput/RTT statistics generated by the load actor are periodically
// logged via NActorsServices::INTERCONNECT_SPEED_TEST at NOTICE level (see
// ydb/library/actors/interconnect/load.cpp PublishResults()); this class only tracks
// completion and prints a compact result row per test for the stress tool's standard
// output formats (wiki/human/json).
class TInterconnectPerfTestActor : public TActor<TInterconnectPerfTestActor> {
    ui64 CurrentTest = 0;
    const TPerfTestConfig& Cfg;
    const NDevicePerfTest::TInterconnectTest& TestProto;
    TIntrusivePtr<IResultPrinter> Printer;
    const TIntrusivePtr<NMonitoring::TDynamicCounters>& Counters;

protected:
    void LaunchTest(const TActorContext& ctx) {
        const auto& record = TestProto.GetInterconnectTestList(CurrentTest);
        if (record.Command_case() != NKikimr::TEvLoadTestRequest::CommandCase::kInterconnectLoad) {
            Cerr << "Unknown load type in InterconnectTestList[" << CurrentTest << "], skipping" << Endl;
            ++CurrentTest;
            if (CurrentTest < TestProto.InterconnectTestListSize()) {
                LaunchTest(ctx);
            } else {
                InterconnectDoneEvent.Signal();
            }
            return;
        }

        ctx.Register(CreateInterconnectLoadTest(record.GetInterconnectLoad(), ctx.SelfID, Counters, CurrentTest));
    }

    void HandleBoot(TEvTablet::TEvBoot::TPtr& ev, const TActorContext& ctx) {
        if (CurrentTest < TestProto.InterconnectTestListSize()) {
            LaunchTest(ctx);
        }
        Y_UNUSED(ev);
    }

    void Handle(TEvLoad::TEvLoadTestFinished::TPtr& ev, const TActorContext& ctx) {
        Y_ABORT_UNLESS(ev);
        Y_ABORT_UNLESS(ev->Get());

        const auto& record = TestProto.GetInterconnectTestList(CurrentTest);
        const auto& cmd = record.GetInterconnectLoad();

        Printer->SetTestType("InterconnectLoad");
        Printer->AddResult("Duration, sec", cmd.GetDurationSeconds());
        Printer->AddResult("DelayBeforeMeasurements, sec", cmd.GetDelayBeforeMeasurementsSeconds());
        Printer->AddResult("InFlyMax", cmd.GetInFlyMax());
        auto size = cmd.GetSizeMin() == cmd.GetSizeMax() ? std::to_string(cmd.GetSizeMin()) : (std::to_string(cmd.GetSizeMin()) + "-" + std::to_string(cmd.GetSizeMax()));
        Printer->AddResult("Size", size);

        const TInterconnectLoadFinishStats* stats = GetInterconnectLoadFinishStats(*ev->Get());
        if (stats && stats->Valid) {
            const double speedMBps = stats->BytesPerSecond / 1e6;
            const double iops = stats->GetIops();
            Printer->AddResult("Speed", Sprintf("%.1f MB/s", speedMBps));
            Printer->AddResult("IOPS", Sprintf("%.0f", iops));
            Printer->AddSpeedAndIops(TSpeedAndIops(speedMBps, iops));
            for (const auto& [q, valueUs] : stats->LatencyPercentilesUs) {
                TString percName = Sprintf("p%.2f", q * 100);
                Printer->AddResult(percName, Sprintf("%zu us", static_cast<size_t>(valueUs)));
            }
        } else {
            Printer->AddResult("Speed", TString("N/A"));
            Printer->AddResult("IOPS", TString("N/A"));
        }

        InterconnectDoneEvent.Signal();
        InterconnectResultsPrintedEvent.Wait();

        ++CurrentTest;
        if (CurrentTest < TestProto.InterconnectTestListSize()) {
            LaunchTest(ctx);
        }
    }

public:
    TInterconnectPerfTestActor(const TPerfTestConfig& perfCfg, const NDevicePerfTest::TInterconnectTest& testProto,
            const TIntrusivePtr<IResultPrinter>& printer, const TIntrusivePtr<NMonitoring::TDynamicCounters>& counters)
        : TActor(&TThis::StateRegister)
        , Cfg(perfCfg)
        , TestProto(testProto)
        , Printer(printer)
        , Counters(counters)
    {
        Y_UNUSED(Cfg);
    }

    STFUNC(StateRegister) {
        switch (ev->GetTypeRewrite()) {
            HFunc(TEvLoad::TEvLoadTestFinished, Handle);
            HFunc(TEvTablet::TEvBoot, HandleBoot);
        }
    }
};

struct TInterconnectTest : public TPerfTest {
    THolder<TActorSystemSetup> Setup;
    TIntrusivePtr<NActors::NLog::TSettings> LogSettings;
    THolder<TActorSystem> ActorSystem;
    TAppData AppData;
    TIntrusivePtr<NMonitoring::TDynamicCounters> Counters;
    yexception LastException;
    volatile bool IsLastExceptionSet = false;
    TActorId TestId;
    const NDevicePerfTest::TInterconnectTest& TestProto;

    // The load actor talks to itself through the interconnect load responder using
    // NodeHops (see cmd.NodeHops in the config); since everything runs in a single
    // process/node, NodeHops should list this node's id (SelfNodeId) so the loopback
    // round trip resolves to the locally registered TLoadResponderMasterActor.
    ui32 SelfNodeId = 1;
    TDuration InitialSleep = TDuration::Seconds(2);

    TInterconnectTest(const TPerfTestConfig& cfg, const NDevicePerfTest::TInterconnectTest& testProto)
        : TPerfTest(cfg)
        , Setup(new TActorSystemSetup())
        , LogSettings(new NActors::NLog::TSettings(NActors::TActorId(1, "logger"),
                                                   NActorsServices::LOGGER,
                                                   NActors::NLog::PRI_ERROR,
                                                   NActors::NLog::PRI_ERROR,
                                                   0))
        , AppData(0 // sysPoolId
                , 1 // userPoolId
                , 3 // ioPoolId
                , 2 // batchPoolId
                , TMap<TString, ui32>() // servicePools
                , nullptr // typeRegistry
                , nullptr // functionRegistry
                , nullptr // formatFactory
                , nullptr) // kikimrShouldContinue
        , TestProto(testProto)
    {
    }

    void DoBasicSetup() {
        Counters = MakeIntrusive<NMonitoring::TDynamicCounters>();

        Setup->NodeId = SelfNodeId;
        Setup->ExecutorsCount = 4;
        Setup->Executors.Reset(new TAutoPtr<IExecutorPool>[4]);
        Setup->Executors[0].Reset(new TBasicExecutorPool(0, 2, 20, "nameservice"));
        Setup->Executors[1].Reset(new TBasicExecutorPool(1, 2, 20, "load_responder"));
        Setup->Executors[2].Reset(new TBasicExecutorPool(2, 4, 20, "perf_actors"));
        Setup->Executors[3].Reset(new TIOExecutorPool(3, 1, "IO"));
        Setup->Scheduler.Reset(new TBasicSchedulerThread(TSchedulerConfig(64, 20)));

        TIntrusivePtr<TTableNameserverSetup> nameserverTable(new TTableNameserverSetup());
        const TActorId nameserviceId = GetNameserviceActorId();
        TActorSetupCmd nameserviceSetup(CreateNameserverTable(nameserverTable), TMailboxType::Simple, 0);
        Setup->LocalServices.push_back(std::pair<TActorId, TActorSetupCmd>(nameserviceId, std::move(nameserviceSetup)));

        // Register the interconnect load responder actor on this node; it acts as the
        // remote endpoint for NodeHops-based loopback load generation (no real network
        // interconnect is required for a single-node run).
        Setup->LocalServices.push_back(std::pair<TActorId, TActorSetupCmd>(
            NInterconnect::MakeLoadResponderActorId(SelfNodeId),
            TActorSetupCmd(NInterconnect::CreateLoadResponderActor(), TMailboxType::ReadAsFilled, 1)));

        /////////////////////// LOGGER ///////////////////////////////////////////////

        LogSettings->Append(
            NActorsServices::EServiceCommon_MIN,
            NActorsServices::EServiceCommon_MAX,
            NActorsServices::EServiceCommon_Name
        );
        LogSettings->Append(
            NKikimrServices::EServiceKikimr_MIN,
            NKikimrServices::EServiceKikimr_MAX,
            NKikimrServices::EServiceKikimr_Name
        );

        TString explanation;
        LogSettings->SetLevel(Cfg.LogLevel, NKikimrServices::BS_LOAD_TEST, explanation);
        // LogSettings->SetLevel(NActors::NLog::PRI_NOTICE, NActorsServices::INTERCONNECT_SPEED_TEST, explanation);

        NActors::TLoggerActor *loggerActor = new NActors::TLoggerActor(LogSettings,
            NActors::CreateStderrBackend(),
            GetServiceCounters(Counters, "utils"));
        NActors::TActorSetupCmd loggerActorCmd(loggerActor, NActors::TMailboxType::Simple, 3);
        Setup->LocalServices.push_back(std::pair<TActorId, TActorSetupCmd>(NActors::TActorId(SelfNodeId, "logger"),
            std::move(loggerActorCmd)));
    }

    void SetupLoadActor() {
        TestId = MakeBlobStorageProxyID(1);
        TActorSetupCmd testSetup(new TInterconnectPerfTestActor(Cfg, TestProto, Printer, Counters),
            TMailboxType::ReadAsFilled, 2);
        Setup->LocalServices.push_back(std::pair<TActorId, TActorSetupCmd>(TestId, std::move(testSetup)));
    }

    void StartActorSystem() {
        ActorSystem.Reset(new TActorSystem(Setup, &AppData, LogSettings));
        ActorSystem->Start();
        Sleep(InitialSleep);
    }

    void Init() override {
        try {
            DoBasicSetup();
            SetupLoadActor();
            StartActorSystem();
        } catch (yexception& ex) {
            IsLastExceptionSet = true;
            VERBOSE_COUT("Error on init state, what# " << ex.what());
        }
    }

    void Run() override {
        if (IsLastExceptionSet) {
            return;
        }

        try {
            ActorSystem->Send(TestId, new TEvTablet::TEvBoot(MakeTabletID(0, 0, 1), 0, nullptr, TActorId(), nullptr));

            for (ui32 i = 0; i < TestProto.InterconnectTestListSize(); ++i) {
                InterconnectDoneEvent.Wait();
                Printer->PrintResults();
                InterconnectResultsPrintedEvent.Signal();
            }
        } catch (yexception ex) {
            LastException = ex;
            IsLastExceptionSet = true;
            VERBOSE_COUT(ex.what());
        }

        if (ActorSystem.Get()) {
            ActorSystem->Stop();
            ActorSystem.Destroy();
        }
        InterconnectDoneEvent.Reset();
        if (IsLastExceptionSet) {
            IsLastExceptionSet = false;
            ythrow LastException;
        }
    }

    void Finish() override {
    }

    ~TInterconnectTest() override {
    }
};

} // namespace NKikimr
