# YDB Benchmark Development

These instructions apply to `ydb/tools/ydb_bench/`.

For implementation, debugging, UI work, or review, read the
[benchmark development skill](.agents/skills/ydb-bench-development/SKILL.md).
Use [README.md](README.md) for user-facing configuration and CLI contracts.
Follow root build instructions; build and run tests remotely through `ssh_ya`.
