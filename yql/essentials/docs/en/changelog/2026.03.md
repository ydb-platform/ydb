## Math module {#math-module}
Added `Math::Expm1` and `Math::Log1p` for accurate computation near zero.

## DateTime module {#datetime-module}
Added `DateTime::Init` for creating a date-time resource from its components.
