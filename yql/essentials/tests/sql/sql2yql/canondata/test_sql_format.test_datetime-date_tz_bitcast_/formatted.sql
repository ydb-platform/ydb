/* postgres can not */
SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Uint8)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Int8)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Uint16)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Int16)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Uint32)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Int32)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Uint64)
;

SELECT
    BITCAST(Yql::TzDate(AsAtom('1,UTC')) AS Int64)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Uint8)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Int8)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Uint16)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Int16)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Uint32)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Int32)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Uint64)
;

SELECT
    BITCAST(Yql::TzDatetime(AsAtom('1,UTC')) AS Int64)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Uint8)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Int8)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Uint16)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Int16)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Uint32)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Int32)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Uint64)
;

SELECT
    BITCAST(Yql::TzTimestamp(AsAtom('1,UTC')) AS Int64)
;
