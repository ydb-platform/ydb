SELECT
    *
FROM
    plato.Input
ORDER BY
    CAST(c AS Decimal (13, 3))
;

SELECT
    *
FROM
    plato.Input
ORDER BY
    CAST(c AS Decimal (13, 3)) DESC
;

SELECT
    *
FROM
    plato.Input
ORDER BY
    CAST(d AS Decimal (5, 1))
;

SELECT
    *
FROM
    plato.Input
ORDER BY
    CAST(d AS Decimal (5, 1)) DESC
;
