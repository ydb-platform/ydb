/* postgres can not */
pragma EmitAggApply;
pragma EmitTableSource;

$data = [
    <|
        x: (
            1,
            just(2),
            NULL,
            Nothing(Int32?),
            Nothing(pgint4)
        )
    |>
];

SELECT
    x.0,
    x.1,
    x.2,
    x.3,
    x.4
FROM
    AS_TABLE($data)
;
