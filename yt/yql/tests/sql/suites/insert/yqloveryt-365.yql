/* dq can not */
/* dqfile can not */
use plato;
PRAGMA RuntimeUserAttrs;

INSERT INTO @a SELECT JUST("{a=b}");
COMMIT;
$b = SELECT * FROM @a;
$a = CAST($b as String);
INSERT INTO Output WITH USER_ATTRS=$a SELECT 1;

