use plato;

-- Materialize full table, then select only some columns.
-- Without prune_unused_columns, all columns must be preserved in the materialized table.
MATERIALIZE Input ON plato WITH prune_unused_columns INTO $result;
SELECT key, value FROM $result;
