PRAGMA yt.UseQLFilter;

INSERT INTO plato.Output WITH TRUNCATE
SELECT *
FROM plato.Input
WHERE a > 0
UNION ALL
SELECT *
FROM plato.Input
WHERE a > 1
;

SELECT *
FROM plato.Input
WHERE a > 0
UNION ALL
SELECT *
FROM plato.Input
WHERE a > 1
;

SELECT *
FROM plato.Input
WHERE a > 2
UNION ALL
SELECT *
FROM plato.Input
WHERE a > 3
;

SELECT *
FROM plato.Input
WHERE a > 4
;
