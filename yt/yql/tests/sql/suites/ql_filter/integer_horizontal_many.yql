PRAGMA yt.UseQLFilter;

INSERT INTO plato.Output WITH TRUNCATE
SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 0
UNION ALL
SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 1
;

SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 0
UNION ALL
SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 1
;

SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 2
UNION ALL
SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 3
;

SELECT a
FROM plato.CONCAT(Input, Input2)
WHERE a > 4
;
