PRAGMA yt.UseQLFilter;

INSERT INTO plato.Output
SELECT
    *
FROM
    plato.CONCAT(Input, Input2)
WHERE
    a > 5 AND a < 50
;

INSERT INTO plato.Output
SELECT
    *
FROM
    plato.CONCAT(Input, Input2)
WHERE
    f IS NULL OR f > 50
;
