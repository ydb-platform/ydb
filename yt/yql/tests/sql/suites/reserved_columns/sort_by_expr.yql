-- ORDER BY over an expression makes YQL generate auxiliary "_yql_column_N" sort columns:
-- they are internal and must not be rejected by the reserved prefix check
INSERT INTO plato.Output WITH TRUNCATE
SELECT key, subkey, value
FROM plato.Input
ORDER BY CAST(key AS Int32) DESC, subkey;
