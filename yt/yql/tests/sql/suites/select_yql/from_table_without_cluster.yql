/* dqfile can not - missing langver support */
/* hybridfile can not - missing langver support */
/* custom error: No cluster name given and no default cluster is selected */
PRAGMA YqlSelect = 'force';

SELECT
    key,
    subkey,
    value
FROM
    Input
;
