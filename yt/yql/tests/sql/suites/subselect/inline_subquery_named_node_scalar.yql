/* dqfile can not */
/* hybridfile can not - missing langver support */
/* custom error: Name not found */
USE plato;

-- FIXME(YQL-20998)

$a = (SELECT Max(key) FROM Input);

$b = '1' || (SELECT Max(key) FROM Input);

$c = (SELECT Max(key) FROM Input) || '1';

$d = '1' || (SELECT Max(key) FROM Input) || '1';

SELECT $a, $b, $c, $d;
