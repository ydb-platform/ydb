/* postgres can not */
/* kikimr can not */
/* dqfile can not - missing langver support */
/* hybridfile can not - missing langver support */
pragma yt.EarlyPartitionPruning;

$l = [
<|TablePath:"Input1", x:1|>,
<|TablePath:"Input2", x:2|>,
];

select * from plato.partition_list($l)
where x < 0 and key > "000"; -- predicate is pruning all partitions, but we need to keep at least one to derive type of output
